# Beta Code Review — Findings

Format: `- [Critical|Major|Minor] path:line — problem. Suggested direction for fix.`
Files without findings are fine and are not listed. Coverage is tracked in `coverage.md`.

## Summary

Coverage: 704/704 files (100%). 164 findings after deduplication (cross-assembly root causes merged into one entry each).

| Section | Critical | Major | Minor | Total |
|---|---|---|---|---|
| Cross-assembly (merged) | 3 | 3 | 0 | 6 |
| Reihitsu.Core | 0 | 3 | 15 | 18 |
| Reihitsu.Cli | 0 | 3 | 11 | 14 |
| Reihitsu.Formatter | 4 | 7 | 14 | 25 |
| Reihitsu.Analyzer | 0 | 15 | 35 | 50 |
| Reihitsu.Analyzer.CodeFixes | 3 | 22 | 26 | 51 |
| **Total** | **10** | **53** | **101** | **164** |

Recommended fix order (Criticals first):

1. Expression-body throw conversion — `ExpressionBodyToBlockConverter.cs:74` produces non-compiling `return throw ...;` (also reachable via the RH3202 IDE fix); fix together with the `ValueTask` gap in `ExpressionBodiedTransformUtilities.cs:51`.
2. Comment swallowing on line joins — the merged Critical (`LineBreakTriviaUtilities.cs:196` plus the manual re-implementations in the RH5101/RH5102/RH5111 fixes); one central guard fixes the formatter and all delegating Layout fixes and removes the need for the manual copies.
3. Switch-brace comment deletion — `SwitchCaseBraceRewriter.cs:215` (AddBraces/RemoveBraces), together with the same-family `ControlFlowBraceTransform.cs:48`.
4. Remaining Criticals: `StatementBracesCodeFixProviderBase.cs:105` (deletes the `if (x) `/`else` header), the casing-conversion crash family (`CasingUtilities.cs:104` + `CasingCodeFixProviderBase{T}.cs:101`), interpolation-marker brace corruption (`StringInterpolationUtilities.cs:39`), the RH5601 raw-string tab fix (`RH5601UseTabsCorrectlyCodeFixProvider.cs:35`), and the RH6018/RH6019 sign-gluing fixes (`- -x` → `--x`).
5. Majors next, prioritizing semantics-changing rewrites (`NoSpaceSpacingRule.cs:115` token gluing, RH7103/RH7109 field-initializer reordering, RH7004 using-declaration conversion, RH3001/RH3104 semantic rewrites) and the analyzer/formatter policy conflicts that produce permanent diagnostics or fix ping-pong (the merged using-ordering Major, RH4107/RH4108 overlap, RH5206/RH5401/RH5408/RH5111 divergences); Minors last, batched per family (copy-paste consolidation, `Environment.NewLine`, `ConfigureAwait`).

## Status (closeout, 2026-06-11)

- Coverage: 704/704 files reviewed (100%); the last open file (Reihitsu.Analyzer.CodeFixes\CodeFixResources.cs) was reviewed in the closeout session. See `coverage.md`.
- All cross-checks resolved. Entries describing the same root cause across assemblies were merged into the "Cross-assembly findings (merged)" section below; per-assembly sections keep only single-assembly findings.
- Session history: see `session-prompts.md` in this directory.

## Cross-assembly findings (merged)

Root causes that span assemblies; the constituent per-assembly entries were merged here during closeout.

- [Critical] Comment swallowing on line joins — Reihitsu.Formatter\Pipeline\LineBreaks\LineBreakTriviaUtilities.cs:196 (central defect), re-implemented manually in Reihitsu.Analyzer.CodeFixes\Rules\Layout\RH5101FirstArgumentShouldBeOnSameLineCodeFixProvider.cs:45, RH5102ArgumentsShouldBeOnSingleOrSeparateLinesCodeFixProvider.cs:60 and RH5111AssignmentsMustHaveProperLineBreaksCodeFixProvider.cs:156. `CollapseTokenToSameLine`/`RemoveTrailingEndOfLineTrivia` keep a single-line comment in the previous token's trailing trivia but remove the EOL after it, so the joined token lands inside the comment: `var x = Foo() // note` + `.Bar();` becomes `var x = Foo() // note.Bar();` — the joined code is commented out, output corrupted. Formatter call sites without a comment guard: ChainLineBreakRewriter (`TryCollapseFirstChainDot`, `CollapseMemberBindingToQuestionToken`), TernaryLineBreakRewriter (`MoveQuestionTokenToNextLine`, `NormalizeColonTokenPosition`), BinaryOperatorLineBreakRewriter, LineBreakAssignmentRewriter (all `Normalize*ToSameLine` paths), LineBreakListRewriter (`CollapseFirstElementToSameLine`, `CollapseSeparatorsToPreviousParameterLine`, `CollapseCloseParenToParameterLine`), DeclarationBraceLineBreakRewriter (`CollapseParameterListToDeclarationLine`), PropertyLayoutLineBreakRewriter (`CollapseExpressionBodiedProperty`); the existing `HasCommentDirectlyAbove` guard only covers comments in the moved token's own leading trivia, not end-of-line comments on the previous line. Inherited via `FormatNodeInDocumentAsync` by the delegating Layout code fixes RH5109, RH5110, RH5112, RH5204, RH5205, RH5206, RH5301, RH5303, RH5304, RH5305, RH5306, RH5307, RH5401, RH5408 — their analyzer/fix comment guards only cover the flagged construct, not the whole formatted scope (e.g. a trailing comment on the property signature line, `public int X // note`, passes the RH5408 guard and the collapse joins the accessor list into the comment). The manual code-fix copies add their own variants: RH5101 keeps a trailing comment after `(` and deletes the EOL (`Foo( // note` + argument becomes `Foo( // note<arg>`) and is additionally non-convergent when a standalone comment line precedes the first argument (comment and EOL kept, diagnostic persists); RH5102 keeps comments but strips every EOL from the rebuilt argument leading trivia, so `Foo(a,` / `// note` / `b)` becomes `// note    b` — the argument is swallowed; RH5111's `CollapseOperatorToTargetLine`/`CollapseValueToEqualsLine` join `x // note` + `= 5` into `x // note = 5`. Fix centrally: refuse to join when the previous token's trailing trivia (or the gap) contains comment trivia, and delegate the three manual fixes to the formatter.
- [Critical] Casing conversion crash / empty identifier — Reihitsu.Core\CasingUtilities.cs:104 + Reihitsu.Analyzer.CodeFixes\Base\CasingCodeFixProviderBase{T}.cs:101. `ToCamelCase`/`ToUnderLineCamelCase` crash (StringBuilder indexer out of range) for valid identifiers without letters/digits, and `ToPascalCase("___")` returns an empty string. `CasingCodeFixProviderBase` invokes `_casingConversion(GetIdentifier(node))` without any guard, so every RH40xx/RH41xx casing fix (25 exported providers) executes the defect inside the code action: `var _ = Foo();` (RH4115 flags `_`), the common lambda parameter `__` (RH4105 excludes only `_`), and a field named `__` (RH4106/RH4107) throw; the empty `___` result is passed to `Renamer.RenameSymbolAsync` (throws) or `ReplaceIdentifier` (writes an empty identifier into the document). Fix the Core utilities (guard against an empty `ConvertToPascalCase` result and return the input unchanged) and guard the provider (skip registration when the conversion throws or returns an empty/invalid identifier).
- [Critical] Interpolation-marker removal corrupts escaped braces — Reihitsu.Core\StringInterpolationUtilities.cs:39, executed by both the formatter's `InterpolationMarkerRemovalTransform` and the RH3204 code fix. `RemoveInterpolationMarkers` changes string values when the interpolated string contains escaped braces: in `$"a {{b}}"` the `{{`/`}}` mean literal `{`/`}`, but after dropping the `$` the literal `"a {{b}}"` contains two braces — the runtime value silently changes from `a {b}` to `a {{b}}`. The RH3204 analyzer flags such strings (`HasInterpolations` only checks for `InterpolationSyntax`), so analyzer, fix, and formatter all corrupt program behavior. Either unescape `{{`/`}}` when removing the marker or skip strings whose text contains brace escapes (analyzer and utility).
- [Major] Three divergent using-ordering policies — Reihitsu.Core\UsingDirectiveOrderingUtilities.cs (aliases sorted by alias identifier), Reihitsu.Formatter\Pipeline\UsingDirectives\UsingGrouping.cs:36 (groups by root namespace, aliases sorted by `"alias=target"`), Reihitsu.Analyzer\Rules\Organization\RH7204UsingAliasDirectivesMustBeOrderedAlphabeticallyByAliasNameAnalyzer.cs:52 (alias name) vs. RH7207 (target root namespace first; additionally carries a third private copy of the grouping policy — `GetRootNamespace`/`GetUsingTypeOrder`/`GetNamespaceGroupOrderKey` duplicated from `UsingGrouping`). For `using Z = A.B; using A = Z.Q;` the formatter/RH7207 order `[Z=A.B, A=Z.Q]` violates RH7204 and the RH7204 ordering violates RH7207 — fix ping-pong; files ordered by one policy can be flagged/reordered by the other (oscillation between analyzer fix and formatter). The code-fix side routes all RH72xx fixes through `UsingDirectiveCodeFixUtilities.OrganizeScopeUsingsAsync`, which executes the formatter's `UsingDirectiveOrderingPhase` — i.e. the RH7204 fix applies the conflicting formatter policy and cannot satisfy its own analyzer (the code action no-ops while the diagnostic persists). One alias-ordering policy must win; consolidate it in Core. `UsingGrouping` also shares Core's `global::` misclassification (see the Core Minor on UsingDirectiveOrderingUtilities.cs:44).
- [Major] Three hand-rolled modifier switches — Reihitsu.Core\DeclarationModifierUtilities.cs:79, Reihitsu.Analyzer\Core\DocumentationAnalysisUtilities.cs:593, and the analyzer base property all re-implement `MemberDeclarationSyntax.Modifiers`/`.WithModifiers` (available since Roslyn 5.3) as 12-case switches with three different unknown-kind behaviors: the Core variant throws `ArgumentOutOfRangeException` for every other member kind — including `IncompleteMemberSyntax`, which appears in `TypeDeclaration.Members` while the user is typing, and `EnumMemberDeclarationSyntax` — a crash path in analyzers on incomplete code; the DocumentationAnalysisUtilities copy silently returns `default`. Replace all three with the base-class members.
- [Major] `GetDocumentationPrefix` heuristic duplicated and flawed — Reihitsu.Formatter\Pipeline\DocumentationComments\DocCommentElementNormalizer.cs:142 + Reihitsu.Analyzer.CodeFixes\Core\DocumentationCommentCodeFixUtilities.cs:52. Both take everything before the first `<` on the start-tag line as the continuation prefix: for a multi-line inline element that starts mid-sentence (`/// Stuff <c>code` … `/// more</c>`) the formatter repeats "Stuff " on every rebuilt continuation line, and the RH8304 fix inserts `/// Some text ` as the new line's prefix, duplicating the sentence text. The formatter's `GetElementContentLines` additionally trims every line, destroying significant indentation inside elements like `<code>`. Derive the prefix from the `///` exterior plus one space only, and share one helper in Core.

## Reihitsu.Core

- [Minor] Reihitsu.Core\CasingUtilities.cs:28 — `IsPascalCase` uses LINQ over the string (`Skip(1).Any(...)`) while `IsCamelCase` has an allocation-free span implementation. Inconsistent and slower; add the span path.
- [Minor] Reihitsu.Core\CasingUtilities.cs:114 — naming inconsistency: `ToUnderLineCamelCase` vs. `IsUnderlineCamelCase` ("UnderLine"/"Underline"). Pick one spelling.
- [Minor] Reihitsu.Core\DeclarationModifierUtilities.cs:56 — `AddAccessibilityModifier`: when the accessibility token is not first (`static public`), the replacement token is inserted at index 0 with the old accessibility token's trivia, leaving the original leading trivia (indentation, doc comments) on the now-second token. Currently relies on the formatter to clean up; at minimum document that contract.
- [Minor] Reihitsu.Core\StringInterpolationUtilities.cs:24 — `Contents.Count == 0` early return is redundant; `OfType<InterpolationSyntax>().Any()` already covers it.
- [Minor] Reihitsu.Core\StringInterpolationUtilities.cs:49 — re-parsed expression is validated only via `IsMissing`; `ContainsDiagnostics` is the stricter guard against silently producing broken replacements. Also the doc of `RemoveLeadingDollarMarkers` ("Start token text") is stale — it receives the full expression text. (Batch 15: the RH3204 code fix consumes this path unchanged, so a defective re-parse would be written into the document.)
- [Major] Reihitsu.Core\AttributeTargetUtilities.cs:22 — `GetAttributeLists`/`WithAttributeLists`/`TryResolveImplicitTarget` miss `DestructorDeclarationSyntax`, `EnumMemberDeclarationSyntax` (both carry attributes) and lambda expressions (C# 10 lambda attributes); the silent `[]`/`owner` fallback makes those attributes invisible to every caller. `MemberDeclarationSyntax.AttributeLists` plus a lambda case covers all of it and shrinks the switch.
- [Major] Reihitsu.Core\RegionDirectiveUtilities.cs:164 — duplication: `TryFindMatchingDirective` (flat directive-list matching) and `SyntaxTreeRegionSearcher` (stateful tree walk) implement the same "find matching #region/#endregion" concern twice in the same assembly with independently maintained nesting logic. Consolidate on the flat-list approach.
- [Minor] Reihitsu.Core\RegionDirectiveUtilities.cs:31 — the `BlockSyntax { Parent: not TypeDeclarationSyntax ... }` guard can never fail (blocks are never direct children of types/namespaces/compilation units), and `case StatementSyntax` below covers blocks anyway. Dead condition.
- [Minor] Reihitsu.Core\RegionDirectiveUtilities.cs:198 — `BelongsToType` enumerates all `DescendantNodes()` once per directive → O(directives × nodes) inside `GetTopLevelRegions`. Precompute the nested-type spans once.
- [Major] Reihitsu.Core\SyntaxTreeRegionSearcher.cs:34 — `_nestedRegionLevel` is never reset in `SearchRegionPair` (the other three fields are). Reusing one searcher instance after a search that ended with a non-zero level returns wrong matches. Latent state bug.
- [Minor] Reihitsu.Core\SyntaxTreeRegionSearcher.cs:60 — `node != null` on the `SyntaxNodeOrToken` struct compares against an implicitly converted default — works, but obscures intent; compare with `SyntaxKind.None` explicitly.
- [Minor] Reihitsu.Core\SyntaxTreeRegionSearcher.cs:53 — `throw new NotSupportedException()` without a message.
- [Minor] Reihitsu.Core\SyntaxTreeRegionSearcher.cs:12 — mutable stateful public class for a pure query; a static method with local state would remove the reuse hazard above (or the class disappears entirely with the duplication finding).
- [Minor] Reihitsu.Core\FormattingTextAnalysisUtilities.cs:71 — `GetStringLineIndices` doc claims raw strings only, but the implementation tracks every string literal and every interpolated string; doc/impl mismatch. Also two full `DescendantNodes()` traversals where one suffices.
- [Minor] Reihitsu.Core\FormattingTextAnalysisUtilities.cs:79 — `FullSpan` includes leading/trailing trivia, so comment/blank lines adjacent to a literal are marked as string lines. Over-broad; use `Span`.
- [Minor] Reihitsu.Core\FormattingTextAnalysisUtilities.cs:23 — `ContainsNonWhitespace` is a trivial alias for `!string.IsNullOrWhiteSpace`; adds an indirection without value.
- [Minor] Reihitsu.Core\UsingDirectiveOrderingUtilities.cs:44 — `using global::System;` is classified as `OtherNamespace` because the alias-qualified name string starts with `global::`. Strip the `global::` prefix before matching.
- [Minor] Reihitsu.Core\UsingDirectiveOrderingUtilities.cs:189 — `OrderSubset` reassigns leading trivia positionally within each group, so a comment written for one specific using directive stays at its old position and ends up on a different directive after sorting.

### Open cross-checks (all resolved)

- `ModifierOrderingUtilities.OrderModifiersForRh7105`/`OrderModifiersForRh7106` move tokens together with their trivia; if a doc comment/indentation sits on the first modifier, reordering can drag it into the middle of the modifier list. Verify whether the RH7105/RH7106 code fixes compensate (CodeFixes batch). — RESOLVED (batch 15, re-verified batch 18): `ModifierOrderingCodeFixProviderBase.NormalizeModifierTrivia` rebuilds the token list (original first leading trivia kept, all other tokens get a single trailing space), so the trivia drag does not reach fix output; the normalization deletes comments between modifiers instead (see batch 15 Minor).
- CasingUtilities crash/empty-result inputs (`__`, `___`, `_` locals) vs. the RH41xx fixes. — RESOLVED (batch 18): no guard exists in `CasingCodeFixProviderBase`; the Core finding is escalated to Critical (merged into the Cross-assembly casing Critical at closeout).
- RH7204 vs. RH7207 alias-ordering policy on the fix side. — RESOLVED (batch 18): both fixes route through `UsingDirectiveCodeFixUtilities.OrganizeScopeUsingsAsync` and therefore apply the *formatter* policy (`UsingGrouping`: aliases sorted by target root namespace, then `alias=target`), so the two fixes do not oscillate against each other — but the RH7204 fix cannot satisfy the RH7204 analyzer (alias-name order): for `using Z = A.B; using A = Z.Q;` the fix's output keeps `Z=A.B` first and the diagnostic persists while the code action no-ops (now part of the merged Cross-assembly using-ordering Major).

## Reihitsu.Cli

- [Major] Reihitsu.Cli\Abstractions\DefaultFileSystem.cs:74 — encoding round-trip: files without BOM are read as UTF-8 with silent replacement-character fallback and written back as UTF-8. A legacy Windows-1252/Latin-1 file that needs formatting gets its non-ASCII characters replaced with U+FFFD — silent content corruption. Decode with a throwing UTF-8 decoder and skip the file with a warning when decoding fails.
- [Major] Reihitsu.Cli\Diff\LcsComputer.cs:18 — full O(n×m) LCS table: two 10 000-line files allocate a ~400 MB int table. `--dry-run` over large files can OOM. Trim the common prefix/suffix before computing (formatting diffs are localized), or cap/fall back.
- [Major] Reihitsu.Cli\FormatCommandHandler.cs:102 — zero collected .cs files returns `ExitCodes.Error` (2). Running over a directory tree that simply contains no C# files is not an error; this breaks scripted/CI invocations. Return Success, or document the contract explicitly.
- [Minor] Reihitsu.Cli\FormatCommandHandler.cs:104 — "No .cs files found." goes to stdout while other error paths use stderr; inconsistent stream usage for an error-exit message.
- [Minor] Reihitsu.Cli\FormatCommandHandler.cs:229 — catch-all in `ProcessFileAsync` also swallows `OperationCanceledException`, turning cancellation into a per-file error while the loop continues. Rethrow OCE.
- [Minor] Reihitsu.Cli\FormatCommandHandler.cs:212 — only `await` in the project without `ConfigureAwait(false)`.
- [Minor] Reihitsu.Cli\FormatCommandHandler.cs:274 — `CollectFiles` doesn't deduplicate overlapping inputs (same file passed directly and via its parent directory) → file formatted and counted twice.
- [Minor] Reihitsu.Cli\FormatCommandHandler.cs:262 — encoding detection re-reads the file after the content was already read once; combined with `DefaultFileSystem.DetectEncodingAsync` reading **all** bytes for a 4-byte BOM sniff, every changed file is read twice in full.
- [Minor] Reihitsu.Cli\Program.cs:47 — path-existence validation uses `File`/`Directory` directly while all other I/O goes through `IFileSystem`; this branch is unfakeable in the `Program.Main()`-based end-to-end tests and duplicates the existence logic in `CollectFiles`.
- [Minor] Reihitsu.Cli\Program.cs:92 — `--check` and `--dry-run` are not validated as mutually exclusive; combined, check silently wins and no diff is produced.
- [Minor] Reihitsu.Cli\Program.cs:66 — full `CancellationToken` plumbing ends in `CancellationToken.None`; Ctrl+C (`Console.CancelKeyPress`) is never wired up, so graceful cancellation is dead code end to end.
- [Minor] Reihitsu.Cli\Program.cs:140 — usage gaps: `-h` alias undocumented, usage printed to stdout even on the unknown-option error path, no `--` separator for paths starting with `-`.
- [Minor] Reihitsu.Cli\Abstractions\IConsoleOutput.cs:17 — asymmetric API: `WriteLine` synchronous, `WriteErrorLineAsync` asynchronous. Pick one model.
- [Minor] Reihitsu.Cli\DiffGenerator.cs:59 — unified-diff conformance: zero-count ranges don't follow the "line before" numbering convention, there is no `\ No newline at end of file` marker, and `LineSplitter` ignores lone `\r` separators. Output may not survive `git apply`.

## Reihitsu.Formatter

### Root, pipeline infrastructure, StructuralTransforms, UsingDirectives (batch 3)

- [Critical] Reihitsu.Formatter\Pipeline\StructuralTransforms\ExpressionBodyToBlockConverter.cs:74 — `=> throw ...` on a value-returning member is converted to `return throw ...;` (CS8115 — does not compile). Affects methods, operators, conversions, indexers, local functions. The unit test `ConvertsExpressionBodiedMethodThrowingException` (ExpressionBodiedTransformTests.cs:439) even enshrines the broken output as expected. `ThrowExpressionSyntax` must become a `ThrowStatementSyntax`. Code-fix side verified (batch 15): the RH3202 fix (Reihitsu.Analyzer.CodeFixes\Rules\Clarity\RH3202ExpressionStyleMethodsShouldNotBeUsedCodeFixProvider.cs:34) delegates to `FormatNodeInDocumentAsync`, which runs this transform, so the IDE fix on `int M() => throw ...;` produces the same non-compiling `return throw ...;`. RH3203 (constructors) is unaffected because constructors use the expression-statement form.
- [Critical] Reihitsu.Formatter\Pipeline\StructuralTransforms\ControlFlowBraceTransform.cs:48 — `WrapWithBraces` replaces the statement's leading/trailing trivia with elastic markers; comments attached to the unbraced statement (`if (x)\n    // important\n    Foo();` or `Foo(); // note`) are silently deleted. (Batch 16: inherited by the Layout code fixes that delegate statement-scope formatting to `FormatNodeInDocumentAsync` — RH5204, RH5205, RH5206, RH5303, RH5304, RH5305 — when the formatted scope contains unbraced control-flow children. Batch 17: also RH5306, RH5307, whose formatting scope is the whole enclosing statement.)
- [Critical] Reihitsu.Formatter\Pipeline\StructuralTransforms\ExpressionBodiedTransformUtilities.cs:51 — `IsNonGenericTaskReturnType` misses `ValueTask`: `async ValueTask M() => await X();` is converted to `{ return await X(); }`, which does not compile for void-typed awaits. Add ValueTask to the non-generic task detection.
- [Major] Reihitsu.Formatter\Pipeline\StructuralTransforms\ExpressionBodiedMethodTransform.cs:69 — method and constructor transforms pass `default` as open-brace leading trivia while the other five transforms pass `ArrowToken.LeadingTrivia`; a comment before `=>` is lost for methods/constructors only. Inconsistent trivia contract across sibling transforms.
- [Major] Reihitsu.Formatter\Pipeline\StructuralTransforms\EmptyTypeDeclarationSemicolonTransform.cs:41 — re-implements `EmptyTypeDeclarationSemicolonAnalysisUtilities` (Core) instead of reusing it, and has already diverged: Core's `ShouldReport` returns `false` when the tree options are not `CSharpParseOptions`, the transform's `SupportsLanguageVersion` returns `true`.
- [Major] Reihitsu.Formatter\Pipeline\StructuralTransforms\FieldDeclarationSplitTransform.cs:105 — splitting `int a, b;` drops the separator tokens' trivia; a comment after the comma (`int a, // first`) is deleted.
- [Minor] Reihitsu.Formatter\Pipeline\StructuralTransforms\FieldDeclarationSplitTransform.cs:145 — `SplitFields` rebuilds the member list (new green nodes) even when no field needs splitting — missing early-out; interfaces are skipped entirely although C# 8+ interfaces can declare static fields.
- [Major] Reihitsu.Formatter\ReihitsuFormatterHelpers.cs:75 — `IsAutoGeneratedSource` scans every comment in the whole file; any comment merely mentioning `<auto-generated` (e.g. in a doc remark) silently disables formatting for the entire file. Roslyn convention checks only the file header (leading trivia of the first token).
- [Minor] Reihitsu.Formatter\ReihitsuFormatterHelpers.cs:24 — `DetectEndOfLine` materializes every EOL trivia as a string into a list; two counters (CRLF/LF) suffice.
- [Minor] Reihitsu.Formatter\ReihitsuFormatterHelpers.cs:129 — `ComputeTokenColumn` calls `root.ToFullString()` (full document text) per invocation; `FormatNodeInDocumentAsync` pays this twice per fix.
- [Minor] Reihitsu.Formatter\ReihitsuFormatter.cs:91 — `FormatNode` lacks the syntax-error/auto-generated guards every other entry point has; guard or document the contract difference.
- [Minor] Reihitsu.Formatter\FormattingContext.cs:5 — doc claims "each phase receives a fresh context snapshot"; in reality one shared instance is passed to all phases. Stale doc.
- [Minor] Reihitsu.Formatter\Pipeline\UsingDirectives\UsingDirectiveOrderingSafety.cs:48 — per-call, uncompiled `Regex` duplicating the structural directive check that already ran; hoist a static compiled regex or drop one of the two checks.
- [Minor] Reihitsu.Formatter\ReihitsuFormatterHelpers.cs:185 — `internal` members (`HasCommentDirectlyAbove`, `IsCommentTrivia`, `StartsOnNewLine`) live in the "Private methods" region; region label contradicts accessibility.

### BlankLines, SwitchCaseBraces, LineBreaks (batch 4)

- [Critical] Reihitsu.Formatter\Pipeline\SwitchCaseBraces\SwitchCaseBraceRewriter.cs:215 — `AddBraces` replaces trivia wholesale and deletes comments at four positions: the last label's trailing comment (`case 1: // note`), leading comments of the first statement, the trailing comment of the last statement, and comments around the trailing `break`. `RemoveBraces` likewise drops comments attached to the block's braces. Same comment-deletion family as ControlFlowBraceTransform.
- [Major] Reihitsu.Formatter\Pipeline\LineBreaks\BracePlacer.cs:159 — chained edits operate on stale tokens: `NormalizeContainedBlock` and `LineBreakContainedBlockRewriter.NormalizeIfStatement` pass the original brace tokens into three successive edit steps; `TokenLocator.GetCurrentToken` re-resolves by `SpanStart`, which shifts after the first edit, and `EnsureFirstContentOnNewLine` never re-resolves at all — later steps silently no-op, leaving under-formatted output in a single pass.
- [Minor] Reihitsu.Formatter\Pipeline\LineBreaks\DeclarationBraceLineBreakRewriter.cs:680 — the dispatch switch misses `ConversionOperatorDeclarationSyntax` and `DestructorDeclarationSyntax`; their bodies and parameter lists never get Allman normalization although plain operators and methods do.
- [Minor] Reihitsu.Formatter\Pipeline\LineBreaks\LineBreakDetection.cs:36 — `IsMultiLine` materializes a `SourceText` (`node.GetText()`) per call and is used in hot list/ternary paths; an EOL-trivia scan suffices.
- [Minor] Reihitsu.Formatter\Pipeline\LineBreaks\LineBreakTriviaUtilities.cs:46 — `StripTrailingWhitespace` removes *all* whitespace trivia in the list, not just trailing; name promises less than it does (e.g. the space before a kept comment also disappears).
- [Minor] Reihitsu.Formatter\Pipeline\LineBreaks\TokenGapNormalizer.cs:104 — three near-identical `NormalizeGapBefore*` variants are copy-paste with subtle differences (one preserves previous-token whitespace, two don't); consolidate to one implementation with flags before they diverge further.

### HorizontalSpacing, Indentation, Region/DocComments/RawString/Cleanup/LineEndings (batches 5-6)

- [Major] Reihitsu.Formatter\Pipeline\HorizontalSpacing\NoSpaceSpacingRule.cs:115 — adjacent unary operators are glued together: `- -x` (negate of negate) becomes `--x` (pre-decrement) because `IsUnarySignToken`/`IsPrefixTightUnaryOperatorToken` demand zero spacing without checking that the merged tokens re-lex to a different operator. Silent semantics change; same for `+ +x` → `++x`. Keep one space when the adjacent token starts with the same operator character.
- [Major] Reihitsu.Formatter\Pipeline\RegionFormatting\NestedRegionRemovalStep.cs:75 — `IsWithinElementBody` is a verbatim copy of `RegionDirectiveUtilities.IsWithinElementBody` (Core), including its dead `BlockSyntax { Parent: not ... }` guard. Reuse Core.
- [Minor] Reihitsu.Formatter\Pipeline\Indentation\Contributors\ListPatternContributor.cs:33 — uses `GetColumn` (original column) where every sibling contributor uses `GetAdjustedColumn`; list patterns align to the pre-indentation bracket column and drift when the bracket line is re-indented.
- [Minor] Reihitsu.Formatter\Pipeline\Indentation\IndentationRewriter.cs:223 — `CountLineBreaks` ignores newlines embedded in multi-line comments (and the EOL inside doc-comment structures), so the simulated line counter can drift from real line numbers inside leading trivia; comment lines may pick up another line's layout entry.
- [Minor] Reihitsu.Formatter\Pipeline\RegionFormatting\NestedRegionRemovalStep.cs:31 — guards `root.SyntaxTree?.GetText()` against a null tree but then dereferences `root.SyntaxTree.WithChangedText(...)` unconditionally; the fallback branch can only end in a NullReferenceException. Also the only mid-pipeline full re-parse (text edit + `WithChangedText`).

## Reihitsu.Analyzer

### Infrastructure: Base, Core, Data, Extensions, Enumerations (batch 7)

- [Major] Reihitsu.Analyzer\Base\DiagnosticAnalyzerBase{TAnalyzer}.cs:108 — `CreateDiagnostic(ImmutableArray<Location>)` uses `locations[1]` as the primary location instead of `locations[0]`, and the additional locations (`Skip(1)`) include the primary again. Every multi-location diagnostic gets the wrong squiggle.
- [Major] Reihitsu.Analyzer\Core\DocumentationAnalysisUtilities.cs:575 — `IsPurePrivateDeclaration` requires an explicit `private` modifier; members with implicit private accessibility are classified as "non-private". The NonPrivate documentation rules flag implicitly-private members (false positive) and the Private rules skip them (false negative).
- [Major] Reihitsu.Analyzer\Core\FluentChainAnalysisHelper.cs:118 — `HasCommentDirectlyAbove`/`IsCommentTrivia` are verbatim copies of `ReihitsuFormatterHelpers` (Formatter assembly). Shared analyzer/formatter heuristics belong in Reihitsu.Core; the two copies can silently diverge.
- [Minor] Reihitsu.Analyzer\Base\DiagnosticAnalyzerBase{TAnalyzer}.cs:124 — `EnableConcurrentExecution` only under `#if RELEASE`; Debug builds run the analyzers with different concurrency semantics than the shipped package, hiding concurrency bugs in local runs.
- [Minor] Reihitsu.Analyzer\Base\DiagnosticAnalyzerBase{TAnalyzer}.cs:47 — parameter name `tileResourceName` (typo for "title"), repeated in `StructEqualityPerformanceAnalyzerBase`.
- [Minor] Reihitsu.Analyzer\Core\DirectDocumentationSyntaxChecker.cs:44 — `GetDirectTags` silently falls back to a full `DescendantNodes()` search when no *direct* tag matches; a `<summary>` nested inside `<remarks>` satisfies a "has direct summary" query. Name and behavior disagree.
- [Minor] Reihitsu.Analyzer\Base\StatementShouldBePrecededByABlankLineAnalyzerBase{TStatement,TAnalyzer}.cs:86 — "blank line" is defined as "≥ 2 EOLs in the gap", so a comment line between statements counts as a blank line; the formatter's `TokenGapAnalysis` requires a whitespace-only line. Analyzer and formatter definitions of "blank line" diverge (same in the FollowedBy base).
- [Minor] Reihitsu.Analyzer\Data\ConfigurationManager.cs:465 — validation-error spans are built from `Utf8JsonReader` byte offsets but consumed as UTF-16 `SourceText` positions; any non-ASCII character in the config (e.g. `©` in the copyright text) shifts all subsequent squiggles.
- [Minor] Reihitsu.Analyzer\Base\DocumentationModeAnalysisContextExtensions.cs:24 — two overloads with identical copy-pasted lambda bodies; forward one to the other.

### Rules: Analyzer, Performance, Clarity, Design (batches 8-9)

- [Minor] Reihitsu.Analyzer\Rules\Clarity\RH3002StatementMustNotUseUnnecessaryParenthesesAnalyzer.cs:31 — resource names passed as string literals (`"RH3002Title"`) instead of `nameof(AnalyzerResources...)`; only rule in the assembly without the compile-time check.
- [Minor] Reihitsu.Analyzer\Rules\Clarity\RH3202ExpressionStyleMethodsShouldNotBeUsedAnalyzer.cs:46 — handler named `OnConstructorDeclaration` with doc referencing `ConstructorDeclaration`, but it handles method declarations. Copy-paste from RH3203.
- [Minor] Reihitsu.Analyzer\Rules\Clarity\RH3101DoNotPrefixCallsWithBaseUnlessLocalImplementationExistsAnalyzer.cs:44 — `AreEquivalent` and `BuildComparisonExpressions` are duplicated nearly verbatim in RH3105; extract a shared speculative-rebinding helper.
- [Minor] Reihitsu.Analyzer\Rules\Analyzer\RH0002DocumentationModeMustNotBeNoneAnalyzer.cs:63 — only the first syntax tree's parse options are checked (`break` after first `CSharpParseOptions`); a compilation with mixed documentation modes is reported incompletely.
- [Minor] Rules\Design\RH2003/RH2006/RH2007 + Rules\Performance\RH1002 (e.g. Reihitsu.Analyzer\Rules\Design\RH2003NotImplementedExceptionShouldNotBeUsedAnalyzer.cs:51) — invocation/creation handlers run `GetSymbolInfo`/`GetTypeInfo` and compare via `ToDisplayString()` allocation for *every* node in the compilation before any cheap syntactic pre-filter (e.g. method name text "Assert"/"Fail"). Hoist a syntax-based name check before semantic binding.
- [Minor] Reihitsu.Analyzer\Rules\Design\RH2002AsyncVoidShouldNotBeUsedAnalyzer.cs:116 — only `MethodKind.Ordinary`: async-void local functions and lambdas are not covered (and they are the more dangerous shape). If deliberate, document it in RH2002.md.

### Rules: Documentation (batch 13)

- [Minor] Reihitsu.Analyzer\Rules\Documentation\RH8030ElementDocumentationMustHaveSummaryTextAnalyzer.cs:55 — the dedicated `EnumMemberDeclarationSyntax` branch is unreachable: enum members are `MemberDeclarationSyntax` and always take the first branch. Dead code.
- [Minor] Reihitsu.Analyzer\Rules\Documentation\RH8201InheritdocShouldBeUsedAnalyzer.cs:60 — only `SingleLineDocumentationCommentTrivia` is inspected; an override documented with `/** ... */` is never flagged.
- [Minor] Reihitsu.Analyzer\Rules\Documentation\RH8402FileMustStartWithConfiguredXmlStyleCopyrightHeaderAnalyzer.cs:96 — `sourceText.ToString()` materializes the whole file for a prefix comparison; compare the prefix range directly.
- [Minor] systemic — stale copy-pasted XML summaries reference the wrong syntax kind in several rules ("Analyzing all LogicalNotExpression occurrences" in RH2001 (Reihitsu.Analyzer\Rules\Design\RH2001PrivateAutoPropertiesShouldNotBeUsedAnalyzer.cs:41)/RH8201 (Reihitsu.Analyzer\Rules\Documentation\RH8201InheritdocShouldBeUsedAnalyzer.cs:41), ConstructorDeclaration in RH3202, parameter text in RH4106). One cleanup pass over handler summaries.
- Note: the entire RH8001-RH8029 private/non-private pair family inherits the `IsPurePrivateDeclaration` misclassification of implicitly-private members (see batch 7 Major).

### Rules: Organization (batch 12)

- [Minor] Reihitsu.Analyzer\Rules\Organization\RH7301RegionsShouldMatchAnalyzer.cs:60 — parses directives with magic offsets (`Substring(10)`, `Substring(8)`, literal `" // "`) instead of `RegionDirectiveUtilities.GetRegionDescription`/`GetEndRegionDescription`; a `#region` with two spaces after the keyword is treated as mismatched. Also the only consumer of the stateful `SyntaxTreeRegionSearcher`.
- [Minor] Reihitsu.Analyzer\Rules\Organization\RH7304RegionDirectivesMustUseConsistentIndentationAnalyzer.cs:58 — `GetExpectedIndentation` runs a full `DescendantTokens()` scan per region pair; O(pairs × tokens) per tree.

### Rules: Spacing (batch 11)

- [Minor] Reihitsu.Analyzer\Rules\Spacing\RH6021ColonsMustBeSpacedCorrectlyAnalyzer.cs:50 — only `' '` is accepted around the base-list colon while every sibling rule also accepts `'\t'`; tab-indented `: Base` lines are false positives. Additionally only `BaseListSyntax` colons are covered although the formatter also spaces constructor-initializer colons (`: base()`).
- [Minor] Reihitsu.Analyzer\Rules\Spacing\RH6011OpeningGenericBracketsMustBeSpacedCorrectlyAnalyzer.cs:50 (and RH6012) — generic-bracket rules cover only `TypeArgumentListSyntax`; `TypeParameterListSyntax` (declarations, `class Foo <T>`) is not checked, although the formatter normalizes both. Titles promise more than the checks deliver (only the space *before* the bracket is verified).
- [Minor] Rules\Spacing (systemic; e.g. Reihitsu.Analyzer\Rules\Spacing\RH6003SemicolonsMustBeSpacedCorrectlyAnalyzer.cs:56) — the backward/forward whitespace text-scan loop is copy-pasted across ~10 of the 22 rules; extract a shared helper. Also: text-scan rules flag whitespace that the formatter deliberately preserves before inline comments (e.g. `( // note`), so such shapes stay flagged even after `reihitsu-format`.

### Rules: Naming (batch 10)

- [Major] Reihitsu.Analyzer\Rules\Naming\RH4107ProtectedFieldCasingAnalyzer.cs:48 / RH4108InternalFieldCasingAnalyzer.cs — a `protected internal` field matches both rules simultaneously: RH4107 demands `_camelCase`, RH4108 demands `PascalCase`. Both diagnostics fire and cannot both be satisfied. The field rules must partition compound accessibilities explicitly (the property rules RH4111-4114 have the same overlap but agree on PascalCase, so only fields conflict). (Batch 18: the fix side executes the conflict — RH4107's fix applies `ToUnderLineCamelCase`, RH4108's applies `ToPascalCase`, so applying both offered code actions renames the field back and forth, ping-ponging the whole solution via Renamer.)
- [Minor] Reihitsu.Analyzer\Rules\Naming\RH4106PrivateFieldCasingAnalyzer.cs:15 — XML summary says "Method parameter names should be in _camelCase"; copy-paste doc error (it is the private-field rule).
- [Minor] Reihitsu.Analyzer\Rules\Naming\RH4116TupleElementCasingAnalyzer.cs:15 — RH4116 (tuple *types*) and RH4118 (tuple *expressions*) carry identical XML summaries and near-identical class names; disambiguate so the scopes are visible.
- [Minor] Reihitsu.Analyzer\Rules\Naming\RH4005InterfaceNameCasingAnalyzer.cs:51 — the `I`-prefix check is only `StartsWith("I")`; any PascalCase name that happens to begin with I ("Item", "Index") passes as prefixed. Check that the second character is uppercase.

### Rules: Layout (batch 14)

- [Major] Reihitsu.Analyzer\Rules\Layout\RH5006ForeachStatementsShouldBePrecededByABlankLineAnalyzer.cs:32 — only `SyntaxKind.ForEachStatement` is registered; deconstruction foreach (`foreach (var (a, b) in ...)`, `ForEachVariableStatement`) is never flagged although the formatter's `BlankLineStatementSpacingRewriter` matches `CommonForEachStatementSyntax` and inserts the blank line. The CLI silently reformats what the IDE never reports. Register both kinds.
- [Major] Reihitsu.Analyzer\Rules\Layout\RH5009GotoStatementsShouldBePrecededByABlankLineAnalyzer.cs:32 — only `SyntaxKind.GotoStatement` is registered; `goto case X;`/`goto default;` (`GotoCaseStatement`/`GotoDefaultStatement`, same `GotoStatementSyntax` node type) are never flagged while the formatter's `case GotoStatementSyntax` covers all three forms. Register all three kinds.
- [Major] Reihitsu.Analyzer\Rules\Layout\RH5019YieldStatementsShouldBePrecededByABlankLineAnalyzer.cs:33 — two divergences from `BlankLineStatementSpacingRewriter`'s `YieldStatementSyntax` rule: (1) only `YieldReturnStatement` is registered, `yield break;` is never flagged; (2) the previous-statement-is-yield exemption in `IsRelevant` only works for `BlockSyntax` parents — in a switch section a `yield return` directly after another yield is flagged although the formatter inserts no blank line there (permanent diagnostic). Handle `SwitchSectionSyntax` like RH5021/RH5029 do.
- [Major] Reihitsu.Analyzer\Rules\Layout\RH5026ChainedStatementBlocksMustNotBePrecededByBlankLineAnalyzer.cs:60 — `lineText.TrimStart().StartsWith("else"/"catch"/"finally")` matches identifiers: `elseBranchTaken = true;` or `finallyHandler();` preceded by a blank line is a false positive. The formatter keys on `ElseKeyword`/`CatchKeyword`/`FinallyKeyword` tokens (`BlankLineTokenCleanupRewriter`); use token-based detection here too.
- [Major] Rules\Layout RH5022/RH5023/RH5024/RH5025/RH5026 (systemic; e.g. Reihitsu.Analyzer\Rules\Layout\RH5022OpeningBraceMustNotBeFollowedByBlankLineAnalyzer.cs:48) — the raw text-line scans do not exclude multi-line comment interiors or preprocessor-disabled text (`#if false` blocks): blank-line runs, `{`-ending lines, and `}`-lines inside those regions are flagged, but the formatter's BlankLines phase is trivia-based and deliberately leaves comment/disabled-text content untouched — diagnostics that `reihitsu-format` can never resolve (same family as the Spacing batch-11 finding). The five rules also share a copy-pasted scan loop (`GetStringLineIndices` + per-line check) that belongs in one helper.
- [Minor] Reihitsu.Analyzer\Rules\Layout\RH5024ClosingBraceMustNotBePrecededByBlankLineAnalyzer.cs:60 — trimmed line must equal `"}"` exactly, so `});`, `};`, `})` preceded by a blank line are not flagged although the formatter removes blank lines before every `CloseBraceToken`. RH5025 likewise only matches a bare `"{"` and, unlike its siblings, omits the `lineIndex - 1` string-line skip — inconsistent guard set across the family.
- [Major] Reihitsu.Analyzer\Rules\Layout\RH5109ParametersMustBeOnSameLineOrSeparateLinesAnalyzer.cs:58 — the GroupBy check requires `lineGroups.Count > 1`, so `void M(int a, Func<int> b)` where only `b`'s type spans lines (both parameters start on one line, list is multi-line) is not flagged — but the formatter's `EnsureParametersOnSeparateLines` splits exactly that shape, and the sibling rule RH5102 flags the equivalent argument-list shape. False negative and analyzer/formatter divergence.
- [Major] Reihitsu.Analyzer\Rules\Layout\RH5111AssignmentsMustHaveProperLineBreaksAnalyzer.cs:113 — Rule 2 flags `Complex =` followed by `{ ... }` on the next line for object-initializer member values, but the formatter's `LineBreakAssignmentRewriter` explicitly exempts `InitializerExpressionSyntax` right-hand sides (only the `CollectionInitializerExpression` kind is collapsed onto the `=` line). The object-initializer-kind shape survives `reihitsu-format` and stays flagged forever. Mirror the formatter's kind distinction.
- [Major] Reihitsu.Analyzer\Rules\Layout\RH5206SwitchExpressionBracesShouldBeAnchoredAnalyzer.cs:67 — every arm must sit at `anchor + 4`, including arms that share a line (`1 => "a", 2 => "b"`), but no formatter phase puts switch-expression arms on separate lines (`LineBreakBlockRewriter` only normalizes the braces and first content; `SwitchExpressionContributor.SetIfFirstOnLine` skips non-first-on-line tokens). Arms sharing a line survive formatting and are permanently flagged. Either split arms in the formatter or restrict the check to first-on-line arm tokens.
- [Major] Reihitsu.Analyzer\Rules\Layout\RH5401ExpressionStyleGetOnlyPropertiesShouldBeSingleLinedAnalyzer.cs:62 — flags any expression-bodied get-only property whose full span is multi-line, but the formatter's `CollapseExpressionBodiedProperty` only moves the arrow and the first expression token onto the signature line. A property body that is a switch expression (which `LineBreakBlockRewriter`/RH5206 force multi-line), a wrapped fluent chain (allowed by RH5201), or a multi-line collection expression can never satisfy RH5401 — rule conflict and permanent diagnostic on formatter output. Check only that the expression *starts* on the signature line.
- [Major] Reihitsu.Analyzer\Rules\Layout\RH5408SimpleAutoPropertiesShouldBeSingleLinedAnalyzer.cs:124 — `IsEligibleSimpleAutoProperty` checks comments/directives only on the initializer, while the formatter's `CanCollapseAutoPropertyToSingleLine` first bails out on `HasCommentsOrDirectives(node.AccessorList)`. An auto-property with a comment between accessors is flagged but never collapsed by `reihitsu-format` — permanent diagnostic. Add the accessor-list guard.
- [Major] Reihitsu.Analyzer\Rules\Layout\RH5601UseTabsCorrectlyAnalyzer.cs:60 — the token exclusion list (`StringLiteralToken`, `CharacterLiteralToken`, `InterpolatedStringTextToken`) misses raw string tokens (`SingleLine`/`MultiLineRawStringLiteralToken`, the `Utf8*` literal kinds) and `Utf8StringLiteralToken`: a tab inside raw string content is semantic content, gets flagged, and no formatter phase (correctly) touches it. False positive on raw strings.
- [Minor] Reihitsu.Analyzer\Rules\Layout\RH5105OpeningParenthesisMustBeOnDeclarationLineAnalyzer.cs:44 (also RH5106/RH5108) — parenthesis/parameter placement is checked for methods and constructors only, while the formatter normalizes every `ParameterListSyntax` (local functions, operators, delegates, record primary constructors); those shapes are silently reformatted without ever being reported. Additionally RH5106's class/file name (`...MustBeOnLineOfOpeningParenthesisAnalyzer`) contradicts both its resource title ("on line of last argument") and its implementation — rename.
- [Minor] Rules\Layout RH5106/RH5107/RH5402-RH5407 (e.g. Reihitsu.Analyzer\Rules\Layout\RH5106ClosingParenthesisMustBeOnLineOfOpeningParenthesisAnalyzer.cs:65) — registered as syntax-tree actions running full `DescendantNodes()` scans (RH5106 even twice, once per declaration kind) where node actions on the specific kinds would do; inconsistent with siblings like RH5105/RH5109 that use node actions, and wasteful on large files.
- [Minor] Rules\Layout RH5306/RH5307/RH5408 (e.g. Reihitsu.Analyzer\Rules\Layout\RH5306ListPatternsShouldBeFormattedCorrectlyAnalyzer.cs:45) — `CanSafelyFormat`/`CanSafelyCollapseToSingleLine`/`HasCommentsOrDirectives`/`IsSingleLineSpan` are verbatim copies of formatter helpers (`LineBreakListRewriter.CanSafelyCollapseBracketedArguments`, `PropertyLayoutLineBreakRewriter`); these shared safety guards belong in Reihitsu.Core next to the batch-7 `FluentChainAnalysisHelper` finding — the copies are exactly where analyzer/formatter policy drifts apart (see RH5408 Major above). (Batch 17: the code-fix side adds further copies of the same guards — Reihitsu.Analyzer.CodeFixes\Rules\Layout\RH5307...CodeFixProvider.cs:53 `CanSafelyCollapseToSingleLine`, RH5408...CodeFixProvider.cs:44-135 `HasCommentsOrDirectives`/`IsSingleLineSpan`/`CanApplyCodeFix`; additionally `RH5604CodeMustNotContainMixedLineEndingsAnalyzer.GetPredominantLineEnding` is an algorithm-identical copy of `ReihitsuFormatterHelpers.DetectEndOfLine`, which the RH5604 code fix itself reuses.)
- [Minor] Reihitsu.Analyzer\Rules\Layout\RH5202RawStringLiteralsShouldBeFormattedCorrectlyAnalyzer.cs:72 + Reihitsu.Formatter\Pipeline\RawStringAlignment — both check only `MultiLineRawStringLiteralToken`/`InterpolatedMultiLineRawStringStartToken`; `u8` raw string literals (`Utf8MultiLineRawStringLiteralToken`) are skipped by analyzer and formatter alike. If deliberate, document it; otherwise add the Utf8 kinds in both places.
- [Minor] Reihitsu.Analyzer\Rules\Layout\RH5603FileMustNotEndWithANewlineAnalyzer.cs:53 — `sourceText.ToString()` materializes the entire file to inspect the last few characters; index `sourceText` directly from the end (same pattern as the RH8402 finding).
- [Minor] Rules\Layout RH5304/RH5305/RH5306 (e.g. Reihitsu.Analyzer\Rules\Layout\RH5304NestedCollectionInitializerAssignmentsShouldBeFormattedCorrectlyAnalyzer.cs:123) — the "single-line form" branch ("Rule 1"/"Rule 3") is dead code: when the open and close delimiters are on one line, every element necessarily lies between them on that same line, so the inner `Any(position => position.Line != openLine)` can never fire.
- [Minor] Rules\Layout RH5301/RH5303/RH5304 (e.g. Reihitsu.Analyzer\Rules\Layout\RH5301ObjectInitializerShouldBeFormattedCorrectlyAnalyzer.cs:97) — diagnostics use the whole creation-expression location, and the per-element loops report one diagnostic per offending element all at that identical span (duplicate same-span diagnostics, poor squiggle precision). Report at the offending brace/element instead.
- [Minor] Reihitsu.Analyzer\Rules\Layout\RH5405BracesMustNotBeOmittedAnalyzer.cs:50 — the `Where(... is BlockSyntax == false)` filter and the identical `if` inside the loop double-check the same condition (dead branch). Coverage gap: only `if` children are checked while the formatter's `ControlFlowBraceTransform` braces both `if` and `else` children — an unbraced `else` child is only caught indirectly via RH5407 when the `if` side is braced. RH5406 is additionally a strict subset of RH5405 (every unbraced multi-line child is already an RH5405 hit) — with both enabled at default severity every multi-line case is double-reported.
- [Minor] Reihitsu.Analyzer\Rules\Layout\RH5402BracesForMultiLineStatementsMustNotShareLineAnalyzer.cs:49 (and RH5403) — inconsistent parent-statement sets: RH5402 covers if/else/while/for/foreach/using/lock/fixed/do, RH5403 only if/else/while/for/foreach; both omit `ForEachVariableStatementSyntax` and checked/unchecked/try blocks. Align the sets (or share one list constant).
- [Minor] Reihitsu.Analyzer\Rules\Layout\RH5410FinalArrayInitializerItemsMustNotHaveTrailingCommasAnalyzer.cs:75 (and RH5411) — analyzer bodies are character-identical except for the registered `SyntaxKind` (and RH5409 is the same pattern for enums); fold into a parameterized base like the other Layout families instead of copy-paste.

## Reihitsu.Analyzer.CodeFixes

### Infrastructure, Clarity, Design, Documentation (batch 15)

- [Critical] Reihitsu.Analyzer.CodeFixes\Base\StatementBracesCodeFixProviderBase.cs:105 — `replacementSpan` starts at `statementLine.Start`, i.e. the start of the line the *child statement* is on. When the unbraced child shares the line with its parent (`if (x) Foo();` — a shape RH5405 flags, its tests only cover the next-line shape), the replacement deletes the `if (x) ` header text and emits only `{ Foo(); }` — the condition is silently removed (for RH5407, an `else Foo();` loses the `else` keyword). Start the replacement at `statement.SpanStart` when the parent starts on the same line, or move the statement to its own line first. Affects RH5405/RH5406/RH5407.
- [Major] Reihitsu.Analyzer.CodeFixes\Base\CasingCodeFixProviderBase{T}.cs:130 — the syntactic fallback (`ReplaceIdentifier`) is used for `TupleElementSyntax` (and whenever symbol resolution fails) and renames only the declaration; references such as `tuple.OldName` keep the old element name and no longer compile. Repo contract is comprehensive fix or no fix — don't register the fix when only the declaration can be renamed. (Batch 18: the `TupleElementSyntax` half is unreachable in practice because the RH4116/RH4118 providers never register — see batch-18 Minor; the symbol-resolution-failure half stands for all other casing fixes, e.g. on incomplete code where `GetDeclaredSymbol` returns null.)
- [Major] Reihitsu.Analyzer.CodeFixes\Base\TargetAttributePlacementCodeFixProviderBase.cs:100 — `tokenAfter.WithLeadingTrivia(SyntaxFactory.TriviaList())` wipes the entire leading trivia of the token following the attribute list. A standalone comment line between the attribute list and the declaration (`[Attr]` / `// note` / `public void M()`) is silently deleted; the `HasCommentsOrDirectives(attributeList)` guard does not cover the next token's leading trivia. Extend the guard to `tokenAfter.LeadingTrivia`.
- [Major] Reihitsu.Analyzer.CodeFixes\Rules\Design\RH2004AccessModifierMustBeDeclaredCodeFixProvider.cs:47 + Reihitsu.Core\DeclarationModifierUtilities.cs:71 — when no accessibility modifier exists (RH2004's entire scenario), `AddAccessibilityModifier` inserts a bare `Token(kind)` at index 0 while the member's first token keeps its leading trivia. For a documented member (`/// doc` then `void M()` or `static void M()`), the doc comment ends up *between* the inserted `private`/`internal` and the rest of the declaration — CS1587, the XML doc is detached from the symbol, layout mangled. Since RH8020/RH8021 mandate doc comments on methods, this is the common shape. Move the first token's leading trivia onto the inserted modifier.
- [Major] Reihitsu.Analyzer.CodeFixes\Rules\Design\RH2001PrivateAutoPropertiesShouldNotBeUsedCodeFixProvider.cs:56 — after `RenameSymbolAsync`, the property is re-located by adjusting only the span *length* for the identifier delta. Any renamed reference at an earlier offset in the same file (e.g. usage in a method declared above the property) shifts the declaration's start, so `FindNode(location)` misses the property and the fix silently degrades to rename-only: the user gets a *property* named `_camelCase` and no field. Re-locate via a `SyntaxAnnotation` on the declaration instead of span arithmetic.
- [Major] Reihitsu.Analyzer.CodeFixes\Rules\Clarity\RH3001NotOperatorShouldNotBeUsedCodeFixProvider.cs:35 — `!x` → `(x == false)` changes semantics for non-plain-bool operands: for `bool? x`, `!x` is a lifted `bool?` (null → null) while `(x == false)` is `bool` (null → false); for operands with a user-defined `operator !` the rewrite is not equivalent (and `== false` may not even compile). The analyzer flags every `LogicalNotExpression` without a type check. Restrict fix (or analyzer) to operands of type `bool`. Additionally the replacement node never takes `WithTriviaFrom(node)`, so leading trivia of the `!` token (indentation/comments) is dropped.
- [Major] Reihitsu.Analyzer.CodeFixes\Rules\Clarity\RH3004UseLambdaSyntaxCodeFixProvider.cs:47 — the single-return shortcut uses only `returnStatement.Expression.ToString()`; comments inside the block around the `return` (`delegate (int x) { /* note */ return x; }`) are silently deleted. Also no fix is registered for `delegate { ... }` without a parameter list although the analyzer flags it — acceptable under the comprehensive-or-nothing contract only if intentional; the analyzer/fix asymmetry should be documented or the diagnostic suppressed for that shape.
- [Major] Reihitsu.Analyzer.CodeFixes\Rules\Clarity\RH3104DoNotUseDefaultValueTypeConstructorCodeFixProvider.cs:44 + Reihitsu.Analyzer\Rules\Clarity\RH3104DoNotUseDefaultValueTypeConstructorAnalyzer.cs:49 — C# 10 structs may declare an explicit parameterless constructor; for such a type `new S()` runs the constructor while `default(S)` does not. The analyzer only checks `IsValueType`/no-args/no-initializer, so the fix silently changes behavior. Skip types whose parameterless instance constructor is user-defined (`!IsImplicitlyDeclared`).
- [Major] Reihitsu.Analyzer.CodeFixes\Rules\Documentation\RH8204DoNotUsePlaceholderElementsCodeFixProvider.cs:36 — `XElement.Parse(placeholderText)` throws `XmlException` for content that is not well-formed XML (unescaped `&` or `<` in the placeholder text, undefined entities) — an unhandled exception inside the code action. Catch the parse failure and return the document unchanged, or strip the tags textually.
- [Minor] Reihitsu.Analyzer.CodeFixes\Base\ModifierOrderingCodeFixProviderBase.cs:80 — `NormalizeModifierTrivia` rebuilds every modifier token with `default`/single-space trivia; a comment between modifiers (`public /* note */ static`) is deleted by the RH7105/RH7106 fixes. Bail out (or keep trivia) when any non-first modifier carries comment trivia.
- [Minor] Reihitsu.Analyzer.CodeFixes\Base\CasingCodeFixProviderBase{T}.cs:116 — the `TupleElementSyntax` arm of the `GetDeclaredSymbol` switch is unreachable: the enclosing `if (node is not TupleElementSyntax)` excludes it. Dead code.
- [Minor] systemic (batch 15) — fixes that insert line breaks use `Environment.NewLine` instead of the document's detected EOL (`ReihitsuFormatterHelpers.DetectEndOfLine` exists and is used by `UsingDirectiveCodeFixUtilities`): Base\StatementBracesCodeFixProviderBase.cs:104, Base\StatementShouldBePrecededByABlankLineCodeFixProviderBase.cs:64, Base\TargetAttributeListShapeCodeFixProviderBase.cs:131, Base\TargetAttributePlacementCodeFixProviderBase.cs:84, Rules\Documentation\RH8201InheritdocShouldBeUsedCodeFixProvider.cs:36, Rules\Documentation\RH8303ElementDocumentationHeaderMustBePrecededByBlankLineCodeFixProvider.cs:36. On LF-only files the IDE fix introduces mixed line endings (which RH5604 then flags). (Batch 16 additions: Rules\Layout\RH5011BreakStatementsShouldBeFollowedByABlankLineCodeFixProvider.cs:37 (fallback only), RH5020SingleLineCommentsShouldBePrecededByABlankLineCodeFixProvider.cs:36, RH5102ArgumentsShouldBeOnSingleOrSeparateLinesCodeFixProvider.cs:88, RH5103CodeMustNotContainMultipleStatementsOnOneLineCodeFixProvider.cs:43, RH5104CommentsMustBeOnTheirOwnLineCodeFixProvider.cs:40, RH5109ParametersMustBeOnSameLineOrSeparateLinesCodeFixProvider.cs:55, RH5201MethodChainsShouldBeAlignedCodeFixProvider.cs:58 — while the sibling RH5030 fix already uses `ReihitsuFormatterHelpers.DetectEndOfLine` correctly. Batch 17 additions: Rules\Layout\RH5402BracesForMultiLineStatementsMustNotShareLineCodeFixProvider.cs:50, RH5403StatementMustNotBeOnSingleLineCodeFixProvider.cs:54, RH5404ElementMustNotBeOnSingleLineCodeFixProvider.cs:55. Batch 18 addition: Rules\Organization\RH7301RegionsShouldMatchCodeFixProvider.cs:51.)
- [Minor] systemic (batch 15) — `WithTriviaFrom`-style unwrapping drops comments located *inside* the removed construct: RH3002 (Reihitsu.Analyzer.CodeFixes\Rules\Clarity\RH3002StatementMustNotUseUnnecessaryParenthesesCodeFixProvider.cs:40; comments between the removed parentheses and the inner expression), RH3005 (comments adjacent to the swapped operator/operands are discarded via `WithoutTrivia`). Guard on interior comment trivia before rewriting.
- [Minor] Reihitsu.Analyzer.CodeFixes\Rules\Clarity\RH3102CodeMustNotContainEmptyStatementsCodeFixProvider.cs:40 — `SyntaxRemoveOptions.KeepNoTrivia` deletes comments attached to the empty statement (`; // why` loses the comment; use KeepExteriorTrivia or skip commented statements). When the `;` is a required child (`if (x) ;`, `label: ;`) `RemoveNode` returns null and the registered fix silently no-ops — don't offer the fix for those shapes.
- [Minor] Reihitsu.Analyzer.CodeFixes\Rules\Clarity\RH3201CommentsMustContainTextCodeFixProvider.cs:17 — only provider in the assembly without `[Shared]`; conversely `[Shared]` sits on the non-exported abstract bases (ModifierOrderingCodeFixProviderBase, TypeMemberOrderingCodeFixProviderBase, UsingDirectiveOrderingCodeFixProviderBase) where it has no effect because MEF attributes are not inherited and the bases are not exported. Align the attribute placement.
- [Minor] Reihitsu.Analyzer.CodeFixes\Rules\Documentation\RH8201InheritdocShouldBeUsedCodeFixProvider.cs:99 — only `context.Diagnostics.First()` is handled instead of iterating all diagnostics like every sibling provider; additionally `ReplaceDocumentation` replaces *every* `SingleLineDocumentationCommentTrivia` in the leading trivia, so a (degenerate) second doc comment yields two `<inheritdoc/>` lines.
- [Minor] Reihitsu.Analyzer.CodeFixes\Base\TargetAttributeListShapeCodeFixProviderBase.cs:193 — `mergedList` is initialized from an empty `mergedAttributes` list and unconditionally recomputed at line 204; dead first assignment.
- [Minor] Reihitsu.Analyzer\Rules\Clarity\RH3003UseStringEmptyForEmptyStringsAnalyzer.cs:47 — `ShouldSkip` misses `goto case "";` (`GotoStatementSyntax` requires a constant); the RH3003 fix then produces `goto case string.Empty;` — CS0150. Add goto-case to the skip list.
- [Minor] Reihitsu.Analyzer.CodeFixes\Rules\Documentation\RH8402FileMustStartWithConfiguredXmlStyleCopyrightHeaderCodeFixProvider.cs:127 — `GetLeadingHeaderSpan` treats *every* leading comment as part of the header to replace; a non-copyright file comment (`// TODO ...` before the first token) is silently deleted and replaced by the configured header. Restrict the replaced span to comments that look like a header (e.g. starting with the configured XML element) and insert before anything else.
- [Minor] systemic (batch 15) — isolated `await` without `ConfigureAwait(false)` against the project-wide convention: Rules\Clarity\RH3001NotOperatorShouldNotBeUsedCodeFixProvider.cs:38, Rules\Design\RH2001PrivateAutoPropertiesShouldNotBeUsedCodeFixProvider.cs:64, Rules\Documentation\RH8201InheritdocShouldBeUsedCodeFixProvider.cs:65. (Batch 18 addition: Rules\Organization\RH7301RegionsShouldMatchCodeFixProvider.cs:34.)
- [Minor] Reihitsu.Analyzer.CodeFixes\Rules\Design\RH2001PrivateAutoPropertiesShouldNotBeUsedCodeFixProvider.cs:94 — `GetFieldName` re-implements the `_camelCase` conversion that `Reihitsu.Core\CasingUtilities.ToUnderLineCamelCase` provides (with culture-sensitive `char.ToLower` instead of the invariant variant). Reuse Core.

### Rules: Layout fixes, part 1 (batch 16)

- [Major] Reihitsu.Analyzer.CodeFixes\Rules\Layout\RH5105OpeningParenthesisMustBeOnDeclarationLineCodeFixProvider.cs:43 + RH5106ClosingParenthesisMustBeOnLineOfOpeningParenthesisCodeFixProvider.cs:43 + RH5108ParameterListMustFollowDeclarationCodeFixProvider.cs:43 — the text replacement deletes everything in `[previousToken.Span.End, token.SpanStart)`, i.e. all trivia in the gap including comments: `void M\n// why\n(int x)` or a comment line before a flagged `)` is silently deleted. The three `ApplyCodeFixAsync` bodies are byte-identical copies — fix once in a shared base and skip (or reposition) comment trivia.
- [Major] Reihitsu.Analyzer.CodeFixes\Rules\Layout\RH5109ParametersMustBeOnSameLineOrSeparateLinesCodeFixProvider.cs:50 — the parameter list is rebuilt from `sourceText.ToString(parameter.Span)` only; every comment between parameters (separator trailing comments, standalone comment lines before a parameter) is silently deleted. The subsequent `FormatNodeInDocumentAsync` call cannot restore them. Rebuild from the original trivia or bail out when the list contains comment trivia.
- [Major] Reihitsu.Analyzer.CodeFixes\Rules\Layout\RH5103CodeMustNotContainMultipleStatementsOnOneLineCodeFixProvider.cs:44 — the replacement span `[previousStatement.Span.End, statement.Span.Start)` deletes comments between the joined statements (`Foo(); /* note */ Bar();` loses the comment). Additionally `GetPreviousStatement` returns null for top-level statements (parent is `GlobalStatementSyntax`, not a block) and for a flagged statement that is first in its block, so the registered fix silently no-ops for those shapes — don't offer the fix there.
- [Major] Reihitsu.Analyzer.CodeFixes\Rules\Layout\RH5204IndentationMustUseFourSpacesPerScopeLevelCodeFixProvider.cs:221 — the directive path re-implements the indentation policy (`ComputeBaseIndentLevel`/`IsIndentingAncestor`) instead of reusing the analyzer's `BuildExpectedIndentationMap` or the formatter's LayoutComputer, and has already diverged: the analyzer adds an extra level for statements inside `SwitchSectionSyntax`, the fix's ancestor set does not, so a directive inside a switch section is re-indented to the wrong column and the diagnostic persists — the fix never converges. Switch expressions and collection expressions are missing from the ancestor set as well. Third indentation policy in the repo; consolidate.
- [Minor] Reihitsu.Analyzer.CodeFixes\Rules\Layout\RH5202RawStringLiteralsShouldBeFormattedCorrectlyCodeFixProvider.cs:107 — `GetLeadingSpaceCount` counts only `' '`, so outdenting (negative delta) a tab-indented raw string removes nothing and the fix silently no-ops (early-out at line 53) while the diagnostic persists. The fix also re-implements raw-string realignment textually instead of delegating to the formatter's RawStringAlignment phase, and `GetQuoteOffset` is a verbatim copy of the analyzer's method — same analyzer/formatter-drift family as the batch-14 helper-copy finding.
- [Minor] Rules\Layout RH5022-RH5028 (systemic; e.g. Reihitsu.Analyzer.CodeFixes\Rules\Layout\RH5022OpeningBraceMustNotBeFollowedByBlankLineCodeFixProvider.cs:35) — seven providers whose bodies are byte-identical (`sourceText.Replace(diagnosticSpan, string.Empty)` plus boilerplate) except for the diagnostic ID and title, and RH5303/RH5304 are likewise identical apart from IDs; the 20 blank-line "preceded by" providers already show the intended pattern (`StatementShouldBePrecededByABlankLineCodeFixProviderBase` with ID/title constructor parameters). Fold the copy-paste families into parameterized bases before they diverge. (Batch 17 additions: RH5409/RH5410/RH5411 are byte-identical apart from IDs and titles, and RH5402/RH5403/RH5404 carry three identical private `GetIndentation` copies.)

### Rules: Layout fixes, part 2 (batch 17)

- [Critical] Reihitsu.Analyzer.CodeFixes\Rules\Layout\RH5601UseTabsCorrectlyCodeFixProvider.cs:35 — the fix replaces the flagged one-character span with four spaces without re-checking the token kind. Combined with the analyzer's missing raw-string exclusions (batch-14 Major on RH5601UseTabsCorrectlyAnalyzer.cs:60), invoking the fix on a tab inside a raw string literal (`SingleLine`/`MultiLineRawStringLiteralToken`, `Utf8*` kinds) rewrites string *content* — the runtime value silently changes (same fix-executes-the-false-positive family as the RH3204/StringInterpolationUtilities Critical, now in Cross-assembly findings). Add the missing token kinds to the analyzer exclusion list and guard the fix on the same kinds.
- [Major] Reihitsu.Analyzer.CodeFixes\Rules\Layout\RH5402BracesForMultiLineStatementsMustNotShareLineCodeFixProvider.cs:48 — the replacement span `[previousToken.Span.End, token.SpanStart)` wipes all trivia in the gap before the open brace, so a comment on the shared line (`if (x) /* note */ {`) is silently deleted. Same gap-deletion family as the batch-16 Major on RH5105/RH5106/RH5108; skip or reposition comment trivia in the gap.
- [Minor] Reihitsu.Analyzer.CodeFixes\Rules\Layout\RH5403StatementMustNotBeOnSingleLineCodeFixProvider.cs:54 + RH5404ElementMustNotBeOnSingleLineCodeFixProvider.cs:54 — layout is rebuilt by string concatenation (manual indentation arithmetic) instead of delegating to `ReihitsuFormatter` (repo convention); RH5403's output keeps the open brace on the parent-statement line and joins all block statements onto one inner line, so applying the fix immediately produces a new RH5402 diagnostic (and RH5103 for multi-statement blocks) that needs further fix rounds, while the formatter resolves the same shape in one pass.
- [Minor] Reihitsu.Analyzer.CodeFixes\Rules\Layout\RH5401ExpressionStyleGetOnlyPropertiesShouldBeSingleLinedCodeFixProvider.cs:62 — the fix registers unconditionally, with none of the safety guards its sibling fixes RH5306/RH5307/RH5408 apply. For the shapes from the batch-14 RH5401 Major (switch-expression bodies, wrapped fluent chains, multi-line collection expressions) `FormatNodeInDocumentAsync` returns the document unchanged, so the offered code action silently no-ops. Don't register the fix when the formatter cannot collapse the property.
- [Minor] Reihitsu.Analyzer.CodeFixes\Rules\Layout\RH5530AccessorAttributesMustFollowPlacementRulesCodeFixProvider.cs:43 + RH5531AccessorAttributeListsMustFollowShapeRulesCodeFixProvider.cs:46 — the `ResolvePlacementMode`/`ResolveListShapeMode` overrides (single-line-property special case) are verbatim copies of the same overrides in the RH5530/RH5531 analyzers; the placement policy for accessor attributes now lives in four places across two assemblies, and drift means analyzer and fix demand different shapes. Move the special case into Reihitsu.Core next to `AttributeTargetUtilities`.
- [Minor] Reihitsu.Analyzer\Rules\Layout\RH5404ElementMustNotBeOnSingleLineAnalyzer.cs:62 (late batch-14 addition, found while reviewing the fix) — the single-line check uses the whole declaration span, which includes attribute lists; `[Serializable]` on its own line above `class C { }` makes the declaration multi-line and the single-line body is never flagged. Compare the lines of the identifier and the brace tokens instead of the full declaration span.

### Rules: Naming, Organization, Spacing fixes (batch 18)

- [Major] Reihitsu.Analyzer.CodeFixes\Rules\Naming\RH4005InterfaceNameCasingCodeFixProvider.cs:40 — `OnTransformIdentifier` strips the first character whenever the name starts with `i`/`I` (case-insensitive) before PascalCasing and re-prefixing: `interface index` becomes `INdex`, `important` becomes `IMportant` — mangled identifiers written via Renamer. Strip only an actual `I` prefix (uppercase `I` followed by another uppercase letter); for names merely starting with the letter i, keep the word and prepend `I`.
- [Major] Reihitsu.Analyzer.CodeFixes\Rules\Organization\RH7004UsingDeclarationsShouldNotBeUsedCodeFixProvider.cs:56 — converting `using var x = ...;` wraps *all* following statements of the block into the new using statement's body. Local functions declared after the using declaration but called before it become invisible (CS0103), and labels moved into the nested block break `goto` from outside (CS0159) — the fix produces non-compiling code for shapes the analyzer flags. Bail out when the following statements contain local-function or labeled statements that are referenced from before the using declaration (or simply when any are present).
- [Major] Reihitsu.Analyzer.CodeFixes\Base\TypeMemberOrderingCodeFixProviderBase.cs:85 + Reihitsu.Core\OrderingDeclarationUtilities.cs:229 — `MoveMemberBefore` moves the member together with its full leading trivia; `#region`/`#endregion`/`#if`/`#endif` directives sitting in that trivia are dragged to the new position, scrambling region structure (a `#region Fields` header lands mid-type) or splitting conditional-compilation pairs. In this repo's own region-heavy style the flagged member is regularly the first member after a `#region` line, so the RH7102/RH7103/RH7109 fixes corrupt the directive layout in exactly the codebases the rules target. Detect directives in the moved member's (and insertion target's) leading trivia and bail out or re-anchor them.
- [Major] Reihitsu.Analyzer.CodeFixes\Rules\Organization\RH7103StaticElementsMustAppearBeforeInstanceElementsCodeFixProvider.cs:36 + RH7109ReadonlyElementsMustAppearBeforeNonReadonlyElementsCodeFixProvider.cs:37 — reordering field declarations changes field-initializer execution order: `static int _a = F(); static readonly int _b = _a * 2;` is flagged by RH7109, and moving `_b` before `_a` silently changes `_b`'s value (same for static fields hopped over by an RH7103 move). Field initializers run in declaration order — semantic preservation is violated. Skip the move (or warn) when both the moved member and any member it passes over carry initializers with potential side effects/dependencies. (RH7102 is safe: consts have no runtime initialization.)
- [Major] Reihitsu.Analyzer.CodeFixes\Rules\Organization\RH7101DoNotCombineFieldsCodeFixProvider.cs:66 — splitting `int a, b;` rebuilds every declarator after the first with `WithLeadingTrivia(ElasticMarker)` and the first with `WithTrailingTrivia(ElasticMarker)`: a comment after the separator (`int a, // first`) and standalone comment lines before later declarators are silently deleted. Same comment-deletion family as the formatter's `FieldDeclarationSplitTransform` Major (batch 3) — and the fix re-implements that transform although its own final `FormatNodeInDocumentAsync` call would perform the split anyway; delegating outright would remove the duplicate (equally defective) logic.
- [Major] Reihitsu.Analyzer.CodeFixes\Rules\Organization\RH7302RegionsShouldStartWithAUpperCaseLetterCodeFixProvider.cs:161 — analyzer/fix predicate mismatch: the analyzer checks exactly the character at index 1 after `#region`'s space (`IsUpper(text[1])`), the fix capitalizes the first *non-whitespace* letter. For `#region  Fields` (double space; flagged because `text[1]` is a space) the fix finds `F` already uppercase and no-ops; for `#region 1abc` (digit) `ToUpperInvariant` changes nothing. Both shapes keep the diagnostic forever while the code action silently does nothing. Align the fix with the analyzer's check (normalize the spacing) or restrict the analyzer to letters.
- [Major] Reihitsu.Analyzer.CodeFixes\Rules\Organization\RH7501BreakStatementsShouldNotBeInsideExplicitSwitchCaseBlocksCodeFixProvider.cs:76 — the analyzer flags *every* `break` directly inside a block under a switch section, but `TryGetFixableNodes` only accepts the shape where the break is the last statement of the block *and* the block is the last statement of the section; for `case 1: { break; } Foo();` or `{ if (x) break; ... }`-style shapes no fix is registered. Repo contract is comprehensive fix or no fix — either extend the fix to the remaining shapes or document/narrow the analyzer.
- [Critical] Reihitsu.Analyzer.CodeFixes\Rules\Spacing\RH6018NegativeSignsMustBeSpacedCorrectlyCodeFixProvider.cs:35 + RH6019PositiveSignsMustBeSpacedCorrectlyCodeFixProvider.cs:35 — the analyzers flag the whitespace after a unary sign including the nested-unary shape `- -x`/`+ +x`, and the fix deletes that whitespace, producing `--x`/`++x` — re-lexed as pre-decrement/increment, a silent semantics change that still compiles (same token-gluing defect as the formatter's `NoSpaceSpacingRule` Major in batch 5/6, here executed by an IDE fix). Keep one space (or skip) when the operand expression itself starts with the same sign character.
- [Major] Reihitsu.Analyzer.CodeFixes\Rules\Spacing\RH6016MemberAccessSymbolsMustBeSpacedCorrectlyCodeFixProvider.cs:46 + RH6021ColonsMustBeSpacedCorrectlyCodeFixProvider.cs:46 — the replacement span `[previousToken.Span.End, nextToken.SpanStart)` wipes everything between the neighbor tokens: comments in the gap (`foo ./*c*/Bar`, `class C /*c*/: Base`) are deleted, and when the flagged token sits on a wrapped line (`foo // note` + `. Bar()` — flagged for the trailing space after the dot; tab-indented `: Base` lines — the RH6021 false positive from batch 11) the fix also joins the lines and deletes the previous line's end-of-line comment. Same gap-deletion family as the batch-16 Major on RH5105/RH5106/RH5108; restrict the replacement to whitespace-only gaps.
- [Major] Reihitsu.Analyzer.CodeFixes\Rules\Spacing\RH6008ClosingSquareBracketsMustBeSpacedCorrectlyCodeFixProvider.cs:35 + RH6017IncrementAndDecrementSymbolsMustBeSpacedCorrectlyCodeFixProvider.cs:35 — the RH6008/RH6017 analyzers report the whole inter-token gap as the diagnostic span when it contains *any* space/tab, and the fix deletes the entire span: `a[i /*c*/ ]` and `i /*c*/ ++` lose the comment. The sibling rules (RH6003/RH6006/RH6012/...) report whitespace-run-only spans and are safe — align RH6008/RH6017 on whitespace-only spans (analyzer) or skip gaps containing non-whitespace (fix).
- [Minor] Reihitsu.Analyzer.CodeFixes\Rules\Naming\RH4116TupleElementCasingCodeFixProvider.cs:44 + RH4118TupleElementCasingCodeFixProvider.cs:44 — `CanRegisterCodeFix` returns `false` unconditionally, so both exported providers never register a code action: dead code, and the `TupleElementSyntax` special-casing in `CasingCodeFixProviderBase` (tuple guard, `GetDeclaredSymbol` arm, `ReplaceIdentifier` overrides) is unreachable. Either implement a comprehensive tuple rename or delete the providers (the repo contract permits rules without fixes).
- [Minor] Reihitsu.Analyzer.CodeFixes\Rules\Naming\RH4001TypeNameShouldMatchFileNameCodeFixProvider.cs:56 — the document rename never checks whether the project already contains a document with the target file name/path; fixing `class Foo` inside `Bar.cs` while `Foo.cs` exists yields two documents with the same file path (overwrite on save). Skip or disambiguate when the target name is taken.
- [Minor] Reihitsu.Analyzer.CodeFixes\Rules\Organization\RH7301RegionsShouldMatchCodeFixProvider.cs:46 — the fix re-parses the matched `#region` line with the same magic offset (`Substring(8)`) the analyzer was flagged for in batch 12 instead of using `RegionDirectiveUtilities.GetRegionDescription`; a region written `#region  Fields` (double space) round-trips the extra space into the rebuilt `#endregion` comment. Reuse the Core helper in both places.
- [Minor] Reihitsu.Analyzer.CodeFixes\Rules\Organization\RH7306RegionDescriptionsShouldNotEndWithImplementationCodeFixProvider.cs:45 — only the flagged directive line is rewritten; the matching `#endregion // ... implementation` comment keeps the forbidden suffix, so applying the fix immediately produces an RH7301 mismatch diagnostic. Update the paired directive in the same fix (cf. RH7302, which does exactly that).
- [Minor] Reihitsu.Analyzer.CodeFixes\Base\UsingDirectiveOrderingCodeFixProviderBase.cs:86 — registration checks only `TryGetUsingDirectiveScope` while the sibling RH7207 provider additionally guards on `UsingDirectiveOrderingSafety.CanSafelyReorder`; for unsafe directive sets (and for RH7204's policy-conflict shapes, where the formatter ordering equals the current order) the RH7201-RH7206 code actions are offered but silently no-op in `OrganizeScopeUsingsAsync`. Apply the same registration guard in the base.
- [Minor] Reihitsu.Analyzer.CodeFixes\Rules\Spacing (systemic; e.g. Reihitsu.Analyzer.CodeFixes\Rules\Spacing\RH6006OpeningParenthesisMustBeSpacedCorrectlyCodeFixProvider.cs:35) — fixes that delete a whitespace-run span separating an opening token from an inline comment execute the batch-11 analyzer false positives: RH6006/RH6007/RH6011/RH6013/RH6018-RH6020/RH6022 turn `( // note` into `(// note`, gluing the comment to the token (the formatter deliberately preserves that space and will not restore it). Skip the deletion when the span is followed by comment trivia. Additionally, all 22 providers are near-identical copy-paste bodies (insert-space-after / insert-space-before / delete-span around the diagnostic span); fold them into a parameterized base like the blank-line fix family (same systemic finding as batch 16 for RH5022-RH5028).

### CodeFixResources (closeout session)

- [Minor] Reihitsu.Analyzer.CodeFixes\CodeFixResources.cs:84 — `RH3007Title` is dead code: no RH3007 code-fix provider exists in the assembly (the RH3007 analyzer deliberately ships without a fix), so the property and its CodeFixResources.resx entry are unused.
- [Minor] Reihitsu.Analyzer.CodeFixes\CodeFixResources.cs:1097 — `GetString` forwards `ResourceManager.GetString(name)`, which returns null for a missing key, through a non-nullable `string` return; a missing or renamed resource key surfaces only later as a null code-action title (`ArgumentNullException` inside `CodeAction.Create`) instead of a clear failure naming the key. Throw with the key name when resolution fails. Same pattern in Reihitsu.Analyzer\AnalyzerResources.cs:2968.
