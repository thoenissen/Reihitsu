# 1.0.0 RC Code Review (Round 2) — Findings

Format: `- [Critical|Major|Minor] path:line — problem. Suggested direction for fix.`
Severity scale and criteria: see `criteria.md`. Files without findings are fine and are not listed. Coverage is tracked in `coverage.md`.

Sessions append their findings under a per-session subsection (see `session-prompts.md`). Before adding a finding, check whether it is an instance of an already-reported systemic issue — if so, extend that finding's affected-file list instead of duplicating it.

## Summary

*(Compiled during the closeout session: counts per severity and per assembly, plus the recommended fix order.)*

## Open cross-checks

*(Sessions add entries here when a suspicion in the current scope can only be verified in an area covered by a later session — e.g. "does the code fix inherit this formatter defect?". Each entry names the suspicion, the file/line it originates from, and the session expected to resolve it. The closeout session resolves or explicitly closes every entry.)*

- **Stale private copies of consolidated Core helpers** (from Session 1, consolidation-integrity sweep). The fix-wave consolidation left private re-implementations of Core logic alive outside Core; each must be either deleted in favor of the Core helper or justified: `Reihitsu.Formatter\Pipeline\LineBreaks\PropertyLayoutLineBreakRewriter.cs:119` (`HasCommentsOrDirectives`, identical to Core), `Reihitsu.Formatter\Pipeline\BlankLines\BlankLineRegionDirectiveRewriter.cs:58` and `Reihitsu.Formatter\Pipeline\Indentation\LayoutComputer.cs:265` (`IsRegionDirective`), `Reihitsu.Formatter\Pipeline\Indentation\Contributors\CommentIndentationContributor.cs:58` (`IsComment`), `Reihitsu.Analyzer\Rules\Layout\RH5204IndentationMustUseFourSpacesPerScopeLevelAnalyzer.cs:241/:295` (`IsComment`/`IsRegionDirective`), `Reihitsu.Analyzer.CodeFixes\Rules\Layout\RH5204...CodeFixProvider.cs:188` (`IsCommentTrivia`), `Reihitsu.Analyzer.CodeFixes\Rules\Layout\RH5101...:44`, `RH5102...:44`, `RH5111...:72` (`HasCommentsOrDirectives`). Note a semantic divergence: the RH5101/RH5102/RH5111 copies additionally treat doc-comment trivia as blocking while Core's `FormattingSafetyUtilities.HasCommentsOrDirectives`/`SyntaxNodeUtilities.HasCommentsOrDirectives` do not — sessions 2/3 (formatter) and 5/8 (Layout rules/fixes) must decide the canonical predicate before consolidating. → Sessions 2, 3, 5, 8. *Session 2 (resolved for its scope):* `PropertyLayoutLineBreakRewriter.cs:119` (`HasCommentsOrDirectives`) is byte-identical to Core and guards a pure layout collapse where doc comments are irrelevant to safety only because `CanCollapseAutoPropertyToSingleLine` separately refuses via `WouldJoinIntoComment` (which does include doc-comment kinds); delete it in favor of `SyntaxNodeUtilities.HasCommentsOrDirectives`. Also found in session 2 scope and added to this list: `PropertyLayoutLineBreakRewriter.cs:138` (`IsSingleLineSpan`, identical to `FormattingSafetyUtilities.IsSingleLineSpan`). Remaining formatter entries (BlankLines, LayoutComputer, CommentIndentationContributor) fall to session 3; the doc-comment-divergence decision remains with sessions 5/8. *Session 3 (resolved for its scope):* `BlankLineRegionDirectiveRewriter.cs:58` and `LayoutComputer.cs:265` (`IsRegionDirective`) are byte-equivalent to `RegionDirectiveBlankLineUtilities.IsRegionDirective`, and `CommentIndentationContributor.cs:58` (`IsComment`) is byte-equivalent to `SyntaxTriviaUtilities.IsCommentTrivia` (doc-comment kinds included in both) — all three can be deleted in favor of the Core helpers with no behavior change; `ReihitsuFormatterHelpers.IsCommentTrivia` already delegates to Core. Only the RH5101/RH5102/RH5111 doc-comment divergence decision remains (sessions 5/8). *Session 5 (resolved for its scope):* the RH5204 analyzer copies (`IsComment`:241, `IsRegionDirective`:295) are byte-equivalent to `SyntaxTriviaUtilities.IsCommentTrivia` (doc-comment kinds included in both) and `RegionDirectiveBlankLineUtilities.IsRegionDirective` — delete in favor of Core, no behavior change. Analyzer-side data for the canonical-predicate decision: the Layout analyzers that guard collapses (RH5306:48, RH5307:58, RH5408:103/:126) all consume Core's `FormattingSafetyUtilities.HasCommentsOrDirectives` (doc comments NOT blocking), and the formatter pairs use the same Core predicate plus `WouldJoinIntoComment` (which does include doc-comment kinds) for join safety — so the fix copies' extra doc-comment blocking is stricter than both sides of the shipping policy; session 8 should delete the copies in favor of Core and rely on the join guards for doc-comment safety.
- **RH7105 `partial`-rank Critical propagation** (from `Reihitsu.Core\ModifierOrderingUtilities.cs:145`, Session 1). Confirm the analyzer really flags `public readonly partial struct` / `public unsafe partial class` / `private async partial Task M()` (no guard was found in the analyzer) and that `ModifierOrderingCodeFixProviderBase`-derived RH7105 fix emits the non-compiling `partial readonly`/`partial unsafe`/`partial async` order (CS0267); verify no derived fix adds its own guard. → Sessions 7 (analyzer), 9 (code fix). *Session 4 (fix side resolved):* `ModifierOrderingCodeFixProviderBase.ApplyCodeFixAsync` applies `GetUpdatedModifiers` verbatim (no `partial` guard anywhere in the base), and the derived RH7105/RH7106 fixes are pure pass-throughs to Core's `OrderModifiersForRh7105`/`OrderModifiersForRh7106` — the fix writes the non-compiling order whenever Core demands it, and no derived fix can add a guard because the base seals `RegisterCodeFixesAsync`. Only the analyzer confirmation (session 7) remains. *Session 6 (analyzer side resolved — RH7105 lives in Organization, session-6 scope):* the RH7105 analyzer is a pure pass-through to Core's `TryGetRh7105Violation` with no `partial` guard (the only `partial` handling in the whole chain is the Core rank at ModifierOrderingUtilities.cs:145), so the session-1 Critical is confirmed end-to-end. Closed; session 9 only needs regression tests once Core is fixed.
- **RH7204/RH7207 alias-ordering policy parity** (from `Reihitsu.Core\UsingDirectiveOrderingUtilities.cs:153`, Session 1). Exactly one policy now exists in Core (`ComputeCanonicalOrder`: using-type slot → target root-namespace group, System first → sort key, aliases keyed by alias name → original index) and RH7203/RH7204/RH7206/RH7207 plus the formatter (`UsingGrouping`) all consume it. Analyzer sessions must confirm the analyzers' violation predicates are exactly the canonical order's complement (especially RH7204's `AreInSameGroup` restriction) so accepted code is formatter-stable and fixes converge in one application. → Sessions 3 (formatter phase), 7 (analyzers), 9 (fixes). *Session 3 (formatter side resolved):* `UsingGrouping` (UsingGrouping.cs:29/:39) is pure delegation to Core's `AreInSameGroup`/`ComputeCanonicalOrder` and `UsingDirectiveOrderingRewriter` consumes only those two — the formatter carries no private ordering policy anymore (the reachable formatter defect is trivia relocation, reported as the `UsingLeadingTriviaBuilder` banner finding). Analyzer/fix confirmation remains with sessions 7/9. *Session 6 (analyzer side resolved — RH72xx live in Organization, session-6 scope):* all seven analyzers consume only the Core predicates and their violation conditions are exact complements of the canonical order within their facets (RH7204 compares same-group adjacent alias pairs by sort key; RH7207 requires positional identity with `ComputeCanonicalOrder`), so no private ordering policy exists on the analyzer side either. Two divergences found and reported as session-6 Majors: RH7207's `HasBlankLineBefore` conflates comment lines with blank lines (permanent diagnostic / silent reformat in both G.4 directions), and none of RH7201–RH7207 carries the formatter's `CanSafelyReorder` directive exemption (flagged conditional-using blocks are unfixable — the fix executes the refusing phase and no-ops, G.3). Fix-side confirmation remains with session 9.
- **Casing rules vs. verbatim (`@`) identifiers** (from `Reihitsu.Core\CasingUtilities.cs:267`, Session 1). `ConvertToPascalCase` strips a leading `@` (non-letter/digit), so `@class`/`@event` convert to the bare keyword (`class`) — a rename that cannot compile — and `Is*Case` returns false for any `@`-prefixed identifier. Verify whether `CasingAnalyzerBase`/`CasingCodeFixProviderBase` pass `token.ValueText` (then `class` stays `class`, likely harmless no-op) or `token.Text` (then false positives plus keyword-producing renames). → Sessions 4 (bases), 9 (fixes). *Session 4 (bases resolved):* both sides consume `Identifier.ValueText` (verified in the sampled RH4002/RH4115 analyzer `GetLocations` and fix `GetIdentifier` overrides), so `@`-prefixed identifiers reach `CasingUtilities` without the `@` and the keyword-stripping conversion path is unreachable from the rules. `CasingCodeFixProviderBase.TryGetFixedIdentifier` additionally rejects empty/unchanged/`SyntaxFacts.IsValidIdentifier`-failing results inside a try/catch (#264 guard covers the Renamer path too, because the validated identifier is the one passed to `RenameSymbolAsync`). Residual for session 9: confirm the remaining `GetIdentifier` overrides all use `ValueText`, and note that a conversion yielding a bare keyword (`@Class` local → `class`) passes `IsValidIdentifier` and relies on the Renamer's keyword escaping.
- **RegionCategoryUtilities vs. generic base type/interface region names** (from `Reihitsu.Core\RegionCategoryUtilities.cs:31`, Session 1). Classification compares the region description against `INamedTypeSymbol.Name` (no arity/type arguments). If RH7309/RH7401–RH7412 produce or accept region descriptions like `IEnumerable<T>` or `Comparer<T>`, those regions silently fall back to `RegionCategory.Custom` and category ordering/member-grouping misbehaves for generic bases. → Sessions 6/7. *Session 6 (resolved):* consistent by construction — the expected-name producers (`OverrideMemberUtilities.GetOriginalDeclaringTypeName`, `InterfaceImplementationUtilities.GetImplementedInterfaceName`) and the classifier all use `INamedTypeSymbol.Name` without arity, so the convention is plain-name regions (`IEnumerable`), which classify and match correctly on every consumer; arity-decorated labels are uniformly Custom/non-matching, i.e. rejected rather than half-recognized. Residual inherent ambiguity only when a type implements both `IComparable` and `IComparable<T>` (one shared region name; first match in `AllInterfaces` order decides the demanded name). Closed.
- **RH8309 unknown-element ordering stability** (from `Reihitsu.Core\XmlDocumentationElementOrderingUtilities.cs:20`, Session 1). All tags outside the canonical list (`inheritdoc`, `seealso`, `include`, …) share `UnknownElementRank == int.MaxValue`. Verify the RH8309 analyzer/fix sort is stable so unknown elements are never reordered relative to each other or hoisted, and that ranking `value` (canonical slot 4) does not fight RH8202 (`value` must not be used). → Sessions 6 (analyzer), 9 (fix). *Session 6 (resolved):* the analyzer skips unknown-rank elements entirely (they neither constrain nor trigger the order) and reports at most one diagnostic per comment; the fix's `OrderBy` is stable so unknown elements keep their relative order — but it sorts them to the *end* (rank `int.MaxValue` participates in the sort), demoting a leading `<inheritdoc/>`/`<seealso>` whenever the fix runs, which the analyzer never demanded — reported as a session-6 Major. `value` does not fight RH8202: RH8309 only orders an existing tag and RH8202's fix removes it; removal is stable under reordering, no ping-pong. Session 9: fix the demotion and add regression tests.
- **RH7110 and implicit accessibility** (from `Reihitsu.Core\Enumerations\OrderingAccessibilityGroup.cs`, Session 1). `None` (no explicit modifier) is the first enum value, so members without modifiers (interface members, implicitly-private members) sort *before* `public`. Verify the new RH7110 analyzer does not demand implicit-accessibility members be moved above explicit ones (interface members with mixed implicit/explicit `public` would oscillate or produce surprising ordering demands). → Session 7. *Session 6 (resolved — RH7110 lives in Organization, session-6 scope):* no oscillation possible — the analyzer skips `OrderingAccessibilityGroup.None` members entirely (RH7110...cs:62, "Members without an explicit accessibility modifier are ignored" per its rule doc), so `None`'s first-position enum value is never consumed as an ordering demand, and RH7110 ships without a code fix. Closed.
- **Guard completeness for unary-sign token joins** (from `Reihitsu.Core\UnaryOperatorSpacingUtilities.cs:23`, Session 1). The re-lex guard lives in Core; RH6018/RH6019 (analyzer + fix) and the formatter's `NoSpaceSpacingRule` reference it. Verify every space-collapse path in those consumers actually routes through `WouldGlueIntoDifferentOperator` (a single unguarded call site re-opens the `- -x` → `--x` corruption class). → Sessions 2 (formatter), 7 (analyzers), 9 (fixes). *Session 2 (partial):* `NoSpaceSpacingRule` lives in HorizontalSpacing (session 3 scope). Within session 2's scope no rewriter collapses the gap between two sign tokens directly, but the LineBreaks/StructuralTransforms rewriters do leave zero-gap token pairs behind (see the new spacing-reinstatement cross-check below) — the sign-sequence case `a -\n    -b` → `a\n- -b` ends with the binary `-` and unary `-` textually adjacent until HorizontalSpacing separates them, so session 3 must confirm `OperatorSpacingRule`/`NoSpaceSpacingRule` restore the space through the Core re-lex guard. *Session 3 (formatter side resolved):* the formatter's only zero-gap-demanding path (`NoSpaceSpacingRule.HasNoSpaceAfter`) routes through `WouldGlueIntoDifferentOperator` first, and the binary-`-`+unary-`-` adjacency is reinstated to one space by `OperatorSpacingRule`; however the guard itself covers only `-`/`+`, leaving the `& &x` address-of adjacency unguarded — reported as a session-3 Major. Analyzer (RH6018/RH6019) and fix call-site verification remains with sessions 7/9. *Session 7 (analyzer side resolved):* RH6018 (:53) and RH6019 (:53) both consult `WouldGlueIntoDifferentOperator` before flagging and their whitespace scans stop at line breaks, so no analyzer-reported span can produce a `-`/`+` glue; the survey of all other zero-gap-demanding spacing analyzers found exactly one guard hole — RH6020's address-of arm flags `& &x` with no guard at all, so its whitespace-run fix writes `&&x` (session-7 Major, same root cause as the session-3 formatter Major). Session 9: verify the RH6018/RH6019/RH6020 fixes delete exactly the reported whitespace-only spans (no wider gap collapse) and that widening the Core guard reaches all three pairs.
- **RH2004 vs. file-local types** (from `Reihitsu.Core\DeclarationModifierUtilities.cs:105`, Session 1). Because `IsAccessibilityModifier` omits `file`, RH2004 flags `file class C` and its fix would insert an accessibility modifier, producing CS9052 (file-local types cannot use accessibility modifiers). Verify analyzer behavior/tests and the fix output on `file` types. → Sessions 7 (analyzer), 9 (fix). *Session 7 (analyzer side confirmed):* RH2004 has no `file` exemption anywhere (`ShouldSkip` covers interface members, partial methods, static constructors and explicit implementations only) and delegates the accessibility test to the Core predicate that omits `FileKeyword`, so `file class C` is flagged; the RH2004 fix chooses `internal` for type declarations (RH2004...CodeFixProvider.cs:44), which on a file-local type emits `internal file class` (CS9052). Session 9: confirm the fix path and require the `file` exemption (fix point is the session-1 Core Major).
- **Spacing phase must reinstate every mandatory space the earlier phases elide** (from Session 2). LineBreaks and StructuralTransforms deliberately leave token pairs with *no* separating trivia and rely on `HorizontalSpacingPhase` (which runs later) to re-insert the space before the text is emitted: `ExpressionBodyToBlockConverter.cs:82` creates `return`+expression with an empty-trivia keyword; `BinaryOperatorLineBreakRewriter.cs:111` leaves operator and right operand adjacent (`&&`+`b`, `-`+`-b`); the `CollapseTokenToSameLine`/`RemoveLeadingEndOfLineAndWhitespace` family leaves `(`+argument, `]`+token, expression+`{` adjacencies. Session 3 must verify `KeywordSpacingRule`/`OperatorSpacingRule`/`SingleSpaceSpacingRule` cover every such pair (keyword+literal, operator+operand including sign sequences via the Core re-lex guard); any uncovered pair ships glued text. → Session 3. *Session 3 (resolved):* every enumerated pair is covered — `return`/`throw`+expression by `KeywordSpacingRule` (1), operator+operand (all `BinaryExpressionSyntax` operators, `=` in assignments/`EqualsValueClause`/`NameEquals`) by `OperatorSpacingRule` (1) including the `-`+`-b` sign sequence, `(`+argument / token+`,`/`;` / chain-dot joins by `NoSpaceSpacingRule` (0, the desired gap), attribute `]`+token by `AttributeListCloseBracketSpacingRule` (1), expression+`{` by `SingleSpaceSpacingRule` (1); `PropertyLayoutLineBreakRewriter` re-adds its accessor-list spaces itself (`BuildAccessorTriviaReplacementMap`), and `BinaryOperatorLineBreakRewriter`'s pattern path (`MoveTokenToNewLine`) preserves the operator's trailing space so `and`/`or` never end up glued. Two defects found during the resolution are reported as session-3 findings: the unguarded `& &x` adjacency and the double-space non-idempotency from rewriter-attached leading spaces.
- **Formatter/analyzer trailing-comma scope parity** (from `Reihitsu.Formatter\Pipeline\StructuralTransforms\InitializerTrailingCommaRemovalTransform.cs:44`, Session 2). The formatter removes trailing commas only from enum member lists and array/collection initializers; object initializers, anonymous-object initializers, collection expressions (`[1, 2,]`), switch-expression arms and `with` initializers keep theirs. If the RH trailing-comma analyzer family (served by `TrailingCommaRemovalCodeFixProviderBase`) flags any of those shapes, a fix that delegates to the formatter will not converge (G.3), and formatter output would keep raising the diagnostic (G.4). Verify the analyzer scope equals the transform scope. → Sessions 5/7 (analyzers), 8/9 (fixes). *Session 4 (fix-base side):* `TrailingCommaRemovalCodeFixProviderBase` has exactly three derived fixes — RH5409 (enum members), RH5410 (array initializers), RH5411 (collection initializers) — matching the formatter transform's scope, and the fix does not delegate to the formatter: `TrailingCommaCodeFixHelper` removes the comma textually (a comma-only line is removed whole, otherwise only the comma span), preserving comments. The analyzer-scope confirmation remains with sessions 5/7. *Session 5 (analyzer side resolved):* exact scope match — RH5409 registers `EnumDeclaration`, RH5410 `ArrayInitializerExpression`, RH5411 `CollectionInitializerExpression`, and no other rule flags trailing commas; object initializers, anonymous-object initializers, collection expressions, switch-expression arms and `with` initializers are neither flagged nor touched by the transform. Parity holds; nothing remains for session 7 on this item.
- **Delegating code fixes inherit the two session-2 formatter Criticals** (from `Reihitsu.Formatter\Pipeline\LineBreaks\AttributeTargetFormattingRewriter.cs:222` and `LineBreakTriviaUtilities.cs:197`, Session 2). Every code fix that calls `ReihitsuFormatter.FormatNodeInDocumentAsync`/`FormatNode` runs the full pipeline over its scope: a fix applied to a member whose parameter attribute is followed by a comment line hangs the IDE (ApplyPlacement loop), and a fix whose scope contains `#if`/`#pragma` in a join gap emits non-compiling code. Sessions 8/9 must enumerate the delegating fixes and check whether any guard (e.g. `HasCommentsOrDirectives` pre-checks in the fix bases) happens to shield them. → Sessions 8, 9. *Session 3 addition:* the inherited-defect set grows by the session-3 findings — a delegating fix whose scope contains a switch section with directives loses them (`SwitchCaseBraceRewriter`), one whose scope contains `x\n= 1`/`int x\n= 1` emits a double space (`HorizontalSpacingRewriter`), and one formatting a documented member cannot fix its indentation (`IndentationRewriter`). *Session 4 addition (fix-base delegating surface):* the bases that delegate are `StatementBracesCodeFixProviderBase:86` (full pipeline over the owning if statement), `OperatorSpacingCodeFixProviderBase:81` (over the smallest node containing operator and neighbours), `ModifierOrderingCodeFixProviderBase:132` and `TypeMemberOrderingCodeFixProviderBase:106` (over the member/type declaration), `EmptyTypeDeclarationShouldUseSemicolonCodeFixProviderBase:56` (over the type declaration) — none pre-checks `HasCommentsOrDirectives` over its formatting scope, so no base-level shield exists against the inherited formatter defects; and `UsingDirectiveOrderingCodeFixProviderBase` → `UsingDirectiveCodeFixUtilities.OrganizeScopeUsingsAsync:43` executes `UsingDirectiveOrderingPhase` directly and therefore also inherits the session-3 `UsingLeadingTriviaBuilder` banner-relocation Major (a file-header banner travels into the middle of the using block when the fix demotes the first directive).
- **Empty-type semicolon conversion comment loss also reaches the analyzer/fix pair** (from `Reihitsu.Formatter\Pipeline\StructuralTransforms\EmptyTypeDeclarationSemicolonTransform.cs:56`, Session 2). Core's `HasMeaningfulBodyTrivia` ignores the open brace's leading trivia, so `EmptyTypeDeclarationShouldUseSemicolonAnalyzerBase` (via `ShouldReport`) flags `class C\n// comment\n{\n}` and the code fix (via `CanConvertSafely` or the formatter transform) deletes the comment. Verify the analyzer base and the fix, and whether the Core check should be widened. → Sessions 4 (analyzer base), 9 (fix). *Session 4 (resolved):* confirmed on both sides. `EmptyTypeDeclarationShouldUseSemicolonAnalyzerBase.OnTypeDeclaration` reports via Core `ShouldReport` (no body-trivia check — the diagnostic itself is defensible), and `EmptyTypeDeclarationShouldUseSemicolonCodeFixProviderBase:103` guards only via `CanConvertSafely` → `HasMeaningfulBodyTrivia`, which inspects open-brace *trailing* and close-brace *leading* trivia but not the open brace's *leading* trivia — so for `class C\n// why this type is empty\n{\n}` the fix is registered and delegates to the formatter transform that deletes the comment (the session-2 Major at `EmptyTypeDeclarationSemicolonTransform.cs:56`; that finding's affected surface therefore includes the RH0815–RH0819 fix family). Derived fixes cannot compensate (the base seals `RegisterCodeFixesAsync`). One Core fix — widening `HasMeaningfulBodyTrivia` to the open brace's leading trivia — repairs the formatter transform and all five fixes at once. Nothing remains for session 9 beyond regression tests.
- **Blank-line analyzer parity with the session-3 formatter gaps** (from Session 3, BlankLines/LineEndings findings). Session 5 must check, for each pair, whether the Layout analyzer flags shapes the formatter provably leaves or produces (permanent diagnostics, G.4): (a) RH5023 vs. the `BlankLineCollapser` off-by-one — two blank lines survive after any directive or doc comment; (b) RH5031 vs. `EnsureBlankLineBeforeFirstDirective` — no blank enforced between a comment line and `#region`, and the switch-label-colon exemption exists only on the formatter side; (c) RH5032 vs. the two-blank-line fixed point after `#region`; (d) RH5011 vs. `BlankLineBreakSpacingRewriter` — no blank enforced after braced `{ ...; break; }` switch sections (the very shape `SwitchCaseBraceRewriter` produces); (e) RH5021 vs. `BlankLineStatementSpacingRewriter.NeedsBlankLineBefore` — the formatter exempts assignment expression statements after local declarations (`ExpressionStatementSyntax` arm) — confirm RH5021 has the same exemption; (f) RH5604 vs. `LineEndingNormalizationPhase` — line breaks inside multi-line comments and disabled text are not normalized; if the analyzer counts raw text, its diagnostic persists (raw strings must stay exempt on both sides); (g) RH5204 vs. the `IndentationRewriter` doc-comment bug — members preceded by doc comments are never re-indented, so RH5204 diagnostics on documented members are unfixable by the CLI (also relevant to the RH5204 code fix, session 8). → Session 5 (analyzers), Session 8 (RH5204 fix). *Session 5 (analyzer side resolved):* (a) confirmed — RH5023 flags the two-blank fixed point; fix point is the session-3 `BlankLineCollapser.cs:53` Major. (b) confirmed — RH5031 flags both the comment-gap and switch-label-colon shapes; fix point is the session-3 `BlankLineEditor.cs:178` Major. (c) no gap — RH5032 is satisfied by two blank lines; folds into (a). (d) no gap — RH5011 exempts break-before-`}` exactly where the formatter's section-level rule stays silent; the real defects in that area are the new RH5010/RH5030 session-5 Major (blocks under switch sections). (e) no gap — RH5021 carries the identical assignment exemption. (f) no gap — RH5604 counts `EndOfLineTrivia` via `descendIntoTrivia: true` exactly like `LineEndingNormalizationPhase`; comment/disabled interiors and raw strings are invisible on both sides. (g) confirmed — RH5204 flags documented members, the CLI cannot fix them until the session-3 `IndentationRewriter.cs:196` Major is fixed; the RH5204 fix shares `BuildExpectedIndentationMap` with the analyzer, so session 8 only needs to verify the fix's line edits converge.
- **Directive reachability for the comment-only fix-base guards** (from Session 4, `CommentSafeSpanReplacementCodeFixProviderBase.cs:106` Major). The session-4 finding shows the new fix bases guard comments but let preprocessor directives through; whether output actually corrupts depends on the analyzers flagging shapes whose edited gap/trivia contains a directive. Sessions 5/7 must check: (a) RH5105/RH5106/RH5108 (CollapseTokenGap consumers — line-placement rules whose flagged gap is by definition multi-line): do they flag `Foo(a\n#if X\n)` / `void M\n#if X\n(int x)`?; (b) the RH74xx placement analyzers vs. a directive between attribute list and member (`[Attr]\n#if X\npublic void M()`); (c) RH7105/RH7106 vs. directives between modifiers. Wherever the analyzer has no directive exemption, escalate the session-4 Major to Critical for that pair. → Sessions 5, 7 (escalation decision), 8/9 (fix-side confirmation). *Session 5 (resolved for its scope):* (a) confirmed and escalated — RH5105/RH5106/RH5108 flag directive-bearing gaps with no exemption (session-5 Critical); the attribute-placement half of (b) is the RH55xx family, not RH74xx: SingleLine-mode pairs (RH5523 parameters, RH5530 accessors) flag `#if X\n[Attr]\n#endif\ntarget` and are escalated (session-5 Critical), SeparateLine-mode pairs never flag the directive-separated shape and stay safe. Remaining for session 7: RH7105/RH7106 directives between modifiers (hole b of the session-4 finding). *Session 6 (resolved — RH7105/RH7106 live in Organization, session-6 scope):* confirmed and escalated — both analyzers are pure Core pass-throughs with no directive exemption, so directive-bearing modifier lists are flagged and the comment-only-guarded fix deletes the directives; recorded as a session-6 Critical (with a reachability caveat: the flagged shape needs a directive *inside* a misordered modifier list). Nothing on this item remains for session 7. *Session 7 (spacing family checked, no escalation):* no RH6xxx rule can feed a directive into a comment-only-guarded fix — every spacing analyzer's whitespace scan stops at `\r`/`\n` or exempts multi-line gaps outright (RH6002 different-line commas, RH6021 line-break neighbours, RH6023/RH6024 via `IsOperatorGapViolation`), so directive-bearing gaps are never flagged in the Spacing category.
- **BlankLineSpanRemoval span contract** (from Session 4, `BlankLineSpanRemovalCodeFixProviderBase.cs:59`). The base deletes the reported diagnostic span with no guard of its own; safety rests entirely on the seven derived analyzers (RH5022/RH5023/RH5024/RH5025/RH5026/RH5027/RH5028) reporting spans that contain only blank lines — a span that overlaps a comment or directive line would silently delete it. Session 5 must verify each analyzer's span computation is whitespace-only. → Session 5. *Session 5 (resolved):* all seven analyzers report whitespace-only spans — RH5022/RH5024/RH5025/RH5026/RH5027 report exactly one blank line (`TextSpan.FromBounds(blankLine.Start, blankLine.EndIncludingLineBreak)`), RH5023 the same per consecutive blank line, RH5028 the leading blank region up to the first non-blank line's start; the only non-code contexts a flagged blank line can occupy are verbatim/raw interpolation holes, where the deleted trivia does not alter the string value. The contract holds; the defensive assert suggested in the session-4 Minor is still worth adding.
- **FollowedBy/PrecededBy blank-line directive-exemption asymmetry** (from Session 4, `StatementShouldBeFollowedByABlankLineAnalyzerBase{TStatement}.cs:67`). `StatementShouldBePrecededByABlankLineAnalyzerBase` exempts statements directly preceded by a directive (`IsPrecededByDirective`), but the FollowedBy base has no such exemption: `break;\n#if DEBUG\n    Log();` is flagged by RH5011 and the fix would insert a blank line between the statement and the directive. Session 5 must check whether the formatter's `BlankLineBreakSpacingRewriter` enforces the same blank line before directives; if not, this is a permanent-diagnostic parity gap (G.4). → Session 5. *Session 5 (resolved):* no permanent diagnostic — `BlankLineBreakSpacingRewriter` does insert the blank line (via `EnsureBlankLineBeforeStatement`, at leading-trivia index 0, i.e. *above* the `#if`/`#endif`), and the analyzer's blank-line walker accepts a blank anywhere in the concatenated trivia, so one formatter run silences RH5011. The asymmetry itself (PrecededBy exempts directive-adjacent statements per #356, FollowedBy does not, formatter exempts nothing) is reported as a session-5 Major together with the odd insert position.
- **Operator declarations excluded from the documentation-rule surface** (from Session 4, `Reihitsu.Analyzer\Core\DocumentationAnalysisUtilities.cs:25`). `DocumentableDeclarationKinds`/`SummaryDocumentationKinds` omit `OperatorDeclaration` and `ConversionOperatorDeclaration`, so RH8030 (summary must have text) and the whole RH80xx family never inspect operator documentation, and no dedicated operator-documentation rule exists. Session 6 must confirm the omission is deliberate (or record it as a false-negative family). → Session 6. *Session 6 (resolved):* recorded as a false-negative-family Minor — no documentation rule covers operators (`DocumentableDeclarationKinds`, `ParameterOwnerKinds`, `ReturnValueOwnerKinds` all omit them) and primary-constructor parameters are equally exempt from RH8101/RH8102; the rule docs scope consistently to the covered kinds, so behavior and doc contract agree (deliberate-looking gap, not a bug). The doc-trivia-based rules (RH8301–RH8309) do cover operator doc comments since they key on the trivia, not the declaration kind. Closed.
- **Layout fix-side verifications from session 5** (Session 5). (a) RH5113's fix is a standalone `CodeFixProvider` (not a `CollapseTokenGapCodeFixProviderBase` consumer) — verify how it collapses the semicolon gap: a textual gap replacement would delete comments/directives exactly like the escalated CollapseTokenGap Critical; the formatter counterpart refuses comment gaps. (b) RH5104's fix must move a comment onto its own line for *every* flagged shape (trailing comments, comments between tokens, multi-line comments sharing both line ends) — the analyzer has no formatter backing (session-5 Major), so the fix is the only remediation. (c) RH5305 has no `HasCommentsOrDirectives` guard (unlike RH5306/RH5307) — check whether its fix reflows comment/directive-bearing collection expressions destructively. (d) RH5204's fix consumes the analyzer's `BuildExpectedIndentationMap` — verify it also skips string lines (`GetStringLineIndices`) like the analyzer does, and that per-line whitespace replacement converges in one application. (e) RH5601's fix on tabs inside multi-line comments and `#if false` disabled text (flagged per the session-5 Major) — check the round-1 raw-string guard now exists and whether comment/disabled interiors are edited. (f) Confirm the CollapseTokenGap consumer list is exactly RH5105/RH5106/RH5108 and the TargetAttributePlacement consumers include RH5523/RH5530 (the two escalated Criticals). → Sessions 8, 9.
- **RH7310 fix vs. comment-only regions** (from `Reihitsu.Analyzer\Rules\Organization\RH7310EmptyRegionsShouldBeRemovedAnalyzer.cs:48`, Session 6). `ContainsContent` counts only *tokens* inside the region span, so a region whose body is only comments (`#region Notes\n// rationale ...\n#endregion`) is classified empty and flagged (directives/disabled text are exempted, comments are not). Session 9 must verify the RH7310 code fix removes only the two directive lines and preserves the interior comment lines — a span-based removal would silently delete them (same class as the CommentSafeSpanReplacement family). → Session 9.
- **RH1003 fix vs. mixed additive chains** (from `Reihitsu.Analyzer\Rules\Performance\RH1003UseStringInterpolationInsteadOfStringConcatenationAnalyzer.cs:80`, Session 7). `EnumerateChainSegments` flattens *every* `AddExpression` and strips parentheses, including non-string arithmetic subexpressions: for `(1 + 2) + "x" + y` the segments are `1`, `2`, `"x"`, `y` although the runtime value concatenates `"3"`. The analyzer only counts segments (flagging the chain is correct), but if the RH1003 code fix builds the interpolated string from the same enumeration it emits `$"{1}{2}x{y}"` — silently changing `"3x..."` to `"12x..."`. Session 9 must verify the fix preserves non-string additive grouping (and parenthesized string subchains). → Session 9.
- **Session-7 analyzer false positives whose fixes emit non-compiling or behavior-changed code** (from Session 7). Each pair needs fix-side confirmation: (a) RH2001 on explicit-impl auto-properties — a field replacement breaks the interface implementation (CS0535); (b) RH3204 on `FormattableString`/`IFormattable` targets — the fix (like the formatter transform) removes the required `$` (CS0029/overload change; the session-7 Critical); (c) RH4103 (and any casing rule using the Renamer) on explicit interface implementations of metadata interfaces — the rename yields CS0539; (d) RH6020's whitespace-run fix writes `&&x` for `& &x`; (e) RH3003 on `""` nested in constant initializer expressions — `string.Empty` yields CS0133; (f) RH3103 on `nameof(Nullable<int>)` — `nameof(int?)` yields CS8081; (g) RH3104 on struct-constrained `new T()` — `default(T)` skips a C#10 parameterless struct constructor. The analyzer-side fixes are the primary remediation; session 9 records which fixes additionally need guards. → Session 9.
- **Organization member/region-moving fixes vs. the #294 safety contracts** (from Session 6, analyzer-side confirmation). The analyzers confirm the flagged shapes require member moves across sibling members: RH7102/RH7103/RH7109 (fixes via `TypeMemberOrderingCodeFixProviderBase`, whose `MoveRangeContainsDirectives` guard session 4 verified for directives) demand field/event-field reorderings that can cross initialized members — Core's `ChangesInitializerExecutionOrder` misses event-field initializers (session-1 Major at OrderingDeclarationUtilities.cs:315), so session 9 must check whether e.g. the RH7103 fix moves a static field past `public event EventHandler E = Handler;` and silently changes initializer execution order. RH7309's fix moves whole `#region` blocks — verify directive-span safety (`#if` opening inside one region and closing in another) and that members *between* regions stay put. → Session 9.

## Reihitsu.Core

### Session 1

- [Critical] Reihitsu.Core\ModifierOrderingUtilities.cs:145 — `GetRh7105Rank` ranks `partial` (7) before `readonly` (8), `volatile` (9), `async` (10), `extern` (11), `unsafe` (12) and `required` (13), but C# requires `partial` to be the last modifier, immediately before the type keyword or return type (CS0267). Valid, idiomatic declarations — `public readonly partial struct S`, `public unsafe partial class C`, `private async partial Task M()` — are therefore reported by `TryGetRh7105Violation` as violations, and `OrderModifiersForRh7105` demands the non-compiling order (`partial readonly`, `partial unsafe`, `partial async`), which the RH7105 code fix writes into the document. No test covers a `partial` combination with a higher-ranked modifier. Rank `partial` after all other modifiers (or pin its position during reordering). See open cross-check for the analyzer/fix confirmation.
- [Major] Reihitsu.Core\DeclarationModifierUtilities.cs:105 — `IsAccessibilityModifier` omits `SyntaxKind.FileKeyword` while the sibling helper `OrderingDeclarationUtilities.HasExplicitAccessibilityModifier` (OrderingDeclarationUtilities.cs:25) includes it: the same assembly now carries two contradictory definitions of "has accessibility". Consequence via RH2004/RH2005 (both call `HasAccessibilityModifier`): `file class C` is flagged although file-local types cannot legally take an accessibility modifier, and `AddAccessibilityModifier` would happily insert one (CS9052). Unify on one predicate and exempt `file`-modified declarations.
- [Major] Reihitsu.Core\CasingUtilities.cs:56 — type-parameter policy rejects digit-suffixed names: `IsTypeParameterName("T1")` is false because `IsPascalCase` demands an uppercase *letter* first, and `ToTypeParameterName("T1")` returns `"TT1"` because the prefix probe only accepts `char.IsUpper(input[1])` (a digit is not upper). `T1`/`T2` are the BCL-standard names for multiple type parameters (`Func<T1, T2>`), so RH4121 flags standard code and its fix renames `T1` → `TT1` (tests enshrine both: `CasingUtilitiesTests` rows `("T1", false)` and `("Tkey", "TTkey")`). Accept `T` + digits, and treat a digit after `T`/`t` as part of an existing prefix.
- [Major] Reihitsu.Core\OrderingDeclarationUtilities.cs:315 — `HasInitializer` misses `EventFieldDeclarationSyntax`: `public event EventHandler E = Handler;` carries a runtime initializer that executes in declaration order like a field initializer, but `ChangesInitializerExecutionOrder` returns false for it, so member-ordering fixes (RH7102/RH7103/RH7109/RH7110 family) may move an initialized event field past initialized fields — exactly the silent execution-order change this guard exists to prevent. Add the event-field case (variables with initializers, same static/instance context split).
- [Major] Reihitsu.Core (consolidation integrity) — the fix-wave consolidation recreated duplication *inside* Core: `FormattingSafetyUtilities.HasCommentsOrDirectives` (FormattingSafetyUtilities.cs:22) is byte-for-byte identical to `SyntaxNodeUtilities.HasCommentsOrDirectives` (SyntaxNodeUtilities.cs:21); `SyntaxNodeUtilities.IsComment` (SyntaxNodeUtilities.cs:41) duplicates `SyntaxTriviaUtilities.IsCommentTrivia` (SyntaxTriviaUtilities.cs:22); `RegionDirectiveUtilities` keeps a private `IsRegionDirective` (RegionDirectiveUtilities.cs:233) identical to the public `RegionDirectiveBlankLineUtilities.IsRegionDirective` (RegionDirectiveBlankLineUtilities.cs:21); and the single-line check exists three times (`SyntaxNodeUtilities.IsSingleLine`, `FormattingSafetyUtilities.IsSingleLineSpan`, `FormattingSafetyUtilities.AreAllSingleLine`). Callers can (and outside Core, do) pick different variants of the same policy. Collapse each family onto one helper.
- [Minor] Reihitsu.Core\SyntaxTriviaUtilities.cs:54 — `HasCommentDirectlyAbove` materializes the full document text via `token.SyntaxTree.GetText()` on every call and then applies string heuristics (`StartsWith("*")`, `EndsWith("*/")`) to decide comment adjacency instead of inspecting the leading-trivia structure; expensive in formatter/fix hot paths and the `*`-prefix heuristic is guesswork. Derive the answer from the trivia list.
- [Minor] Reihitsu.Core\UsingDirectiveOrderingUtilities.cs:131 — System-group detection is case-*insensitive* in `GetNamespaceGroupOrderKey` (`"SYSTEM"` maps into the System slot) but case-*sensitive* in `IsSystemNamespaceUsing` (UsingDirectiveOrderingUtilities.cs:46), so a root namespace differing only by case sorts into the System group while being classified `OtherNamespace` — RH7201 and the canonical order can disagree for such namespaces. Use one comparison (namespaces are case-sensitive; Ordinal on both is the safer choice).

## Reihitsu.Cli

### Session 1

- [Major] Reihitsu.Cli\Abstractions\DefaultFileSystem.cs:34 — the strict (throwing) decode added by #274 covers only UTF-8: BOM-detected UTF-16/UTF-32 files are decoded with the default replacement fallback (`Encoding.Unicode`, `BigEndianUnicode`, `UTF32Encoding`), so an invalid sequence (e.g. a lone surrogate inside a string literal) becomes U+FFFD silently, the file still parses, and a formatting write persists the corruption — the same silent-corruption class #274 fixed for UTF-8. Construct the UTF-16/32 encodings with throwing decoder fallbacks and route failures through the existing skip-with-warning path (the `DecoderFallbackException` handler in `FormatCommandHandler.ProcessFileAsync` already catches the base type).
- [Minor] Reihitsu.Cli\FormatCommandHandler.cs:200 — `IsInBuildOutputDirectory` matches only `/bin/`–`/obj/` with a *leading* separator on the raw enumerated path, and `CollectFiles` (FormatCommandHandler.cs:403) applies the filter before `GetFullPath`: `reihitsu-format bin` (relative) yields paths like `bin/x.cs` that escape the filter and get formatted, while the equivalent absolute invocation `/repo/bin` yields `/repo/bin/x.cs` and is skipped — same target, opposite outcome depending on path form. Also `OrdinalIgnoreCase` skips `/BIN/` on case-sensitive file systems. Normalize to full paths before filtering and decide the explicit-target semantics once.
- [Minor] Reihitsu.Cli\Diff\HunkBuilder.cs:30 — `Build`'s `originalLineCount`/`formattedLineCount` parameters are never used (dead parameters); the only caller (`DiffGenerator.Generate`) passes array lengths that the method ignores.
- [Minor] Reihitsu.Cli\Program.cs:19 — stale exit-code doc: the XML doc ("1 = formatting needed (--check)") and the `--check`-only usage text omit that `--dry-run` also exits 1 when changes are pending (`FormatCommandHandler.ExecuteAsync`, FormatCommandHandler.cs:187). Document the dry-run exit contract in both places.

## Reihitsu.Formatter

### Session 2 — infrastructure, StructuralTransforms, LineBreaks

- [Critical] Reihitsu.Formatter\Pipeline\LineBreaks\AttributeTargetFormattingRewriter.cs:222 — `TryApplyPlacementToList` reports "changed" unconditionally after calling `CollapseTokenToSameLine`, but the collapse refuses the join (returns the owner unchanged) when the token after the attribute list carries a leading comment (`WouldJoinIntoComment`). `ApplyPlacement` (line 173) loops while a change is reported and its body never observes `_cancellationToken`, so a single-line-mode attribute list followed by a comment line — e.g. `void M(\n    [NotNull]\n    // legacy callers pass null\n    string target)` — makes the formatter loop forever (CLI hang; every code fix delegating to `FormatNodeInDocumentAsync` hangs the IDE). The `HasCommentsOrDirectives(attributeList)` pre-check does not cover this because the comment lies outside the list node. Report a change only when the returned owner actually differs (or pre-check the join guard before entering the collapse branch).
- [Critical] Reihitsu.Formatter\Pipeline\LineBreaks\LineBreakTriviaUtilities.cs:197 — the central comment-join guard from #271 (`WouldJoinIntoComment`/`ContainsComment`:279) blocks only comment trivia; preprocessor directives and disabled text pass it. Every join that removes the previous token's trailing end-of-line while the moved token's leading trivia starts with a directive re-emits the directive mid-line (CS1040 — the output no longer compiles), re-opening the round-1 corruption class for the directive family. Reachable joins: `CollapseTokenToSameLine` call sites (LineBreakListRewriter.cs:65 — `Foo(\n#if DEBUG\n    a, ...` becomes `Foo(#if DEBUG ...`; DeclarationSemicolonLineBreakRewriter.cs:52; ChainLineBreakRewriter.cs:70/:86; PropertyLayoutLineBreakRewriter.cs:213–245; AttributeTargetFormattingRewriter.cs:225), direct joins guarded only by `WouldJoinIntoComment` (BinaryOperatorLineBreakRewriter.cs:100 — `a &&\n#if DEBUG\n    b`; TernaryLineBreakRewriter.cs:121/:166; LineBreakAssignmentRewriter.cs:84/:108/:139/:183/:214/:248/:284; LineBreakListRewriter.cs:81/:120/:160; DeclarationBraceLineBreakRewriter.cs:79), and `CollapseChainToSingleLine` (ChainLineBreakRewriter.cs:168; `SpineHasComment`:146 checks comments only). PropertyLayoutLineBreakRewriter.cs:101 additionally deletes such trivia outright (arrow/first-expression leading trivia replaced with a bare space), as do ControlFlowBraceTransform.cs:128 (`CollectComments` silently drops `#pragma`/`#if`/`#region` from statements it wraps in braces) and FieldDeclarationSplitTransform.cs:69 (`GetComments`). *Session 3 addition — same family in the SwitchCaseBraces phase:* `SwitchCaseBraceRewriter.AddBraces` rebuilds all four boundary positions from `CollectComments` (comments only — SwitchCaseBraceRewriter.cs:199 `BuildLeadingTrivia`, :222 `BuildTrailingTrivia`, :252 `BuildLabelTrailingTrivia`), so directives in the first statement's leading trivia, the last statement's trailing trivia, the label's trailing trivia or the trailing `break`'s trivia are deleted when braces are added (`case 1:\n#if DEBUG\n    Log();\n#endif\n    break;` loses both directives — the `DEBUG` conditionality is silently removed), and `RemoveBraces`' keep-guard `BraceTokensCarryComments` (:279) checks comment trivia only, so directives attached to the block's braces are deleted on brace removal. The comment side of #284 is otherwise complete at all four round-1 positions. The two newest call sites got the policy right — SwitchCaseWhenLineBreakRewriter.cs:67 (`GapIsCollapsible` refuses anything but whitespace/EOL) and LineBreakListRewriter.cs:203 (`CanSafelyCollapseBracketedArguments` checks `IsDirective`) — hoist that policy into the central guard and route the trivia-dropping transforms through directive-aware preservation.
- [Major] Reihitsu.Formatter\Pipeline\StructuralTransforms\ExpressionBodyToBlockConverter.cs:27 — all seven expression-bodied transforms pass only `ArrowToken.LeadingTrivia` into the created block and drop the arrow token's *trailing* trivia; a comment after the arrow (`int M() => // why\n    Compute();`) is silently deleted. The semicolon's leading trivia is discarded the same way (`WithSemicolonToken(default)` keeps only its trailing trivia via the close brace). Affected: ExpressionBodiedMethodTransform.cs:58, ExpressionBodiedConstructorTransform.cs:55, ExpressionBodiedOperatorTransform.cs:53, ExpressionBodiedIndexerTransform.cs:50, ExpressionBodiedConversionTransform.cs:53, ExpressionBodiedFinalizerTransform.cs:52, ExpressionBodiedLocalFunctionTransform.cs:57. Carry the arrow's non-whitespace trailing trivia (and the semicolon's non-whitespace leading trivia) into the block. (The #263 scope itself is complete: throw-statement conversion is centralized in the converter for all member kinds, and Task/ValueTask detection covers identifier-, qualified- and alias-qualified names while excluding generic `Task<T>`/`ValueTask<T>`.)
- [Major] Reihitsu.Formatter\Pipeline\LineBreaks\LineBreakDetection.cs:47 — `IsMultiLine` measures `FullSpan`, so a node whose *leading trivia* contains a line break (a blank line or a comment line directly above it) counts as multi-line even when the node's own text is single-line. Consequences: TernaryLineBreakRewriter.cs:56 explodes a single-line ternary onto three lines whenever a blank or comment line precedes the condition (e.g. `var x =\n\n    cond ? a : b;`); LineBreakListRewriter.cs:359 (`HasElementSplitSignal`, consumed at :309) forces one-element-per-line for a single-line list whose first element carries multi-line leading trivia; ChainWalker.cs:290 misclassifies argument lists (conservative refusal only). Measure `Span` (or compare first/last token lines) instead.
- [Major] Reihitsu.Formatter\Pipeline\LineBreaks\LineBreakContainedBlockRewriter.cs:202 — `MoveTrailingConditionCommentToOwnLine` prepends the moved comment to the *if keyword's* leading trivia; for an else-if (`else if (x) // note`) that leading trivia sits between `else` and `if`, so the output is `else // note\nif (x)` — the `else if` is split across two lines and the comment still is not on its own line. Detect the `ElseClause` parent and attach the comment above the `else` keyword (or leave it in place).
- [Major] Reihitsu.Formatter\Pipeline\LineBreaks\LineBreakInitializerRewriter.cs:179 — `VisitInitializerExpression` early-outs only for `ArrayInitializerExpression`; `ComplexElementInitializerExpression` (dictionary-style pairs `{ "a", 1 }` inside a collection initializer) falls through, so each pair's key and value are forced onto separate lines (:186) and the pair's braces are moved onto their own lines (:201) even when the whole initializer was single-line. No test covers the shape. Exclude complex element initializers (or keep pairs single-line).
- [Major] Reihitsu.Formatter\Pipeline\StructuralTransforms\EmptyTypeDeclarationSemicolonTransform.cs:56 — the safety check for the brace-to-semicolon conversion (`CanRewrite` → Core `EmptyTypeDeclarationSemicolonAnalysisUtilities.HasMeaningfulBodyTrivia`, which inspects only open-brace trailing and close-brace leading trivia) never looks at the open brace's *leading* trivia, and `ConvertToSemicolonDeclaration`:99 discards it: `class C\n// why this type is empty\n{\n}` converts to `class C;` and deletes the comment. Include the open brace's leading trivia in the meaningful-trivia check (in Core, so the analyzer/fix consumers inherit it — see open cross-check).
- [Minor] Reihitsu.Formatter\Pipeline\StructuralTransforms\EmptyTypeDeclarationSemicolonTransform.cs:44 — incomplete round-1 consolidation: the transform now reuses Core's `HasMeaningfulBodyTrivia` but still keeps a private `CanRewrite`/`SupportsLanguageVersion` instead of calling `EmptyTypeDeclarationSemicolonAnalysisUtilities.CanConvertSafely`, and the language-version guard retains the opposite polarity round 1 flagged: for non-`CSharpParseOptions` trees the transform proceeds (:46) while Core's `ShouldReport` refuses (Core :45). Delete the local copy in favor of `CanConvertSafely`.
- [Minor] Reihitsu.Formatter\Pipeline\LineBreaks\BracePlacer.cs:129 — `EnsureCloseBraceContinuation` is dead at every call site: the close token is always the last token of the detached node being rewritten, so `GetNextToken()`:138 returns `default` (or, when the node is still attached and unchanged, the next token lies outside the node and `ReplaceToken` silently no-ops), and the remaining in-node successors (`;`, `,`, `)`) are all in the skip list. Call sites: LineBreakBlockRewriter.cs:162/:187/:206/:225, DeclarationBraceLineBreakRewriter.cs:113, LineBreakInitializerRewriter.cs:205/:272/:303/:334/:387/:417. The `}`-followed-by-content break it was written for can never be applied from inside these rewriters; hoist the concern to a parent-level rewriter or remove the method.
- [Minor] Reihitsu.Formatter\Pipeline\LineBreaks\LineBreakBlockRewriter.cs:57 — the two `EnsureStatementsStartOnSeparateLines` overloads (:57 for blocks, :103 for switch sections) are copy-paste identical except for the statement-list owner; hoist one shared implementation over the statement list.
- [Minor] Reihitsu.Formatter\Pipeline\LineBreaks\DeclarationBraceLineBreakRewriter.cs:69 — `CollapseParameterListToDeclarationLine` duplicates `CollapseOpenParenToDeclarationLine` (LineBreakListRewriter.cs:73), which runs earlier in the same phase and performs the same join through the *actual* previous token; the declaration-token variant measures the gap identifier→`(` and therefore no-ops for generic members (`M<T>\n(...)` — the break sits in `>`'s trailing trivia) while the list-rewriter variant handles them. By the time this rewriter runs, the list rewriter has already collapsed the paren; keep one policy.
- [Minor] Reihitsu.Formatter\Pipeline\LineBreaks\LineBreakContainedBlockRewriter.cs:124 — `NormalizeBlockBraces` re-implements `BracePlacer.NormalizeContainedBlock` (BracePlacer.cs:169) including the same four-step annotation dance, but with a divergent open-brace step (`NormalizeGapBeforeToken` strips the previous token's trailing whitespace; the BracePlacer variant preserves it). Consolidate on one implementation.
- [Minor] Reihitsu.Formatter\Pipeline\LineBreaks\LineBreakContainedBlockRewriter.cs:71 — `TryGetContainedBlock` handles checked/unchecked, using, lock, fixed, try/catch/finally, loops, accessors and lambdas but misses `UnsafeStatementSyntax`; `unsafe { ... }` never gets contained-block brace normalization (`VisitBlock` alone cannot supply it because the block's open brace has no in-node previous token). Add the case.
- [Minor] Reihitsu.Formatter\Pipeline\LineBreaks\TokenLocator.cs:40 — `TryGetPreviousToken` performs a full `DescendantTokens(descendIntoTrivia: true)` walk from the node start on every call, and the gap normalizer calls it for every brace/gap edit — for type- and namespace-level `NormalizeBraces` that is repeated linear walks over the whole declaration (quadratic on large types). Resolve the previous token structurally (`token.GetPreviousToken()` plus a containment check) instead of walking.
- [Minor] Reihitsu.Formatter\Pipeline\LineBreaks\PropertyLayoutLineBreakRewriter.cs:63 — `CollapseExpressionBodiedProperty` returns `null` for "nothing to do" (`node?.ExpressionBody == null`); the only current caller pre-checks `ExpressionBody != null`, but any future call on an accessor-list property would delete the property from the tree (a rewriter treats null as node removal). Return `node` instead.
- [Minor] Reihitsu.Formatter\ReihitsuFormatterHelpers.cs:113 — the manual string-scan fallback in `ComputeTokenColumn` is unreachable: every token reachable from a node has a non-null (for detached nodes, fabricated) `SyntaxTree`, so the `syntaxTree != null` fast path always wins. Dead code; drop the fallback.
- [Minor] Reihitsu.Formatter\Pipeline\LineBreaks\TernaryLineBreakRewriter.cs:177 — the colon join is asymmetric to the question-token join: `MoveQuestionTokenToNextLine` strips the when-true operand's leading end-of-line (:131) and re-adds a separating space, but `NormalizeColonTokenPosition` replaces only `[whenTrueLast, colon]` and leaves the when-false operand's leading trivia intact, so `cond ? a :\n\n    b` yields a `:` dangling alone on its line (stable on re-run, but under-normalized). Mirror the question-token handling.

Idempotency notes (criterion G.6, LineBreaks subphases): the examined pairs converge — assignment value pull-up vs. ternary/binary operator breaking (disjoint token sets), list first-element collapse vs. separator splitting (split re-adds the EOL after the separator), property expression-body collapse vs. operator rewriters (collapse touches only arrow/first token), attribute split/merge/placement (each reaches a fixed point). The one non-convergent loop found is the `ApplyPlacement` hang reported above; no silent two-phase oscillation was identified.

### Session 3 — remaining pipeline phases

- [Major] Reihitsu.Formatter\Pipeline\HorizontalSpacing\HorizontalSpacingRewriter.cs:85 — the phase edits only the *left* token's trailing trivia and never inspects or clears the right token's leading whitespace, but the LineBreaks phase creates exactly that shape mid-pipeline (no re-parse happens between phases): `NormalizeLeadingTriviaToSameLine` (LineBreakAssignmentRewriter.cs:58, used at :144 for `x\n= 1` and :289 for `int x\n= 1`) attaches the separating space as *leading* trivia of the `=` while the previous token's trailing is emptied. `OperatorSpacingRule` then demands 1 space and `SetTrailingWhitespace` adds a trailing space on top of the surviving leading space, so the single-run output is `x  = 1` (two spaces); the second run collapses it back to one — a direct B.1 idempotency break (`--check` after format still reports changes) and spacing-analyzer bait on formatter output. `CleanupPhase` does not repair interior double spaces. Make the rewriter fold the next token's same-line leading whitespace into the gap computation (or forbid rewriters from attaching same-line leading whitespace).
- [Major] Reihitsu.Formatter\Pipeline\HorizontalSpacing\NoSpaceSpacingRule.cs:44 — the #267 re-lex guard is applied, but `UnaryOperatorSpacingUtilities.WouldGlueIntoDifferentOperator` (Core :25) covers only `-`/`+` sign pairs while `IsPrefixTightUnaryOperatorToken` demands zero spacing for *every* prefix unary operator: for an address-of chain `& &x` (both `&` tokens parented by `AddressOfExpression`) the rule glues the tokens into `&&`, which re-lexes as the `&&` operator and no longer parses — the same corruption class #267 fixed for `- -x`. The other prefix operators are safe (`* *p`, `^ ^`, `~ ~`, `! !` have no merged token; `-- -x`/`++ +x` re-lex greedily to the same tokens). Widen the Core guard to any adjacency whose concatenation lexes to a different first token (`&`+`&`, `&`+`&&`), or exclude `&` from the tight set. The sign-sequence adjacencies handed over by session 2 are otherwise safe: for `-`+`-b` left by `BinaryOperatorLineBreakRewriter`, the guard suppresses the no-space rule and `OperatorSpacingRule` reinstates the single space.
- [Major] Reihitsu.Formatter\Pipeline\BlankLines\BlankLineCollapser.cs:53 — `CollapseBlankLinesInTrivia` splits the trivia list into lines only at top-level `EndOfLineTrivia`, but every preprocessor directive and every documentation comment *embeds* its terminating newline inside the structured trivia. The first EOL after such trivia is therefore glued to the directive/doc line (non-blank) and escapes the blank-run detection: `#region R` or `/// </summary>` followed by three blank lines collapses to **two** blank lines, never to one — a stable-but-wrong fixed point. The text-line-based RH5023 (multiple blank lines) analyzer will keep flagging output the formatter cannot fix (G.4). Treat EOL-embedding trivia (directives, doc comments) as line terminators when splitting.
- [Major] Reihitsu.Formatter\Pipeline\BlankLines\BlankLineEditor.cs:178 — `EnsureBlankLineBeforeFirstDirective` counts the contiguous `EndOfLineTrivia` run directly before the directive and compares it against `previousTokenEndsWithLineBreak ? 1 : 2`, which assumes everything between the previous *token* and the directive is whitespace. With a comment line in between (`code();\n// header\n#region R`) the counted EOL is the comment's line terminator, so the check is satisfied and **no blank line is enforced between a comment and `#region`**, while the Core policy the RH5031 analyzer consumes (`RegionDirectiveBlankLineUtilities.IsMissingRequiredBlankLineBefore`, text-line based, no comment exemption) flags exactly that shape — a permanent diagnostic the CLI cannot resolve. Related divergence in the same rewriter: `BlankLineRegionDirectiveRewriter.cs:135` exempts region directives after a switch-label colon via `IsFirstInBlock`, but the Core policy exempts only open-brace lines. Align the formatter's gap measurement (count blank lines, not EOLs) and reconcile the exemption sets with Core.
- [Major] Reihitsu.Formatter\Pipeline\Indentation\IndentationRewriter.cs:196 — `HandleNonWhitespaceTrivia` resets `atLineStart` only after `DirectiveTriviaSyntax`, but documentation comments also embed their terminating newline. After a doc comment the builder still believes it is mid-line, so the whitespace before the following token is passed through raw and the token's layout entry is never applied: **a member preceded by a doc comment is never re-indented**. Because `CommentIndentationContributor` *does* set the doc comment's first line to the member's target column (applied via the line-start path), a mis-indented doc/member pair is actively torn apart — the `/// <summary>` line moves to the target column while the member (and the interior `///` lines) stay at the original column. Also breaks analyzer/formatter parity for RH5204 on any documented member. Set `atLineStart = true` (and advance the line) after doc-comment trivia just as for directives.
- [Major] Reihitsu.Formatter\Pipeline\Indentation\LayoutComputer.cs:287 — pass 1 sets a block-level column for *every* first-on-line token of a statement, so any multi-line construct with no pass-2 contributor is actively **flattened** to the statement column instead of being left alone. Uncovered constructs found: `with`-expression initializers (`InitializerExpressionSyntax` parented by `WithExpressionSyntax` — ObjectInitializerContributor.cs:78 matches only `AssignmentExpressionSyntax` parents and the four creation-expression shapes), classic array/object initializers attached through `EqualsValueClause` (`int[] x =\n{\n    1,\n};` — members and both braces are all forced to the declaration column), and `stackalloc` initializers. Either add contributor coverage for the missing parents or make pass 1 skip lines inside constructs no contributor claims (leave-alone is the safe default per B.6).
- [Major] Reihitsu.Formatter\Pipeline\DocumentationComments\DocCommentElementNormalizer.cs:128 — `GetElementContentLines` drops every whitespace-only content line (`string.IsNullOrWhiteSpace(currentLine) == false`), so when an element is rebuilt (e.g. a `<code>` or `<remarks>` block that starts on its tag line and therefore `NeedsLineAlignmentNormalization`), blank separator lines inside the element — blank lines between paragraphs or inside code samples — are silently deleted. The #270 fix preserves *indentation* inside `<code>` (prefix-relative content survives) but not *blank lines*, which are equally significant there. Keep empty content lines (emit the bare continuation prefix for them).
- [Major] Reihitsu.Formatter\Pipeline\UsingDirectives\UsingLeadingTriviaBuilder.cs:51 — `CreateLeadingTrivia` moves *all* significant leading trivia with a reordered directive. A file-header banner (copyright comment, `// <auto-generated>`-adjacent headers below the guard threshold, tool banners) is leading trivia of the *first* using, so when the canonical order demotes the original first directive (`using Z.Lib;` above `using System;`, alias/global reordering), the banner travels into the middle of the using block: `using A;\n\n// <copyright/>\nusing Z.Lib;`. That both corrupts the file layout and makes RH8402 (file must start with copyright header) flag formatter output. Detach the original first directive's header block (comments separated from the directive by a blank line, or all leading comments when the directive moves) and keep it at the top of the scope.
- [Minor] Reihitsu.Formatter\Pipeline\HorizontalSpacing\TrailingWhitespaceWriter.cs:162 — `NeedsTrailingSpaceNormalization` treats any single-character whitespace trivia as already normalized, so a lone tab between tokens survives (`var\tx`) while two tabs collapse to one *space*; the "collapse multiple spaces" fallback is inconsistent for tabs. Compare the trivia text against a single space instead of testing `Span.Length > 1`.
- [Minor] Reihitsu.Formatter\Pipeline\HorizontalSpacing\NoSpaceSpacingRule.cs:87 — `IsMemberAccessToken` covers only `MemberAccessExpressionSyntax`/`QualifiedNameSyntax`/`AliasQualifiedNameSyntax` dots: the `.` of a conditional-access `MemberBindingExpressionSyntax` (`x?. Foo` / `x ?.Foo`) and the `->` of pointer member access are never tightened, while the plain `x. Foo` is — under-normalization inconsistent with the rule's stated intent. Add the missing parents (and `MinusGreaterThanToken` if pointer access is meant to be covered).
- [Minor] Reihitsu.Formatter\Pipeline\BlankLines\BlankLineBreakSpacingRewriter.cs:74 — the break-spacing rules key on the raw statement list (`previousStatement is not BreakStatementSyntax`, `:168` last statement of the previous switch section), so a *braced* body (`case 1: { ...; break; }` — the shape `SwitchCaseBraceRewriter` itself produces for multi-line switches, and `{ break; }` blocks in plain lists) never triggers the blank-line insertion; within blocks the generic close-brace rule in `BlankLineStatementSpacingRewriter` masks this, but between braced switch sections no blank line is ever enforced. If RH5011 (break followed by blank line) descends into blocks, this is a permanent-diagnostic parity gap — see open cross-check. Unwrap single blocks when testing for a terminal `break`.
- [Minor] Reihitsu.Formatter\Pipeline\RegionFormatting\RegionNamingRewriter.cs:34 — no `IsActive` check anywhere in the region/blank-line handling: region directives inside excluded `#if false` blocks are still renamed/capitalized and their `#endregion` comments synchronized, `BlankLineRegionDirectiveRewriter.cs:123` inserts blank lines around them, and `LayoutComputer.SetDirectiveIndentation` (LayoutComputer.cs:246) re-indents them — the formatter edits disabled code that the analyzer side deliberately skips (`RegionDirectiveBlankLineUtilities.IsAdjacentToNonFormattableLine` excludes disabled lines). Whitespace-only and semantically harmless, but it violates the leave-disabled-code-alone guard philosophy (B.5) and creates one-time diffs the analyzers never asked for. Skip trivia with `IsActive == false`.
- [Minor] Reihitsu.Formatter\Pipeline\LineEndings\LineEndingNormalizationPhase.cs:24 — only `EndOfLineTrivia` is rewritten; line breaks embedded inside multi-line comment trivia and `DisabledTextTrivia` are not normalized (raw-string tokens are rightly untouched), so a CRLF file with LF-only line breaks inside a `/* */` comment or an `#if false` block keeps mixed endings after formatting. If RH5604 (mixed line endings) measures raw text, the diagnostic persists on formatter output — see open cross-check. Extend normalization to the text of comment/disabled trivia (not string tokens).
- [Minor] Reihitsu.Formatter\Pipeline\DocumentationComments\DocumentationCommentFormattingPhase.cs:57 — the `///`-prefix normalization regex anchors at line start (`(?m)^(///)`), but inside a doc-comment trivia every continuation line begins with its indentation whitespace, so only comments at column 0 are ever matched — for any indented member the `///text` → `/// text` normalization silently never applies. Allow leading `[ \t]*` before the exterior.
- [Minor] Reihitsu.Formatter\Pipeline\DocumentationComments\DocumentationCommentFormattingPhase.cs:40 — when normalization candidates are nested (`<code>` needing alignment inside a `<remarks>` needing alignment), only the outermost is rebuilt in this run and the inner candidate's raw text is spliced through; the inner element may still require normalization afterwards, so the *second* pipeline run changes the file again (B.1 two-run convergence instead of one). Iterate until stable within the phase or rebuild nested candidates inside-out.
- [Minor] Reihitsu.Formatter\Pipeline\Indentation\IndentationPhase.cs:22 — the whole phase ignores its `CancellationToken`: `LayoutComputer.Compute` (pass 1 recursion, pass 2 `DescendantNodesAndSelf` × 16 contributors, pass 3) and `IndentationRewriter.Apply` contain no `ThrowIfCancellationRequested`, making the most expensive phase the only uncancellable one (A.3). Thread the token through and observe it in the traversal loops.

Idempotency/parity notes (G.6, session-3 scope): HorizontalSpacing, Indentation, RawStringAlignment, SwitchCaseBraces and UsingDirectives each converge on the second run apart from the double-space finding above (the raw-string shifter reaches `openingColumn == closingColumn`, added switch braces re-classify as multi-line, canonical using order is order-stable, and the blank-line insert/collapse pairs meet at one blank line — except the two-blank-line fixed point after EOL-embedding trivia reported against `BlankLineCollapser`). The cross-phase pair BlankLineTokenCleanupRewriter (collapses blanks before `while`) ↔ BlankLineStatementSpacingRewriter (re-inserts one for while-statements) converges within a single phase run because the subphases execute in that order.

## Reihitsu.Analyzer

### Session 4 — infrastructure

- [Major] Reihitsu.Analyzer\Core\FluentChainAnalysisHelper.cs:73 — `CollectChainLinks` never descends into `ConditionalAccessExpressionSyntax.WhenNotNull`: the conditional-access arm adds the `?.` token and then walks `conditionalAccess.Expression` (the left side), so every link inside the when-not-null portion (`.B()`, further `?.`s in `a?.B()?.C()`) is dropped — and no other registration analyzes that portion because `IsInnerChainMember` classifies its member accesses as inner. Consequence in the consuming rule: `RH5201MethodChainsShouldBeAlignedAnalyzer.AnalyzeChain` sees `chainLinks.Count < 2` for a pure `?.` chain and returns — conditional-access fluent chains are never alignment-checked (false negatives for RH5201, and RH5112 inherits the same truncated link list). Collect the when-not-null links in chain order.
- [Major] Reihitsu.Analyzer\Base\StructEqualityPerformanceAnalyzerBase.cs:36 — `AreEqualityMembersImplemented` tests `type.Interfaces` (directly listed interfaces only) instead of `AllInterfaces`, so a struct that implements `IEquatable<T>` through an intermediate interface (`interface IValue<T> : IEquatable<T>`) is classified as lacking equality members and the derived performance rules keep flagging it — a false positive the user cannot silence by implementing the interface indirectly. Use `AllInterfaces`. *(Session 7 — affected rules confirmed: RH1001 and RH1002, the base's only two derived analyzers; both funnel every struct type argument through `AreEqualityMembersImplemented`.)*
- [Minor] Reihitsu.Analyzer\Core\DirectDocumentationSyntaxChecker.cs:59 — unfixed round-1 finding: `GetDirectTags` still silently falls back to a full `DescendantNodes()` search when no *direct* child tag matches, so a `<summary>` nested inside `<remarks>` satisfies a "has direct summary" query (`HasDirectTag`/`GetFirstDirectTag`); name and behavior disagree and the fallback changes rule semantics for nested tags. Either drop the fallback or rename/document the contract.
- [Minor] Reihitsu.Analyzer\Core\SpeculativeRebindingHelper.cs:20 — `AreEquivalent` returns `true` when one side bound a symbol and the other failed to bind at all (one `Symbol` null, both candidate lists empty → the 0==0 candidate comparison succeeds). Both current callers (RH3101:135, RH3105:57) compensate with a duplicated did-it-bind precheck, so the hole is unreachable today, but the guard belongs inside the helper so a future call site cannot re-open it (G.2).
- [Minor] Reihitsu.Analyzer\Base\DiagnosticAnalyzerBase.cs:96 — `SupportedDiagnostics => [_rule]` allocates a fresh `ImmutableArray` on every access; the analyzer driver queries the property repeatedly for every analyzer instance. Cache the array in a field. (Verified fixed in this file: #278's primary-location bug — `CreateDiagnostic(ImmutableArray<Location>)` now uses `locations[0]` with `Skip(1)` as additional locations; `EnableConcurrentExecution`/`ConfigureGeneratedCodeAnalysis(None)` are unconditional, no `#if RELEASE` split remains; all 191 derived `Initialize` overrides call `base.Initialize`.)
- [Minor] Reihitsu.Analyzer\Data\ConfigurationManager.cs:49 — the configuration file is located with `Path.EndsWith("reihitsu.json", OrdinalIgnoreCase)`, so any additional file whose name merely ends with that string (`myreihitsu.json`, `team-reihitsu.json`) is silently treated as *the* configuration. Match the file-name segment exactly.
- [Minor] Reihitsu.Analyzer\Base\StatementShouldBeFollowedByABlankLineAnalyzerBase{TStatement}.cs:53 — copy-paste artifacts: `IsFollowedByBlankLine` is documented as "Check if, the statement preceded by a blank line", `GetNextToken` as "Get previous token", and `OnStatement` in both blank-line bases as "Analyze try statement" (also `StatementShouldBePrecededByABlankLineAnalyzerBase{TStatement}.cs:116`). The two bases additionally carry byte-identical private blank-line walkers (`IsFollowedByBlankLine`/`IsPrecededByBlankLine`) — hoist one shared helper.
- [Minor] Reihitsu.Analyzer\Base\InterfaceImplementationUtilities.cs / Base\OverrideMemberUtilities.cs / Core\SplitElementDocumentationAnalyzerBase.cs — folder inconsistency: the first two are static helper classes (not bases) living in `Base` while their peers (`FluentChainAnalysisHelper`, `NestedTypeAnalyzerHelper`, …) live in `Core`; conversely `SplitElementDocumentationAnalyzerBase` is an analyzer base living in `Core`. Note both helpers return `INamedTypeSymbol.Name` without arity, feeding the open generic-region-name cross-check (`IEnumerable<T>` regions classify as Custom).
- [Minor] Reihitsu.Analyzer\AnalyzerResources.resx — leftover designer template entries `Bitmap1`, `Color1`, `Icon1`, `Name1` (dead resources; same four in `Reihitsu.Analyzer.CodeFixes\CodeFixResources.resx`). All 624 analyzer / 222 code-fix RH resource names referenced by the wrapper classes are present in the resx files (mechanically verified).

### Session 5 — Layout rules

- [Critical] Reihitsu.Analyzer\Rules\Layout\RH5106ClosingParenthesisMustBeOnLineOfLastArgumentAnalyzer.cs:51 (also RH5105...cs:50, RH5108...cs:54) — directive reachability for the CollapseTokenGap pairs is confirmed, escalating the session-4 Major (hole a) to Critical: all three analyzers compare token lines with no directive exemption, so `void M(int a\n#if X\n    , int b\n#endif\n)` (conditional parameters — a realistic pattern) is flagged by RH5106, `void M(\n#if X\n    int x)` by RH5108, and `void M\n#if X\n(int x)` by RH5105. The registered fix (`CollapseTokenGapCodeFixProviderBase.cs:33`) replaces the whole inter-token gap with an empty string, deleting the `#if`/`#endif` and emitting non-compiling code (CS1027). Either exempt directive-bearing gaps in the analyzers (mirroring the formatter's `CanSafelyCollapseBracketedArguments` policy) or add the directive guard to the fix base.
- [Critical] Reihitsu.Analyzer\Base\TargetAttributePlacementAnalyzerBase.cs:75 — directive reachability confirmed for the SingleLine-mode placement pairs, escalating the session-4 Major (hole c) to Critical: `hasViolation` for `TargetAttributePlacementMode.SingleLine` is a bare line comparison, so RH5523 (parameter attributes) and RH5530 (single-line-accessor attributes) flag the conditionally-compiled-attribute idiom `#if X\n[NotNull]\n#endif\nstring target` (close line of the list != target line). The fix (`TargetAttributePlacementCodeFixProviderBase.cs:142`) guards `tokenAfter.LeadingTrivia` for comments only and then wipes it, deleting the `#endif` (orphaned `#if`, CS1027). SeparateLine-mode consumers are unaffected (the attribute already sits on its own line, so the shape is not flagged). Exempt directive-bearing gaps in the analyzer or make the fix guard directive-aware.
- [Major] Reihitsu.Analyzer\Base\StatementShouldBePrecededByABlankLineAnalyzerBase{TStatement}.cs:126 — embedded-statement false-positive family: the exemption set (`OpenBraceToken`, `ColonToken`, `None`) misses `)` and `else`, so every registered statement kind appearing as an *unbraced child* is flagged — `while (x)\n    return;` (RH5008), `foreach (var i in items)\n    yield return i;` (RH5019: `GetPreviousStatement` returns null for non-list parents, so the yield exemption does not apply), `else return;` (RH5008 — a blank line between `else` and `return` on the same line is demanded), `if (x) return;` (shielded in practice only because RH5405 forces braces on if-children; while/for/foreach/using/lock/fixed children have no braces rule and are analyzer-legal). The formatter never inserts blank lines for embedded children (`BlankLineStatementSpacingRewriter` walks statement lists only), so the fix inserts a blank line between a control-flow header and its own body that no formatter phase would produce. Exempt statements that are the embedded child of another statement (parent not Block/SwitchSection/GlobalStatement). Affects RH5001–RH5019 (RH5002 handles only the `else if` shape).
- [Major] Reihitsu.Analyzer\Base\StatementShouldBePrecededByABlankLineAnalyzerBase{TStatement}.cs:131 + Reihitsu.Formatter\Pipeline\BlankLines\BlankLineEditor.cs:209 — the #356 directive exemption is one-sided: the PrecededBy analyzers (and RH5020...cs:62) exempt statements/comments whose immediately-preceding line is a preprocessor directive, but the formatter has no matching exemption — `EnsureBlankLineBeforeStatement` inserts the EOL at leading-trivia index 0, so analyzer-clean `#if DEBUG\nFoo();\n#endif\nreturn x;` is rewritten to `Foo();\n\n#endif\nreturn x;` (a blank line *inside* the conditional region, above the `#endif`), and `#endif\n// note` gains a blank line via `EnsureBlankLineBeforeFirstComment` although RH5020 exempts it. G.4 direction 2: the CLI silently reformats what the IDE never reports, and the blank lands at a position no rule asked for. The sibling FollowedBy base has no directive exemption at all (RH5011 flags `break;\n#endif\n...` while RH5008 would exempt the mirrored shape) — pick one policy: mirror the exemption in the formatter's blank-line inserters (skip directive-adjacent statements) and add it to the FollowedBy base, or drop #356 from the analyzers.
- [Major] Reihitsu.Analyzer\Rules\Layout\RH5030BlankLineAfterClosingBraceAnalyzer.cs:131 (and RH5010...cs:53) — switch-section blocks diverge from the formatter in both directions. (1) `OnBlock` hardcodes `inSwitchSection: false` while the formatter's `BlankLineStatementSpacingRewriter.VisitBlock` passes `node.Parent is SwitchSectionSyntax`, so inside a user-braced section `case 1: { Foo(); break; }` (multi-line, left as-is by `AddBraces`) RH5030 demands a blank line before `break` after an inner `}` and RH5010 demands one after `Foo();`, but the formatter refuses to insert either — permanent diagnostics (G.4 direction 1). (2) RH5010's `IsRelevant` exempts any break preceded by `}` regardless of context, while the formatter's close-brace rule inserts a blank line before break-after-`}` in non-switch blocks — analyzer-clean code is reformatted. (3) RH5030's `HasBlankLineBetween` (:46) counts EOLs without resetting on content, so `}\n// note\nFoo();` counts two EOLs and passes although no blank line exists — the formatter inserts one (false negative plus instability). Thread the block-under-switch-section flag through RH5030/RH5010 and count blank lines, not EOLs.
- [Major] Reihitsu.Analyzer\Rules\Layout\RH5104CommentsMustBeOnTheirOwnLineAnalyzer.cs:99 — no formatter phase owns the comments-on-own-line concern: the only comment relocation in the pipeline is `LineBreakContainedBlockRewriter.MoveTrailingConditionCommentToOwnLine` (if-conditions only), and `SwitchCaseBraceRewriter` deliberately *preserves* `case 1: // note` trailing comments. Every ordinary trailing or inline comment (`int x = 1; // note`, `Foo(/* count: */ 5)`) is therefore a permanent diagnostic on formatter output (G.4 direction 1). Either add a formatter phase that moves inline comments onto their own line or restrict/downgrade the rule.
- [Major] Reihitsu.Analyzer\Rules\Layout\RH5204IndentationMustUseFourSpacesPerScopeLevelAnalyzer.cs:146 — `GetChildIndentLevel` only increments inside the five brace constructs (namespace/type/block/switch/accessor-list), so the embedded child of an unbraced statement inherits the *parent statement's* level: for `while (x)\n    return;` the `return` line's expected column equals the `while` column, and the correctly indented shape is flagged (keyword-led embedded statements — return/throw/break/continue/nested if/…; expression statements escape only because `ShouldAnalyzeToken` requires the first token's immediate parent to be the statement). The formatter's `LayoutComputer` shares the identical model (same `GetIndentingBraceRange`/child-indent logic, no embedded-statement contributor), so `reihitsu-format` actively de-indents `while (x)\n    count++;` to the `while` column — the two engines agree with each other but both are wrong, and `ControlFlowBraceTransform` shields only if/else children. Add an embedded-statement level increment to both (extends the session-3 `LayoutComputer.cs:287` Major).
- [Major] Reihitsu.Analyzer\Rules\Layout\RH5601UseTabsCorrectlyAnalyzer.cs:75 — no formatter phase converts tabs anywhere in the pipeline (the only `'\t'` handling lives in `DocCommentElementNormalizer`), so every RH5601 diagnostic outside the lines the IndentationRewriter happens to rewrite survives `reihitsu-format` — tab-indented files keep permanent diagnostics (G.4 direction 1). Additionally `root.FindToken(index)` maps tabs inside multi-line comments and `#if false` disabled text to the owning non-string token, so comment/disabled-text interiors are flagged although no formatter phase may rewrite them (same class as the #288 family; the disabled-text case would also make the code fix edit inactive code). Add tab normalization to the formatter and exempt comment/disabled-text interiors (or document both as intended).
- [Major] Reihitsu.Analyzer\Rules\Layout\RH5602CodeMustNotContainTrailingWhitespaceAnalyzer.cs:48 — the exclusion set is only `GetStringLineIndices` although Core provides `GetNonFormattableLineIndices` (used by RH5023 for exactly this purpose): trailing whitespace *inside* comment trivia — `// note ` (trailing spaces are part of the single-line-comment trivia text), multi-line comment interiors, doc-comment lines, and `#if false` disabled text — is flagged, but `CleanupPhase` removes only `WhitespaceTrivia` before `EndOfLineTrivia` and never edits trivia interiors, so these are permanent diagnostics on very common shapes (G.4). Switch to `GetNonFormattableLineIndices` and decide the `// note ` policy on both sides at once (the comment-trailing-space case needs either a text-level cleanup in the formatter or a line-scan exclusion here).
- [Major] Reihitsu.Analyzer\Rules\Layout\RH5408SimpleAutoPropertiesShouldBeSingleLinedAnalyzer.cs:93 (comment-in-gap permanent-diagnostic family; also RH5113...cs:62, RH5101...cs:56, RH5108...cs:54) — analyzers flag shapes whose collapse the formatter refuses because a comment sits in the join gap: RH5408's eligibility guards the accessor list and initializer but not the signature's trailing trivia, so `public int X // note\n{ get; set; }` is flagged while `CanCollapseAutoPropertyToSingleLine` refuses via `WouldJoinIntoComment`; RH5113 flags `int _x = 1 // note\n;` while `DeclarationSemicolonLineBreakRewriter`'s `CollapseTokenToSameLine` refuses the comment join; RH5101/RH5108 flag `Foo( // note\n    a)` / the first-parameter analog whose collapse `CollapseFirstElementToSameLine` refuses. Each is a permanent diagnostic (G.4 direction 1). Mirror the comment-gap refusal in the analyzers (one shared predicate next to the Core join guards).
- [Minor] Reihitsu.Analyzer\Rules\Layout\RH5106ClosingParenthesisMustBeOnLineOfLastArgumentAnalyzer.cs:65 — two round-1 Minors here remain unfixed: the rule still covers only methods and constructors while its siblings RH5105/RH5108 were extended to seven declaration kinds (local functions, operators, conversion operators, delegates, records) and the formatter normalizes every `ParameterListSyntax` including lambdas; and it is still a syntax-tree action running two full `DescendantNodes()` scans (round-1 finding also listed RH5107 and RH5402–RH5407, all still tree actions today). Extend the scope to match RH5105 and convert the family to node actions.
- [Minor] Reihitsu.Analyzer\Rules\Layout\RH5402BracesForMultiLineStatementsMustNotShareLineAnalyzer.cs:49 (and RH5403...cs:49) — round-1 finding unfixed: the parent-statement sets are still inconsistent (RH5402 nine parents, RH5403 five) and both still omit `ForEachVariableStatementSyntax`, so braces of a deconstruction foreach (`foreach (var (a, b) in ...)`) are exploded by the formatter but never flagged. Share one parent-set constant including both foreach kinds.
- [Minor] Reihitsu.Analyzer\Rules\Layout\RH5409FinalEnumMemberMustNotHaveTrailingCommaAnalyzer.cs:60 — round-1 finding unfixed: RH5409/RH5410/RH5411 remain three character-identical implementations differing only in the registered `SyntaxKind`; fold into a parameterized base like the other Layout families. (Scope itself is correct — see the resolved trailing-comma cross-check.)
- [Minor] Reihitsu.Analyzer\Rules\Layout\RH5417MemberDeclarationBracesMustNotShareLineAnalyzer.cs:44 — `IsCoveredMemberBody` covers method/ctor/dtor/operator/conversion/accessor bodies, but the formatter's `LineBreakBlockRewriter.VisitBlock` explodes *every* single-line block: local-function bodies and statement-lambda/anonymous-method bodies are reformatted with no analyzer reporting them (and no other RH54xx rule covers them). Add `LocalFunctionStatementSyntax` (and decide the lambda policy) or document the omission.
- [Minor] Reihitsu.Analyzer\Rules\Layout\RH5111AssignmentsMustHaveProperLineBreaksAnalyzer.cs:77 — the rule covers assignments, variable declarators, and property initializers, but the formatter's `VisitEqualsValueClause` (`NormalizeEqualsValuePlacement`) also joins parameter default values (`void M(int x =\n    5)`) and enum-member equals values onto the `=` line — shapes RH5111 never reports (silent reformatting, G.4 direction 2). Register the missing EqualsValueClause parents or leave a comment documenting the deliberate gap.
- [Minor] Reihitsu.Analyzer\Base\RegionDirectiveBlankLineAnalyzerBase.cs:71 — exemption sets diverge from `BlankLineRegionDirectiveRewriter` in the analyzer-lenient direction: the Core policy exempts a `#region` whose next line starts with `}` (`IsMissingRequiredBlankLineAfter`) and any directive adjacent to a non-formattable line (`IsAdjacentToNonFormattableLine`), while the formatter inserts blank lines in both shapes (`EnsureBlankLineAfterRegionDirectives` exempts only `#endregion` before `}`/EOF and has no non-formattable check) — analyzer-clean `#region R\n/* banner */` or `#region R\n}` is reformatted. Also, each of the two derived rules independently runs three full-tree scans (`GetLineIndicesEndingWithToken`, `GetLineIndicesBeginningWithToken`, `GetNonFormattableLineIndices`) plus a full `DescendantTrivia` walk with no `root.ContainsDirectives` early-out — six tree scans per file even without a single `#region`. Align the exemption sets and add the early-out.
- [Minor] Reihitsu.Analyzer\Rules\Layout\RH5020SingleLineCommentsShouldBePrecededByABlankLineAnalyzer.cs:133 — comment-kind asymmetry with `BlankLineTriviaBoundaryRewriter.EnsureBlankLineBeforeFirstComment`, which enforces the blank line before the first comment of *any* kind (multi-line, doc, `////`): RH5020 checks only `//` comments, exempts `////`, and exempts directive-adjacent comments, so `/* banner */`-, doc-comment-, and `////`-preceded shapes are silently reformatted by the CLI without ever being reported (G.4 direction 2). Align the comment-kind set (or exempt the extra kinds in the formatter).

Cross-check resolutions and parity notes (G.4, session-5 scope): RH5006/RH5009/RH5019 round-1 kind gaps are closed (both foreach kinds, all three goto kinds, both yield kinds plus the switch-section-aware previous-statement walk). RH5021/RH5029 exactly mirror `NeedsBlankLineBefore`'s local-declaration/assignment exemptions (cross-check (e) resolved). RH5011's close-brace exemption matches the formatter's braced-section behavior on both sides (cross-check (d) resolved — the RH5010/RH5030 gaps above are the real defect in that area). RH5022–RH5028 all report whitespace-only spans (blank lines including their line break, or the leading blank region of the file), satisfying the `BlankLineSpanRemovalCodeFixProviderBase` contract; blank lines flagged inside verbatim/raw interpolation holes are hole trivia whose removal does not alter string values. RH5023 confirms cross-check (a): it flags the two-blank-line fixed point the `BlankLineCollapser` off-by-one leaves after directives/doc comments — the session-3 Major at `BlankLineCollapser.cs:53` is the fix point; RH5032 itself is satisfied by two blank lines, so cross-check (c) collapses into the same formatter finding. RH5031 confirms cross-check (b): it flags `// comment\n#region` and `case 1:\n#region` which the formatter never fixes — both halves already reported as the session-3 Major at `BlankLineEditor.cs:178`. RH5604 resolves cross-check (f) with no finding: both sides enumerate `EndOfLineTrivia` via `descendIntoTrivia: true` and share Core's `DetectEndOfLine`, so comment/disabled-text interiors are invisible to analyzer and formatter alike and raw strings stay exempt on both. RH5204 confirms cross-check (g): documented members are flagged (member line and doc-comment lines both carry expectations) while the session-3 `IndentationRewriter.cs:196` Major makes the CLI unable to re-indent them — permanent diagnostic until the formatter bug is fixed; the code fix shares `BuildExpectedIndentationMap`, so the IDE fix should converge (session 8 verifies). Round-1 alignment held for RH5109 (shared-start-line detection), RH5111 (initializer-kind exemption with a formatter-mirroring comment at :53), RH5206 (first-on-line arm restriction), RH5401 (arrow/expression-start check only), RH5408 (accessor-list guard via Core), RH5601/RH5202 (raw-string and Utf8 kinds on both sides — the formatter's RawStringAlignment covers the Utf8 kinds too), RH5304 (single-line branch is now an explicit allow), RH5301/RH5303 (per-element diagnostic spans), RH5404 (brace-line comparison instead of full span), RH5405/RH5406/RH5407 (else-children covered, single/multi-line split, dead filter removed), RH5603 (no full-text materialization). #342 shipped formatter-only (SwitchCaseWhenLineBreakRewriter, no analyzer counterpart — `when`-guard placement is silently normalized by design). All 111 Layout rule docs exist (helpLinkUri contract).

### Session 6 — Documentation and Organization rules

- [Critical] Reihitsu.Analyzer\Rules\Organization\RH7105DeclarationKeywordsMustFollowOrderAnalyzer.cs:54 (and RH7106...cs:54) — directive reachability confirmed for the modifier-ordering pairs, escalating the session-4 Major (hole b) per the established escalation rule: both analyzers are pure pass-throughs to Core (`TryGetRh7105Violation`/`TryGetRh7106Violation`) with no directive exemption, so a misordered modifier list with `#if`/`#endif` between the modifiers (e.g. a conditionally compiled `unsafe`/`required` modifier) is flagged, and `ModifierOrderingCodeFixProviderBase` (whose `ReorderingWouldDropComment` guard checks comments only, session-4 finding) rebuilds the tokens with default trivia and deletes the directives — orphaned `#endif`, CS1027. Reachability requires a directive inside a flagged modifier list (rarer than the RH5105/RH5523 shapes); exempt directive-bearing modifier lists in the analyzers or make the fix guard directive-aware.
- [Major] Reihitsu.Analyzer\Base\MembersShouldBeGroupedByRegionsAnalyzerBase.cs:79 (via RH7401–RH7404 vs RH7409–RH7412) — contradictory region demands for an override member that also implements an interface member: for `abstract class Base { public abstract void Run(); }` + `class C : Base, IFoo { public override void Run() { } }` (an interface listed on the derived type, satisfied by the override — the virtual-`Dispose` pattern), RH7401 demands region `Base` (`OverriddenMethod` chain, OverrideMemberUtilities) while RH7409 demands region `IFoo` (`FindImplementationForInterfaceMember` resolves to the override, InterfaceImplementationUtilities), and neither analyzer excludes the other's members. Whichever region the member is placed in, the other rule reports — an unresolvable diagnostic pair (neither rule has a code fix, so the user cannot even ping-pong out). Same conflict for the property/event/indexer quadruples. Define precedence (e.g. interface region wins; the override rules skip members that implement an interface member) and encode it in both bases.
- [Major] Reihitsu.Analyzer\Rules\Organization\RH7207UsingDirectivesShouldBeOrganizedIntoGroupsAnalyzer.cs:48 — `HasBlankLineBefore` treats *any* `EndOfLineTrivia` in the leading trivia as a blank line, but a comment line contributes exactly that trivia: a same-group pair separated by a comment (`using System;\n// I/O helpers\nusing System.IO;`) is flagged as wrongly grouped while the formatter (`UsingLeadingTriviaBuilder.CreateLeadingTrivia`) preserves the comment and inserts no blank line — a permanent diagnostic (G.4 direction 1); conversely a different-group pair separated by a comment line *without* a blank is accepted by the analyzer (`hasBlankLine` true via the comment's EOL) while the formatter prepends an extra EOL — silent reformatting of analyzer-clean code (G.4 direction 2). Count only whitespace-line runs (mirror the formatter's `GetFirstSignificantTriviaIndex` split) instead of any EOL.
- [Major] Reihitsu.Analyzer\Rules\Organization\RH7207UsingDirectivesShouldBeOrganizedIntoGroupsAnalyzer.cs:63 (family: RH7201–RH7206) — no analyzer in the using-ordering family carries the formatter's `UsingDirectiveOrderingSafety.CanSafelyReorder` exemption (any directive trivia or `#`-line inside the block refuses reordering): a conditional-using block (`#if NETFRAMEWORK\nusing System.Web;\n#endif` amid other usings) out of canonical order is flagged by RH7203/RH7207, the formatter refuses to touch it, and the fixes execute the same phase (`UsingDirectiveCodeFixUtilities.OrganizeScopeUsingsAsync`, session 4) so they no-op while the diagnostic persists — the round-1 RH7204 no-op-fix pattern (G.3) reintroduced for the directive shape. Exempt using blocks that `CanSafelyReorder` refuses in all seven analyzers.
- [Major] Reihitsu.Analyzer\Rules\Documentation\RH8301DocumentationLinesMustBeginWithSingleSpaceAnalyzer.cs:79 — only the *first* line of each `///` block is checked (any line whose predecessor is also a doc line is skipped), so continuation-line violations (`/// <summary>\n///text` or a double-space continuation) are never reported, contradicting the rule doc ("Each /// documentation comment line must have exactly one space") — and the code fix edits only the diagnostic's line, so the block is never repaired either. Check every doc line (the fix already works per line).
- [Major] Reihitsu.Analyzer\Rules\Documentation\RH8302ElementDocumentationHeadersMustNotBeFollowedByBlankLineAnalyzer.cs:63 (and RH8303...cs:59) — the `StartsWith("///")` line classification also matches `////` fenced-out code, which RH8301 (`////` exemption) and RH5020 deliberately treat as *not* documentation: RH8302 flags the blank line after a `////` block (its fix deletes a legitimate separator) and RH8303 demands a blank line before a `////` line that directly follows code — false positives that additionally contradict RH5020's explicit `////` exemption. Exclude `////` lines in both analyzers.
- [Major] Reihitsu.Analyzer.CodeFixes\Rules\Documentation\RH8309XmlDocumentationElementsMustFollowPrescribedOrderCodeFixProvider.cs:44 — analyzer/fix policy divergence on unknown elements (resolves the RH8309 cross-check): the analyzer (RH8309...Analyzer.cs:66) skips `UnknownElementRank` elements entirely ("tolerated and do not constrain the order", matching the rule doc), but the fix's `OrderBy(rank)` sorts unknown elements (`inheritdoc`, `seealso`, `include`, custom tags) to the *end* of the comment, so any fix application relocates elements the diagnostic never covered — a leading `<inheritdoc/>` above an out-of-order pair is demoted below `<remarks>`. Both sides consume the same Core rank table and the stable sort keeps unknown elements' relative order, so convergence holds; only the demotion diverges. Make the fix reorder canonical elements around fixed unknown-element positions.
- [Minor] Reihitsu.Analyzer\Rules\Documentation\RH8102ElementParameterDocumentationMustMatchElementParametersAnalyzer.cs:80 (same in RH8109...cs) — the violation condition is `(A || B) && (C || A || B)` with A = index out of range, B = positional name mismatch, C = name not declared; the second conjunct is implied by the first, so the `Contains` clause is dead code (the intended lenient handling of correctly-named but re-ordered tags, or a distinct message for unknown names, never materializes). Simplify to `(A || B)` or implement the intended distinction.
- [Minor] Reihitsu.Analyzer\Rules\Documentation\RH8301DocumentationLinesMustBeginWithSingleSpaceAnalyzer.cs:61 (also RH8302:48, RH8303:48) — the three text-scan doc-layout analyzers exclude only string lines (`GetStringLineIndices`) although Core provides `GetNonFormattableLineIndices`: `///`-prefixed lines inside multi-line comment bodies and `#if false` disabled text are analyzed and flagged although no formatter phase or fix may rewrite them (same class as the session-5 RH5601/RH5602 findings). They are also the only Documentation analyzers besides the explicitly-guarded RH8402 that run under `DocumentationMode.None` (tree actions, no doc-mode check) while the rest of the RH83xx family is silenced — inconsistent guard policy. Switch to `GetNonFormattableLineIndices` and decide the doc-mode policy once.
- [Minor] Reihitsu.Analyzer\Core\DocumentationAnalysisUtilities.cs:25 — false-negative family, resolving the session-4 operator cross-check: no documentation rule covers operators — `DocumentableDeclarationKinds`, `ParameterOwnerKinds` and `ReturnValueOwnerKinds` omit `OperatorDeclaration`/`ConversionOperatorDeclaration`, so RH80xx never demands operator documentation and RH8101–RH8107 never validate operator `<param>`/`<returns>`; likewise primary-constructor parameters (records and C# 12 class/struct primary constructors) are exempt from RH8101/RH8102 because the type-declaration kinds are not parameter owners, although `<param>` on record declarations is the standard documentation shape. The rule docs consistently scope to the covered kinds (no doc/behavior mismatch), so this is a deliberate-looking coverage gap rather than a bug — but it is the only member-kind hole in an otherwise exhaustive 29-rule family. Add operator/primary-constructor coverage or document the exclusion.
- [Minor] Reihitsu.Analyzer\Rules\Documentation\RH8201InheritdocShouldBeUsedAnalyzer.cs:83 — registration covers method/property/event(-accessor)/indexer declarations but not `EventFieldDeclaration`, so a documented field-like override event (`public override event EventHandler E;`) is never checked for `<inheritdoc/>` although RH7403/RH7407 demonstrate the event-field override shape is supported elsewhere. Register the event-field kind.
- [Minor] Reihitsu.Analyzer\Rules\Documentation\RH8303ElementDocumentationHeaderMustBePrecededByBlankLineAnalyzer.cs:66 — two parity gaps: (a) a doc header preceded by an ordinary comment line (`// note\n/// <summary>`) is flagged, but no formatter phase inserts a blank line between two comment blocks (`EnsureBlankLineBeforeFirstComment` covers only the first comment of a trivia block) — CLI-permanent diagnostic, IDE-fix only (G.4 direction 1); (b) the #356 directive exemption exists in RH5020/the PrecededBy statement bases but not here, so `#endif\n/// doc` is flagged and the fix inserts a blank line between the directive and the doc comment. Align the exemption sets with RH5020.
- [Minor] Reihitsu.Analyzer\Rules\Organization\RH7301RegionsShouldMatchAnalyzer.cs:60 — the rule re-implements region-description extraction with raw string surgery (`ToString().Substring(10)`, `" // "` prefix probe, `Substring(8)` on the region text) instead of Core's `GetRegionDescription`/`GetEndRegionDescription`, making byte-exact demands the description-based family does not: `#region  Name` (double separator) or a trailing space produces a mismatch diagnostic although the trimmed descriptions match. It also runs `TryFindMatchingDirective` (a full-root `DescendantTrivia` walk) per `#endregion` — quadratic in region count. Bonus copy-paste artifact: `OnEndRegion`/`OnStartRegion` in RH7301/RH7302 are both documented as "Analyzing all SyntaxKind.LogicalNotExpression occurrences". Consume the Core helpers and hoist the directive index.
- [Minor] Reihitsu.Analyzer\Rules\Organization\RH7109ReadonlyElementsMustAppearBeforeNonReadonlyElementsAnalyzer.cs:88 — registration excludes `InterfaceDeclaration` while sibling RH7102 includes it (C# 8+ interfaces can declare static/const fields), so `static readonly` interface fields are never ordering-checked — inconsistent kind sets within the same field-ordering family. Align the registration lists.

Cross-check resolutions and parity notes (session-6 scope): RH7105 partial-rank (analyzer side) — confirmed, the analyzer is a pure pass-through to Core with no `partial` guard anywhere in the chain (the only `partial` handling is the Core rank at ModifierOrderingUtilities.cs:145), so the session-1 Critical is confirmed end-to-end: `public readonly partial struct S` is flagged and the fix writes the non-compiling `partial readonly` order; nothing remains for session 7. RH7110 implicit accessibility — resolved, no oscillation: the analyzer skips `OrderingAccessibilityGroup.None` members entirely (RH7110...cs:62), matching its rule doc, and RH7110 ships without a code fix. The RH7102/RH7103/RH7109/RH7110 grouping keys nest consistently (accessibility ⊃ static ⊃ const/readonly, per-kind, RH7103/RH7110 additionally per-region), so no mutual-contradiction shapes were found in the ordering family. RegionCategoryUtilities vs. generic names — resolved, consistent by construction: the producers (`OverrideMemberUtilities`/`InterfaceImplementationUtilities`) and the classifier both use `INamedTypeSymbol.Name` without arity, so the convention is plain-name regions and arity-decorated labels (`IEnumerable<T>`) uniformly classify Custom on every consumer; residual ambiguity only where `IComparable` and `IComparable<T>` are both implemented (one shared region name). RH8309 — resolved: the analyzer never flags or constrains unknown elements and reports at most one diagnostic per comment; ranking `value` (slot 4) does not fight RH8202 because RH8202's fix removes the tag and removal is stable under reordering; the one divergence is the fix's unknown-element demotion (Major above). RH8306 — final #370 behavior (sentence terminator or leading verb on property summaries, no fix) exactly matches its rule doc and resources. RH7103 per-region scoping (#355) and RH7308/RH7409–RH7412 (#344/#348) check out on the emphasized shapes: explicit implementations and static abstract implementations resolve to the interface region via `FindImplementationForInterfaceMember`, partial types are analyzed per part with per-part regions, RH7308's noun/modifier label parser ignores non-standard region names, and RH7305A correctly ships disabled-by-default under the suffix-letter convention. RH7501's deliberately narrow shape (terminal break in terminal block) matches its fix contract by design. All 106 Documentation/Organization rule docs exist (helpLinkUri contract), and the RH8306/RH8309 resources match the rescoped behaviors.

### Session 7 — remaining rule categories

- [Critical] Reihitsu.Formatter\Pipeline\StructuralTransforms\InterpolationMarkerRemovalTransform.cs:51 (analyzer side: Reihitsu.Analyzer\Rules\Clarity\RH3204InterpolatedStringsWithoutInterpolationShouldNotUseDollarAnalyzer.cs:52) — the interpolation-marker removal is target-typing-blind: an interpolated string without holes also converts to `FormattableString`/`IFormattable`, and a plain string literal does not, so `FormattableString f = $"...";` is rewritten to `FormattableString f = "...";` (CS0029 — non-compiling output) and `M($"...")` silently switches from an `M(FormattableString)` overload to `M(string)` (behavior change, e.g. `FormattableString.Invariant`, EF `FromSqlInterpolated`). The RH3204 analyzer flags the same shapes (pure syntax check, no converted-type test), so the code fix inherits the corruption (session 9). The #265/#337 scope itself is correct — brace unescaping applies only to non-raw kinds and `RemoveLeadingDollarMarkers` handles `$@`/`@$` — the sole hole is the conversion target. Make the analyzer check `SemanticModel.GetTypeInfo(...).ConvertedType` and drop (or semantic-gate) the formatter transform, which has no semantic model to consult.
- [Major] Reihitsu.Analyzer\Rules\Design\RH2001PrivateAutoPropertiesShouldNotBeUsedAnalyzer.cs:46 — explicit interface implementations have `DeclaredAccessibility.Private`, and an explicit-impl auto-property (`int IFoo.X { get; set; }`) has a compiler-generated backing field so `IsAutoProperty()` is true: the rule flags it although it cannot be replaced by a field (converting it breaks the interface implementation, CS0535). No test covers the shape. Exempt `IPropertySymbol.ExplicitInterfaceImplementations.Length > 0` (and verify the fix side in session 9).
- [Major] Reihitsu.Analyzer\Rules\Clarity\RH3104DoNotUseDefaultValueTypeConstructorAnalyzer.cs:62 — the C#10 guard stops at named types: for a struct-constrained type parameter (`where T : struct`), `new T()` is flagged (`HasUserDefinedParameterlessConstructor` returns false for `ITypeParameterSymbol`, `IsValueType` is true), but at runtime `new T()` invokes a substituted struct's explicit parameterless constructor while `default(T)` does not — the fix silently changes behavior for exactly the case the method's own comment documents for named types. Exempt type parameters.
- [Major] Reihitsu.Analyzer\Rules\Clarity\RH3201CommentsMustContainTextAnalyzer.cs:50 — `IsEmptyComment` computes `Substring(2, text.Length - 4)` for multi-line comment trivia, but an *unterminated* block comment is still `MultiLineCommentTrivia` and can be 2 or 3 characters (`/*`, `/**`, `/*/` at end of file — a routine mid-typing state in the IDE): the negative length throws `ArgumentOutOfRangeException` and kills the analyzer run (AD0001). For longer unterminated comments the substring silently chops two content characters. Guard the length (and treat unterminated comments as non-empty).
- [Major] Reihitsu.Analyzer\Rules\Naming\RH4115LocalVariableCasingAnalyzer.cs:35 — registration covers only `LocalDeclarationStatement`, but the rule doc claims local variable names generally: foreach iteration variables (`foreach (var BadName in ...)`), for-initializer declarations, classic `using (var X = ...)`/`fixed` declarations, out-var and pattern designations (`out var BadName`, `is int BadName`), and catch variables are never casing-checked. RH4117 covers only parenthesized (deconstruction) designations and RH4119 covers these shapes only for single letters, so the casing gap is an accidental family-wide false negative. Register the missing owners (or route single designations through RH4117).
- [Major] Reihitsu.Analyzer\Rules\Performance\RH1001TypesUsedAsKeysMustImplementEqualityMembersAnalyzer.cs:103 — the type-argument loop checks *every* argument of the matched collection type, but only keys are hashed: `Dictionary<string, MyStruct>` (and the Concurrent/Immutable/Frozen dictionaries) flags the *value* type when it is a struct without equality members — a false positive the title explicitly scopes away ("used as keys"). Tests pair struct keys only with `string` values, so the shape is uncovered. Restrict the check to the key position per collection type (index 0; dictionaries key-only).
- [Major] Reihitsu.Analyzer\Rules\Performance\RH1002TypesUsedForEqualityComparisonMustImplementEqualityMembersAnalyzer.cs:105 — `GetKeyIndex*` return one fixed type-parameter index per method name, but the key's position differs per overload family: `ToFrozenDictionary(source, keySelector)` has type parameters `[TSource, TKey]` while the code returns 0 (checks the *element* type — FP on struct elements, FN on struct keys), and the .NET-8 KVP overloads `ToDictionary(IEnumerable<KeyValuePair<TKey,TValue>>)`/`ToImmutableDictionary(...)` have `[TKey, TValue]` while the code returns 1 (checks the *value*). The `*By` family (`DistinctBy`, `UnionBy`, `IntersectBy`, `ExceptBy`, .NET 6) and `ToHashSet` are absent from `_relevantMethodNames` (FN), and overloads taking a custom `IEqualityComparer<T>` are still flagged although the comparer bypasses the type's own equality (FP). Resolve the key index from the overload's signature (find the `IEqualityComparer<TKey>`/selector return type) instead of the name table.
- [Major] Reihitsu.Analyzer\Rules\Spacing\RH6020DereferenceAndAccessOfSymbolsMustBeSpacedCorrectlyAnalyzer.cs:52 — the address-of arm demands zero whitespace after `&` with no re-lex guard (unlike RH6018/RH6019, it never consults `UnaryOperatorSpacingUtilities.WouldGlueIntoDifferentOperator` — which would not help anyway, since the Core guard covers only `-`/`+`): for `& &x` (address-of address-of in unsafe code) the whitespace-run fix glues `&&x`, which re-lexes as the `&&` operator and no longer parses — the analyzer/fix twin of the session-3 `NoSpaceSpacingRule` Major. The pointer-indirection arm is safe (`* *p` re-lexes unchanged). Widen the Core guard to `&`+`&` adjacencies and consult it here (one fix repairs formatter and analyzer).
- [Major] Reihitsu.Analyzer\Rules\Spacing\RH6002CommasMustBeSpacedCorrectlyAnalyzer.cs:63 — the rule doc promises "followed by exactly one space and must not be preceded by a space", but the analyzer only checks that *some* whitespace or line break follows: space before the comma (`M(a , b)`) and multiple spaces after (`a,  b`) are never flagged, while the formatter's `CommaSpacingRule` normalizes both — analyzer-clean code is silently reformatted by the CLI (G.4 direction 2) and the doc contract (C.8) is broken. The #325 unbound-generic exemption itself is correct (`typeof(Dictionary<,>)` skipped via `OmittedTypeArgumentSyntax`, matching the doc). Check the preceding gap and the exact run length.
- [Major] Reihitsu.Analyzer\Rules\Spacing\RH6004PreprocessorKeywordsMustNotBePrecededBySpaceAnalyzer.cs:50 — no formatter phase moves directives to column zero (`LayoutComputer.SetDirectiveIndentation` and the RH5204 map handle *region* directives only, matching this rule's `#region`/`#endregion` exemption), so every indented `#if`/`#pragma`/`#nullable` line is a CLI-permanent diagnostic (G.4 direction 1) — and Visual Studio's own "Suppress in source" inserts `#pragma warning disable` at member indentation, making the shape ubiquitous. Additionally the line scan excludes only string lines (`GetStringLineIndices`), so trimmed lines starting with `#` inside multi-line comment bodies (`/* # heading */` markdown) and `#if false` disabled text are flagged although no formatter phase or fix may rewrite them — extends the session-5/6 `GetNonFormattableLineIndices` family (RH5601/RH5602/RH8301–RH8303). Add a formatter counterpart (or downgrade) and switch to `GetNonFormattableLineIndices`.
- [Minor] Reihitsu.Analyzer\Rules\Design\RH2004AccessModifierMustBeDeclaredAnalyzer.cs:129 — the registration list lacks `SyntaxKind.RecordStructDeclaration`, so `record struct S { }` without an accessibility modifier is never flagged although every other type kind is (sibling RH7104 registers both record kinds). Related scope seam at :48: `ShouldSkip` tests for *any* interface ancestor, so members of a class nested inside an interface are also exempted, not just interface members proper. Register the record-struct kind and test the immediate containing type.
- [Minor] Reihitsu.Analyzer\Rules\Design\RH2004AccessModifierMustBeDeclaredAnalyzer.cs:55 + Reihitsu.Analyzer\Rules\Organization\RH7104PartialElementsMustDeclareAccessModifierAnalyzer.cs:48 — overlapping claims: RH2004 skips *partial methods* but not partial types, so `partial class C { }` is reported by both RH2004 and RH7104 (two diagnostics, two fixes for one edit). Skip partial type declarations in RH2004 (RH7104 owns them) or retire the overlap.
- [Minor] Reihitsu.Analyzer\Rules\Design\RH2005FieldsMustBePrivateAnalyzer.cs:48 — the parent filter admits `ClassDeclarationSyntax` and `RecordDeclarationSyntax` (which also matches `record struct` nodes) but not `StructDeclarationSyntax`: public fields in record structs are flagged while identical fields in plain structs are not — an accidental seam, since neither doc nor tests motivate treating the two struct kinds differently. Decide the struct policy once and align the filter.
- [Minor] Reihitsu.Analyzer\Rules\Design\RH2006DebugAssertMustProvideMessageTextAnalyzer.cs:92 — asymmetric with RH2007: RH2006 only counts arguments, so `Debug.Assert(condition, "")`/`Debug.Assert(condition, null)` satisfies it, while RH2007 checks the constant value and flags `Debug.Fail("")`. Apply RH2007's `ShouldReport` constant-emptiness test to the Assert message argument.
- [Minor] Reihitsu.Analyzer\Rules\Design\RH2003NotImplementedExceptionShouldNotBeUsedAnalyzer.cs:93 — only `ObjectCreationExpression` is registered; the target-typed form (`NotImplementedException e = new(); throw e;`) escapes, although sibling RH3104 demonstrates the two-registration pattern (`ImplicitObjectCreationExpression`). Register the implicit kind and check the converted type.
- [Minor] Reihitsu.Analyzer\Rules\Clarity\RH3202ExpressionStyleMethodsShouldNotBeUsedAnalyzer.cs:53 (and RH3203...cs:53) — analyzer/formatter scope parity (G.4 direction 2): the formatter's expression-body-to-block conversion covers seven member kinds (methods, constructors, operators, indexers, conversions, finalizers, local functions — session 2), but only methods (RH3202) and constructors (RH3203) have analyzers, so expression-bodied operators/conversions/finalizers/indexers/local functions are silently rewritten by the CLI without ever being reported. Also both diagnostics span the whole declaration instead of the arrow/expression body (C.3). Add the missing rules or document the deliberate gap, and narrow the location.
- [Minor] Reihitsu.Analyzer\Rules\Clarity\RH3003UseStringEmptyForEmptyStringsAnalyzer.cs:56 — the const exemption only recognizes `""` as the *direct* initializer value: an empty literal nested in a constant initializer expression (`const string A = true ? "" : "b";`, `const string B = "" + C;`) is flagged and the `string.Empty` fix produces CS0133 (not a constant). Exempt literals whose nearest enclosing declaration is const (walk up like the attribute check does).
- [Minor] Reihitsu.Analyzer\Rules\Clarity\RH3103UseShorthandForNullableTypesAnalyzer.cs:47 — `ShouldSkip` exempts `typeof(...)` but not `nameof(...)`: `nameof(Nullable<int>)` is flagged and the shorthand rewrite (`nameof(int?)`) does not compile (CS8081). Add the nameof-invocation ancestor to the exemption.
- [Minor] Reihitsu.Analyzer\Rules\Naming\RH4102EventNameCasingAnalyzer.cs:35 — only `EventFieldDeclaration` is registered, so property-style events (`public event EventHandler E { add { } remove { } }` — `EventDeclarationSyntax`) are never casing-checked; no other rule claims them. Register the event-declaration kind.
- [Minor] Reihitsu.Analyzer\Rules\Naming\RH4103MethodNameCasingAnalyzer.cs:46 — no exemption for explicit interface implementations: `void IFoo.parse()` is flagged although its identifier is dictated by the interface member, and for a metadata interface the diagnostic is unresolvable (renaming just the implementation produces CS0539 — session 9 must check the Renamer-based fix). The property quadruple RH4111–RH4114 skips explicit impls only incidentally (they carry no accessibility modifiers) — the family policy is inconsistent. Skip declarations with an `ExplicitInterfaceSpecifier`.
- [Minor] Reihitsu.Analyzer\Rules\Naming\RH4111PrivatePropertyCasingAnalyzer.cs:47 (family RH4111–RH4114, RH4106–RH4109) — the accessibility partition requires an *explicit* modifier keyword, so members with implicit accessibility are claimed by no casing rule: interface properties (`interface I { int badName { get; } }` — RH2004 deliberately skips interface members, so nothing ever demands the modifier either) and implicitly-private class properties/fields are never casing-checked, while RH4103 (methods), RH4110 (const fields) and RH4102 (events) check their kinds regardless of modifiers. The compound-accessibility partition itself is correct — `private protected` is claimed exactly by RH4107/RH4112, `protected internal` exactly by RH4108/RH4113, and no shape is double-claimed. Map implicit accessibility to the corresponding rule (interface members → public) or document the hole.
- [Minor] Reihitsu.Analyzer\Rules\Naming\RH4001TypeNameShouldMatchFileNameAnalyzer.cs:55 — `FindFirstTypeDeclaration` returns the recursion result of the *first* namespace unconditionally: in a file whose first namespace contains no types (`namespace A { } namespace B { class C { } }`), it returns null and the rule stays silent although a type exists (RH7001 flags the double namespace, but RH4001's own contract is not met). Continue scanning siblings when the nested search returns null.
- [Minor] Reihitsu.Analyzer\Rules\Spacing\RH6024BinaryOperatorsMustBeSpacedCorrectlyAnalyzer.cs:50 — the comment "Keyword operators such as `is` and `as` are covered by RH6005" is false: RH6005 matches only `SyntaxKind.OperatorKeyword` (the `operator` keyword in operator declarations), so `is`/`as` binary-expression spacing (`x as  B`) is checked by no rule while the formatter's `OperatorSpacingRule` normalizes every `BinaryExpressionSyntax` operator including keywords — silent CLI reformatting (G.4 direction 2) plus a comment contradicting code (A.10). Drop the keyword filter (or fix the comment and add coverage).
- [Minor] Reihitsu.Analyzer\Rules\Spacing\RH6003SemicolonsMustBeSpacedCorrectlyAnalyzer.cs:56 (family: RH6011...cs:70, RH6012...cs:70, RH6014...cs:55, RH6015...cs:55) — unlike RH6008/RH6016/RH6017, these five have no same-line guard, so when the token starts a continuation line (`int x = 1\n    ;`, `List<int\n    >`) the flagged "preceding whitespace" is the line's *indentation*, and the whitespace-run fix strips it leaving the token dangling at column 0 instead of joining lines (the join is RH5113's/the formatter's job). This is also the reachable instance of the session-4 `RemoveWhitespaceRunCodeFixProviderBase` guard-span Minor (leading-trivia runs exist; the misplaced guard can only cause a false refusal since the reported spans are whitespace-only). Add the same-line guard the sibling rules already have.
- [Minor] Reihitsu.Analyzer\Rules\Spacing (family, all 24 rules) — every spacing rule is a syntax-*tree* action performing its own full `DescendantTokens()`/`DescendantNodes()` scan, several with per-token `GetLocation().GetLineSpan()` line lookups (RH6002, RH6016, RH6017, RH6008); a file is thus token-scanned 24 times for one analyzer package. Same class as the round-1/session-5 Layout tree-action family (RH5106 Minor). Convert to node/token-kind actions or share one scan.

Cross-check resolutions and parity notes (session-7 scope): RH6018/RH6019 resolve the unary-sign cross-check on the analyzer side — both consult Core's `WouldGlueIntoDifferentOperator` before flagging and their whitespace scans stop at line breaks, so no flagged span can join `-`/`+` sign pairs; the zero-gap survey across RH6002/RH6006–RH6017/RH6022 found every other demanded adjacency token-preserving (`* *p`, `(x`, `x]`, `>`+`>` re-lexes to shift tokens the parser re-splits in type contexts, `1 .ToString()` → `1.ToString()` still lexes as int-literal + dot) — the sole re-lex hole is RH6020's `&` (Major above). Directive reachability (session-4 CommentSafeSpanReplacement Major) does *not* extend to the Spacing family: every RH6xxx whitespace scan stops at `\r`/`\n` (RH6002 skips different-line commas, RH6021 exempts line-break neighbours, RH6023/RH6024 treat any non-space gap as no-violation via `IsOperatorGapViolation`), so no directive-bearing gap is ever flagged and no RH6xxx fix can delete a directive. RH2004 confirms the file-local cross-check on the analyzer side (see updated entry). The RH4106–RH4110/RH4111–RH4114 compound-accessibility partition (#286/#333) verifies clean: no member shape is claimed by two rules (`private protected` → RH4107/RH4112 only, `protected internal` → RH4108/RH4113 only, const → RH4110 only), the only claimed-by-none shapes being the implicit-accessibility hole reported above. RH4005 (#295) correctly demands `I` + uppercase + PascalCase for interfaces. RH4121 confirms the analyzer-side reach of the session-1 `CasingUtilities` Major: it is the sole `IsTypeParameterName` consumer, registered on every `TypeParameter`, so `class Pair<T1, T2>` is flagged wherever declared and the fix renames via `ToTypeParameterName` (`T1` → `TT1`) — nothing analyzer-side softens the Core defect; the Core fix repairs analyzer and code fix at once. The session-4 `StructEqualityPerformanceAnalyzerBase` `type.Interfaces` Major affects exactly RH1001 and RH1002 (both funnel struct type arguments through `AreEqualityMembersImplemented`; the session-4 finding's affected-rule list is updated in place). RH3202/RH3203 fixes delegating to the formatter converge (the transform silences the diagnostic in one run); RH3204's escaped-brace/raw-string handling matches #265/#337 on both sides. RH3102's fix is properly guarded for embedded empty statements (`Parent is BlockSyntax or SwitchSectionSyntax` — no brace-less `while (x) ;` corruption). RH4119's lambda-parameter coverage is deliberate (tests enshrine it). RH2101–RH2108 share `NestedTypeAnalyzerHelper` consistently (static exemption only where `static` is legal); RH2102/RH2103 correctly exclude record primary constructors by registering only class/struct kinds. RH0001/RH0002, RH3001 (bool-only guard from the fix wave), RH3002 (parent whitelist with safe-chain restriction), RH3005–RH3007, RH3101/RH3105 (speculative-rebinding pattern, compensating null prechecks noted in session 4), RH3106/RH3107 (EmptyParentheses base) and RH2109 are clean. RH6021's line-break exemptions match the formatter's colon handling. All 96 rule docs for the session-7 scope exist (helpLinkUri contract), and every constructor's `DiagnosticCategory` matches its RH#### range/folder (C.6).

## Reihitsu.Analyzer.CodeFixes

### Session 4 — infrastructure

- [Major] Reihitsu.Analyzer.CodeFixes\Base\CommentSafeSpanReplacementCodeFixProviderBase.cs:106 — the round-1 comment-preservation contract (#291) is genuinely enforced centrally (`SyntaxNodeUtilities.SpanContainsComment` runs at registration, `RegisterCodeFixesAsync` is sealed), but the guard family stops at comments: preprocessor directives and disabled text pass every guard in the new base set. Reachable holes: (a) `CollapseTokenGapCodeFixProviderBase.cs:33` replaces the *entire* inter-token gap with an empty string, so a directive inside the gap is deleted outright — `Foo(a\n#if X\n)` fixed by RH5106 emits an orphaned `#endif` (CS1027); its consumers RH5105/RH5106/RH5108 are line-placement rules whose flagged gap is by definition multi-line, making directive-bearing gaps realistic; (b) `ModifierOrderingCodeFixProviderBase.cs:91` — `ReorderingWouldDropComment` checks comment trivia only, so `#if`/`#endif` between modifiers is deleted when the tokens are rebuilt with default trivia; (c) `TargetAttributePlacementCodeFixProviderBase.cs:142` guards `tokenAfter.LeadingTrivia` with `IsComment` only before wiping it (`WithLeadingTrivia(TriviaList())` at :113), so `[Attr]\n#if X\npublic void M()` loses the `#if`. The two bases that got the policy right — `TypeMemberOrderingCodeFixProviderBase` (`MoveRangeContainsDirectives`) and `TargetAttributeListShapeCodeFixProviderBase` (`HasCommentsOrDirectives`) — show the intended predicate; extend it to the comment-only guards. Same corruption class as the session-2 formatter Critical; escalate per pair once sessions 5/7 confirm the analyzers flag directive-bearing shapes (see open cross-check).
- [Minor] Reihitsu.Analyzer.CodeFixes\Base\RemoveWhitespaceRunCodeFixProviderBase.cs:48 — `root.FindToken(diagnosticSpan.Start)` names its result `precedingToken`, but when the reported whitespace run lies in *leading* trivia (line-start indentation) `FindToken` returns the *following* token (leading trivia is owned by the next token), and `guardSpan` then covers the gap *after* that token instead of the gap being edited — the comment guard inspects the wrong span while `replacementSpan` (the diagnostic span) is deleted regardless. Derive the guard span from the whitespace run's actual neighbours (e.g. `FindTrivia` + owning-token arithmetic). Whether any of the eight derived spacing/layout fixes can report leading-trivia runs is a session-8/9 detail; the base contract is wrong independently.
- [Minor] Reihitsu.Analyzer.CodeFixes\Base\BlankLineSpanRemovalCodeFixProviderBase.cs:59 — the base removes the diagnostic span with no guard whatsoever (contrast: every other new base guards or re-validates); comment/directive safety rests entirely on the seven derived blank-line analyzers reporting whitespace-only spans. Assert the span contains only whitespace/EOL before replacing (cheap, and turns a silent deletion into a no-op) — see open cross-check for the analyzer-side verification.

Verified fixed in this scope (no re-report): #298 EOL handling — no `Environment.NewLine` or hardcoded `\r\n` remains in either assembly; every line-break-inserting base routes through `ReihitsuFormatterHelpers.DetectEndOfLine` (StatementShouldBePrecededByABlankLine :64, RegionDirectiveBlankLine :76, TargetAttributePlacement :96, TargetAttributeListShape :111, DocumentationCommentCodeFixUtilities :62 with per-line detection first). #283 — `StatementBracesCodeFixProviderBase` now delegates to `FormatNodeInDocumentAsync` on the owning if statement, and its owner switch (if/else-if only) exactly matches the RH5405/RH5406/RH5407 analyzer scope (all three flag if statements only). Round-1 `NormalizeModifierTrivia` comment deletion — fixed by the `ReorderingWouldDropComment` registration guard (comments only; directives reported above). Round-1 `[Shared]`/export mismatch — fixed: the abstract bases no longer carry `[Shared]`, and all 220 exported providers do. Round-1 `TargetAttributePlacementCodeFixProviderBase` comment wipe — fixed by the `tokenAfter.LeadingTrivia` comment guard (directives reported above). FixAll: every base returns `WellKnownFixAllProviders.BatchFixer` with `GetType().Name` as a stable, per-provider-distinct equivalence key.
